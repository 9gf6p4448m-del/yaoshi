# A3 完整體驗切片：驗收凍結 v0.1（2026-09-18）

建立時基準：v0.57.23（main `997006b`）。計畫：[plans/2026-09-18-a3-experience-slice.md](../../plans/2026-09-18-a3-experience-slice.md)。六題裁定來源：[交接信末節](../handoffs/2026-09-15-a1-premium-table.md)（使用者 2026-09-18「按照建議」）。一經訂定不得為了通過縮案例、降門檻、換讀者／聽者或以新名字覆蓋舊失敗；要改依 `02 §2.1`。段落順序（#2–#7 聲音 → #8 台詞 → #9 因果 → #10 滿編構圖 → #11 再玩研究）固定，前一段「過」或「未過已簽字」才進下一段。

**沿用不重列的門檻**：A1 凍結檔 G1（規則不變、trace 相等、測試全綠）、G6（perf32 逐輪 ≥ .40、draw／tris 上限、dispose 不增）；A2 #2 硬指標；A2 #4 滿編讀者規程（`2026-09-17-a2-closeout/duel-readers/`）。

| # | 條件 | 什麼實作會讓它變紅 |
|---|---|---|
| 1 | **共通**：每段實作後 trace seeds 1–20 相等（音效／台詞／敘事只用 `S.rngUi`）、`node --test` 全綠、console 0 error；改 `assets/audio/*`／`js/`／`index.html` 必 bump `VERSION`；每次發布逐版核對送達（`grep 'VERSION="'`）。 | 新音用 `Math.random` 或 `S.rng`；改了 sfx.js 沒 bump 版本 |
| 2 | **聲音盤點**：12 支既有音→觸發行號→事件對照表落檔（計畫 §4）；四個目標事件（落籌／封標／揭盅／受咒）的現況各指到一個 `index.html` 行號或明寫「無專屬音」。 | 表憑印象寫、行號對不上 |
| 3 | **四種差別（機械閘，先於聆聽）**：四支新音（落籌／封標／揭盅／受咒）由 `tests/tools/sfx-render.mjs` 在 headless Chromium 以 OfflineAudioContext 渲染（`rnd` 固定 0.5），量三特徵：有效時長（RMS 高於 −40 dBFS 的長度）、頻譜質心（Hz）、峰值（dBFS）。**兩兩之間**（四支互比 6 對，＋每支對既有 12 支的最近鄰）至少一項成立：時長比 ≥ 1.5，或質心比 ≥ 2（差一個八度）。峰值落在既有 12 支的 [min, max] 內。揭盅新音與開市鑼 gong 也要過同一條。 | 四支＝gong 改音高 5%；峰值比 gong 大或比 woodfish 小 |
| 4 | **盲聽指認**（聽板 `tests/tools/sfx-board.html` 模式 A，公開網址，**手機**為主、桌機為對照）：固定種子隨機播四支新音各 2 次共 8 題，每題四選一（落籌／封標／揭盅／受咒）＋「都不像」，聽者＝使用者；門檻 **≥6/8 且每支至少 1/2**；結果由頁面產出 JSON（含種子、順序、答案、裝置 UA）落檔 `2026-09-18-a3-sound/blindlisten-r*.json`。**最多 3 輪**，每輪要換的支數與改法寫進執行紀錄；第 3 輪未過交最佳版標「未過」由使用者簽字。甲／乙挑選（模式 D）與使用者原話必在第 1 輪之前落檔。 | 只在桌機測；同一輪聽兩遍挑好的；沒有挑選紀錄就盲聽 |
| 5 | **既有 12 支聆聽**（模式 B）：使用者逐支回答「留／換／關」，原話落檔；「換」的支數與改法進下一輪同走 #3／#4 程序（對照組＝該支原版特徵）；「關」＝從觸發點移除並記行號。未點名的一支參數不動（`VOICES` 該函式 md5 與基準相同）。BGM 四首本卷不動，使用者若點名要換只記錄、另立小卷。 | 沒給使用者聽就標「留」；順手改沒被點名的支 |
| 6 | **接線與序列**：①`tests/sfx-wiring.test.mjs` 攔 `YS_SFX.play` 記名稱序列，headless 跑 seeds 1–3 各一局：封標音每夜 ≥1 次、揭盅音次數 ＝ 該局得標件數、受咒音次數 ＝ `S.history` 中 `intent==='poison'` 且成功塞入的次數；落籌音以 Playwright 點出價「＋」3 次斷言 3 次；`SKIP`／`?sfx=0` 下全部 0 次。②模式 C 一夜序列連播（落籌×3→封標→開市鑼→揭盅×3→受咒→受擊→天明），使用者回答「哪兩聲糊在一起」＝ **0 對**。③真機實玩一局，規則頁 `audioDiag` 播過次數 ≥ 該局四事件次數總和、且使用者原話確認四事件在遊戲裡都聽到。 | 聽板過了但遊戲沒接線；揭盅音只在第一件響 |
| 7 | **聲音不動的東西**：iOS 喚醒三招（`unlock`／`release`／`ctx.close()`）路徑不改、不新開 AudioContext；BGM `TRACKS`／`FADE`／`volume` 不動；`S.rng` 消耗次數不變（trace 相等已含）。 | 新音自建 AudioContext；改 BGM 音量遷就音效 |
| 8 | **角色台詞**（②）：公開事件集合＝`S.history.nights` 記錄的六類：得標（誰／哪系）、落標（自己出價未得）、毒標（誰塞誰）、盯上宣告、淘汰、請神；每個 AI 角色對每類 ≥2 句模板，每句輸出附 `evRef {round,type,who}`。機械（`tests/lines.test.mjs`，seeds 1–20）：每句 `evRef` 在 `S.history` 對得上；台詞字串不得含**私有資訊**（他人密封出價金額、心願 `wish`、袋中未公開物、AI `aggr`／`spite` 數值）——測試把句中數字與名詞對照這些欄位，命中即紅；同 seed 兩次生成逐字相同；trace 相等。讀者：6 位 context-free 讀者拿「公開事件一行＋台詞一句」判「這句在回哪件事」，**≥4/6**，最多 2 輪。不改 AI 決策、不跨局記憶。 | 台詞寫「你出了 7 命」（密封金額）；evRef 指到不存在的夜 |
| 9 | **因果短敘事**（③）：局末回顧新增一段 3–6 句樣板敘事，**只從 `S.history` 生成**；機械（`tests/ledger.test.mjs`，seeds 1–20）：每句掛 `evRef`、私有欄位比對同 #8、同 seed 兩次逐字相同、0 例外；邊界：異事夜殺到剩一人（無 `recordNightEnd` 那條路）與無毒標的局都產出 ≥1 句不 crash；`S.history` 不變量 `life.length===nights.length+1` 照舊由 `review.test.mjs` 守。讀者：3 位只看敘事回答「誰贏、主因是什麼」，與 `S.history` 實際勝者及主因（口徑：勝者以外壽命單夜跌幅最大的那件事）一致 **≥2/3**，最多 2 輪。 | 敘事引用 `grudge` 以外的 AI 內心；空局 throw |
| 10 | **滿編構圖小卷**（⑤）：目標＝A2 #4 補做 0/6 的三件（拼板舟／福壽綿長／紅帽）；改法**只限**兩條：8v8 同型單位錯位排列（站位／朝向）、描邊改該尊主色；不動模型、ART_BIBLE 色票、演出時長。重讀同 A2 #4 讀者規程（每件 2 位、材料同 `duel-readers`）：**每件 ≥1/2 且合計 ≥4/6**（基線 0/6）；硬指標同 A2 #2（perf32 逐輪 ≥ .40、8v8 draw 不增、trace 相等、矩陣全過）；遮擋率（legend-presence 量法）每件 ≤ 30%；最多 2 輪。 | 改模型換讀者猜中；8v8 改 4v4；換一批更容易的讀者材料 |
| 11 | **再玩研究**（④，門檻自本檔建立即凍結）：使用者＋家人共 **3 人各 2 局**，手機、正式線上版、不預先講解規則、每局後立刻問兩題並記**原話**：Q1「這局主要輸／贏在哪」——「過」＝答案與該局 `S.history` 主因（口徑同 #9）一致，判者＝主對話對照 JSON，不是玩家自評；Q2「下一局想換什麼打法」——「過」＝說得出一個具體要改的動作（「不知道／隨便／再玩看看」＝不過）。以人為單位、該人兩局至少一局達標即算該人過；**兩指標各 ≥2/3 人**。每局 `S.history` JSON＋原話落檔 `2026-09-18-a3-replay/`。結果不論好壞不改口徑、不加人、不加局。 | 只 1 人玩 2 局；主對話代答；事後把「隨便」算成過 |
| 12 | **收卷**：每段一個子卷目錄（執行紀錄、材料、聽者／讀者原始 JSON）；藍圖 §5 A3 狀態改寫並列未過項；交接信與 memory 更新；發布逐版核對送達。 | 只交截圖不留原始 JSON |

## 執行紀錄

- **開卷（2026-09-18）**：計畫與本檔建立於 main `997006b`；S0 盤點寫在計畫 §4（12 支對照、四缺口：落籌／封標＝通用 woodfish、揭盅＝與開市鑼共用 gong `index.html:5628`、受咒＝無專屬音）。下一步 S1 聽板治具＋離線渲染治具。
