# A3 完整體驗切片：驗收凍結 v0.1（2026-09-18）

建立時基準：v0.57.23（main `997006b`）。計畫：[plans/2026-09-18-a3-experience-slice.md](../../plans/2026-09-18-a3-experience-slice.md)。六題裁定來源：[交接信末節](../handoffs/2026-09-15-a1-premium-table.md)（使用者 2026-09-18「按照建議」）。一經訂定不得為了通過縮案例、降門檻、換讀者／聽者或以新名字覆蓋舊失敗；要改依 `02 §2.1`。段落順序（#2–#7 聲音 → #8 台詞 → #9 因果 → #10 滿編構圖 → #11 再玩研究）固定，前一段「過」或「未過已簽字」才進下一段。

**沿用不重列的門檻**：A1 凍結檔 G1（規則不變、trace 相等、測試全綠）、G6（perf32 逐輪 ≥ .40、draw／tris 上限、dispose 不增）；A2 #2 硬指標；A2 #4 滿編讀者規程（`2026-09-17-a2-closeout/duel-readers/`）。

| # | 條件 | 什麼實作會讓它變紅 |
|---|---|---|
| 1 | **共通**：每段實作後 trace seeds 1–20 相等（音效／台詞／敘事只用 `S.rngUi`）、`node --test` 全綠、console 0 error；改 `assets/audio/*`／`js/`／`index.html` 必 bump `VERSION`；每次發布逐版核對送達（`grep 'VERSION="'`）。 | 新音用 `Math.random` 或 `S.rng`；改了 sfx.js 沒 bump 版本 |
| 2 | **聲音盤點**：12 支既有音→觸發行號→事件對照表落檔（計畫 §4）；四個目標事件（落籌／封標／揭盅／受咒）的現況各指到一個 `index.html` 行號或明寫「無專屬音」。 | 表憑印象寫、行號對不上 |
| 3 | **四種差別（機械閘，先於聆聽）**：四支新音（落籌／封標／揭盅／受咒）由 `tests/tools/sfx-render.mjs` 在 headless Chromium 以 OfflineAudioContext 渲染（`rnd` 固定 0.5），量三特徵：有效時長（RMS 高於 −40 dBFS 的長度）、頻譜質心（Hz）、峰值（dBFS）。**兩兩之間**（四支互比 6 對，＋每支對既有 12 支**逐一**比、12 對全過，不取最近鄰）至少一項成立：時長比 ≥ 1.5，或質心比 ≥ 2（差一個八度）；比值一律大除以小。峰值落在既有 12 支的 [min, max] 內。揭盅新音與開市鑼 gong 也要過同一條。 | 四支＝gong 改音高 5%；峰值比 gong 大或比 woodfish 小 |
| 4 | **盲聽指認**（聽板 `tests/tools/sfx-board.html` 模式 A，公開網址，**手機**為主、桌機為對照）：固定種子隨機播四支新音各 2 次共 8 題，每題四選一（落籌／封標／揭盅／受咒）＋「都不像」，聽者＝使用者；門檻 **≥6/8 且每支至少 1/2**；結果由頁面產出 JSON（含種子、順序、答案、裝置 UA）落檔 `2026-09-18-a3-sound/blindlisten-r*.json`。**最多 3 輪**，每輪要換的支數與改法寫進執行紀錄；第 3 輪未過交最佳版標「未過」由使用者簽字。甲／乙挑選（模式 D）與使用者原話必在第 1 輪之前落檔。 | 只在桌機測；同一輪聽兩遍挑好的；沒有挑選紀錄就盲聽 |
| 5 | **既有 12 支聆聽**（模式 B）：使用者逐支回答「留／換／關」，原話落檔；「換」的支數與改法進下一輪同走 #3／#4 程序（對照組＝該支原版特徵）；「關」＝從觸發點移除並記行號。未點名的一支參數不動（`VOICES` 該函式 md5 與基準相同）。BGM 四首本卷不動，使用者若點名要換只記錄、另立小卷。 | 沒給使用者聽就標「留」；順手改沒被點名的支 |
| 6 | **接線與序列**：①`tests/sfx-wiring.test.mjs` 攔 `YS_SFX.play` 記名稱序列，headless 跑 seeds 1–3 各一局：封標音每夜 ≥1 次、揭盅音次數 ＝ 該局得標件數、受咒音次數 ＝ `S.history.nights[].auction[]` 中 `intent==='poison'` 的筆數逐夜加總（**只算 auction 層＝得標者意圖，不算 `bids[]` 層**；`recordAuction` `index.html:4400` 只在得標者為毒標時寫 `intent`，等同「已塞入」）；落籌音以 Playwright 點出價「＋」3 次斷言 3 次；`SKIP`／`?sfx=0` 下全部 0 次。②模式 C 一夜序列連播（落籌×3→封標→開市鑼→揭盅×3→受咒→受擊→天明），使用者回答「哪兩聲糊在一起」＝ **0 對**。③真機實玩一局：判準＝**使用者原話確認四事件在遊戲裡各自聽到**（四句各一）；規則頁 `audioDiag` 只用來排查（`plays` 是含 woodfish／wind 的全域計數器，數字大小**不構成過關證據**）。  | 聽板過了但遊戲沒接線；揭盅音只在第一件響 |
| 7 | **聲音不動的東西**：iOS 喚醒三招（`unlock`／`release`／`ctx.close()`）路徑不改、不新開 AudioContext；BGM `TRACKS`／`FADE`／`volume` 不動；`S.rng` 消耗次數不變（trace 相等已含）。 | 新音自建 AudioContext；改 BGM 音量遷就音效 |
| 8 | **角色台詞**（②）：公開事件集合＝`S.history.nights` 記錄的六類：得標（誰／哪系）、落標（自己出價未得）、毒標（誰塞誰）、盯上宣告、淘汰、請神；每個 AI 角色對每類 ≥2 句模板，每句輸出附 `evRef {round,type,who}`。既有 `ROLES.*.lines` 六鍵（`win`／`lose`／`poison`／`poisoned`／`bwin`／`blose`，`index.html:1135`）對應得標／落標／毒標三類；盯上宣告、淘汰、請神三類**新增鍵**，不塞進既有鍵。機械（`tests/lines.test.mjs`，seeds 1–20）：每句 `evRef` 在 `S.history` 對得上；台詞字串不得含**私有資訊**（他人密封出價金額、心願 `wish`、袋中未公開物、AI `aggr`／`spite` 數值）——測試把句中數字與名詞對照這些欄位，命中即紅；同 seed 兩次生成逐字相同；trace 相等。讀者：6 位 context-free 讀者拿「公開事件一行＋台詞一句」判「這句在回哪件事」，**≥4/6**，最多 2 輪。不改 AI 決策、不跨局記憶。 | 台詞寫「你出了 7 命」（密封金額）；evRef 指到不存在的夜 |
| 9 | **因果短敘事**（③）：局末回顧新增一段 3–6 句樣板敘事，**只從 `S.history` 生成**；機械（`tests/ledger.test.mjs`，seeds 1–20）：每句掛 `evRef`、私有欄位比對同 #8、同 seed 兩次逐字相同、0 例外；邊界：異事夜殺到剩一人（無 `recordNightEnd` 那條路）與無毒標的局都產出 ≥1 句不 crash；`S.history` 不變量 `life.length===nights.length+1` 照舊由 `review.test.mjs` 守。讀者：3 位只看敘事回答「誰贏、主因是什麼」，與 `S.history` 實際勝者及主因（口徑：勝者以外壽命單夜跌幅最大的那件事；跌幅同分取較晚的夜，同夜取 `auction[]` 序號較小者，口徑寫進 `ledger.test.mjs` 同一函式，讀者判定與測試共用）一致 **≥2/3**，最多 2 輪。 | 敘事引用 `grudge` 以外的 AI 內心；空局 throw |
| 10 | **滿編構圖小卷**（⑤）：目標＝A2 #4 補做 0/6 的三件（拼板舟／福壽綿長／紅帽）；改法**只限**兩條：8v8 同型單位錯位排列（站位／朝向）、描邊改該尊主色；不動模型、ART_BIBLE 色票、演出時長。重讀同 A2 #4 讀者規程（每件 2 位、材料同 `duel-readers`）：**每件 ≥1/2 且合計 ≥4/6**（基線 0/6）；硬指標同 A2 #2（perf32 逐輪 ≥ .40、8v8 draw 不增、trace 相等、矩陣全過）；遮擋率（legend-presence 量法）每件 ≤ 30%；最多 2 輪。 | 改模型換讀者猜中；8v8 改 4v4；換一批更容易的讀者材料 |
| 11 | **再玩研究**（④，門檻自本檔建立即凍結）：使用者＋家人共 **3 人各 2 局**，手機、正式線上版、不預先講解規則、每局後立刻問兩題並記**原話**：Q1「這局主要輸／贏在哪」——「過」＝答案與該局 `S.history` 主因（口徑同 #9）一致，判者＝主對話對照 JSON，不是玩家自評；Q2「下一局想換什麼打法」——「過」＝說得出一個具體要改的動作（「不知道／隨便／再玩看看」＝不過）。以人為單位、該人兩局至少一局達標即算該人過；**兩指標各 ≥2/3 人**。每局 `S.history` JSON＋原話落檔 `2026-09-18-a3-replay/`。結果不論好壞不改口徑、不加人、不加局。 | 只 1 人玩 2 局；主對話代答；事後把「隨便」算成過 |
| 12 | **收卷**：每段一個子卷目錄（執行紀錄、材料、聽者／讀者原始 JSON）；藍圖 §5 A3 狀態改寫並列未過項；交接信與 memory 更新；發布逐版核對送達。 | 只交截圖不留原始 JSON |

## 執行紀錄

- **開卷（2026-09-18）**：計畫與本檔建立於 main `997006b`；S0 盤點寫在計畫 §4（12 支對照、四缺口：落籌／封標＝通用 woodfish、揭盅＝與開市鑼共用 gong `index.html:5628`、受咒＝無專屬音）。下一步 S1 聽板治具＋離線渲染治具。
- **開卷 read-back 修訂（2026-09-18，動工前、fresh sonnet 讀者）**：①#6③ 原「`audioDiag` 播過次數 ≥ 四事件總和」經核對 `index.html:2151–2160` 是含 woodfish／wind 的全域計數器＝**恆真**，依 `02 §6.1` 第 6 條自行修正為「使用者原話四句各一」，數字只供排查；②#6① 受咒次數口徑明訂為 auction 層 `intent==='poison'` 筆數（`bids[]` 層含落標毒標企圖會偏高）；③#3 最近鄰改為對 12 支逐一比、比值大除以小；④#8 補既有六鍵事實（計畫 §2 原誤寫三類）與三類新增鍵；⑤#9／#11 主因平手判法補齊。五條皆為細化或加嚴，無一提高通過機率。
- **S1 聽板治具（2026-09-18）**：`tests/tools/sfx-board.html`（A 盲聽／B 逐支／C 序列／D 甲乙，JSON 輸出）＋`tests/tools/sfx-render.mjs`（OfflineAudioContext 三特徵＋#3 閘門比對＋逐支 `fnMd5`）。既有 12 支渲染基準落 `2026-09-18-a3-sound/sfx-features-base-0.57.23.json`（#5 對照組；同 seed 重跑 12 支×5 欄 0 diff）。headless 冒煙（手機視口、`?map=` 替身）：A 同種子順序相同、全對 8/8 過、錯一題 7/8 仍過、換種子順序不同；B／C／D 各輸出 JSON；ctx running、播 45 次、console error 0；無 `?map=` 時 A／C／D 顯示缺聲部並停用。閘門比對在替身上乾跑一次只驗輸出格式（hurt 峰值低於範圍→FAIL 屬預期），**不是 #3 的結果**。
- **S2 候選音（2026-09-18，v0.57.24）**：`assets/audio/sfx.js` `VOICES` 新增 8 支候選 `chip_a/b`（玉籌落桌／木籌三連落）、`seal_a/b`（紙摩擦＋落印／摺紙＋壓印兩下）、`reveal_a/b`（陶盅揭蓋／瓷蓋輕碰兩下）、`curse_a/b`（陰氣下沉／陰氣滲入抖音），只走既有 `tone`／`noise`／同一 `ctx`（#7）。#3 機械閘先於聆聽：`sfx-render.mjs` 渲染 20 支（`2026-09-18-a3-sound/sfx-features-candidates-0.57.24.json`），16 種甲乙組合逐一跑 `gate()`（新音互比 6 對＋每支對既有 12 支）＝ **16/16 PASS**（`gate3-combos-0.57.24.txt`）；既有 12 支特徵與 `fnMd5` 對 S1 基準 0 diff（#5）。調整過程：第一版 3 對紅（curse_a/gong 時長 1.45、chip_b/whoosh、seal_b/babble）→ 拉長／縮短時長；curse_b 抖音原本加在包絡上尾巴收不掉（eff 3350ms＝到 stop 硬切），改掛第二級增益後 1710ms。trace seeds 1–20 對 HEAD equal；`node --test` 96 檔案／案例（版本釘測試同步 0.57.24）。遊戲**尚未接線**，等使用者在聽板模式 D 挑定（品味題）。
- **S2 接線（2026-09-18，v0.57.25）**：使用者聽板模式 D 挑定（`2026-09-18-a3-sound/ab-picks-r1.json`＋對話補答：落籌乙「比較有落籌的感覺」、封標甲「單純的沈悶聲」、揭盅甲、受咒乙）；`sfx.js` 以別名 `chip`／`seal`／`reveal`／`curse` 指向挑定候選，#3 對定案四支正式閘門 **54/54 對＋峰值 4/4 PASS**（`gate3-final-0.57.25.json`）。接線 `index.html`：出價「＋」（出價視窗＋押寶夜）`data-sfx="chip"`；封標在 `submitHumanBids` 通過重入閘後播一次、該下主鈕不播木魚（委派在 capture 階段認 `onclick===submitHumanBids`）；揭盅迴圈每件得標 `sfx("reveal")`（開市鑼仍 gong）；得標者 `intent==="poison"` 在揭盅音 0.9 秒後播 `curse`；新增 `?sfx=0` 本次靜音。**#6① `tests/sfx-wiring.test.mjs` 綠**：seeds 1–3 整局（夜數 9／9／12）封標 9／9／12＝每夜 1、揭盅 26／26／36＝得標件數、受咒 7／5／2＝毒標得標筆數、「＋」三下＝3 聲落籌且 0 木魚、SKIP 期間 0 聲、`?sfx=0` 整局 0 聲、pageerror 0；鑑別力：接線前三輪同測試紅在「第 1 夜沒有封標音」（封標 0、木魚 337）。trace seeds 1–20 對 HEAD equal；`node --test` 全綠（`reveal-table.test.mjs:64` 原始碼掃描改認 `sfx("reveal")`、版本釘 0.57.25）。修法過程：①委派原本用一次性 `data-sfx` 掛主鈕，被盯上相位的主鈕點擊提前拆掉→改綁 handler；②只認 handler 會讓連點每下都響（seed 1 封標 82 次）→改在 handler 通過閘後播；③測試直接 `newGame` 跳過 `startEntry→initSfx()`，委派根本沒掛→測試補叫同一支 `initSfx()`。觀察：seed 3 第 11 夜請神對決曾在 headless 卡在 `PW_WAKE`（未重現於最終輪），治具加停滯 3 秒按跳過脫困，S3 真機實玩留意。#6②③ 與 #4／#5 待使用者（S3）。
- **S3 第 1 輪（2026-09-18，使用者 iPhone Safari，v0.57.25，`standin:false`）**：#4 盲聽 `2026-09-18-a3-sound/blindlisten-r1.json` 種子 20260918：**6/8 過**（落籌 2/2、封標 1/2、揭盅 1/2、受咒 2/2；錯的兩題是揭盅→答封標、封標→答揭盅；重播次數 6,2,1,1,2,3,1,2）。#6② 序列 `sequence-r1.json`：11 聲全播、糊在一起 **0 對，過**。#5 逐支 `voices-verdict-r1.json`：12 支 11 留、**hurt 換**（原話「感覺很不夠精緻的聲音」）；BGM 未點名。hurt 依 #5 走甲／乙→挑→#3（對照組＝原版 hurt 120ms／650Hz／−10.07 dBFS）。#6③ 真機實玩四句待使用者。
- **S3 hurt 換案（2026-09-18，v0.57.26）**：`sfx.js` 加 `hurt_a`（紙裂：3 kHz 帶通撕紙＋極輕悶撞，50ms／4141Hz／−9.76 dBFS）、`hurt_b`（深悶撞：90→38Hz 沉音＋低頻餘震，180ms／86Hz／−3.65 dBFS）；對照組原版 hurt 120ms／650Hz／−10.07 dBFS。#3 各自對其餘 11 支＋四支新音＋另一案逐一比 **17/17 PASS、峰值在範圍內**（`gate3-hurt_a-0.57.26.json`、`gate3-hurt_b-0.57.26.json`）；hurt_a 前三版（悶撞主導 137Hz／162Hz／1070Hz）分別紅在 stamp／woodslam／woodfish，改成撕紙主導才過。既有 12 支 fnMd5 對 S1 基準 0 diff（原版 hurt 未動，遊戲仍播原版）。聽板模式 D 以 `?ab=hurt:hurt_a|hurt_b` 加題（不算替身）。trace equal、`node --test` 96 綠（接線測試未重跑：本輪未動 index.html 接線與 sfx 四事件）。待使用者挑定後接線＝`VOICES.hurt` 改指向挑定案。
- **S3 hurt 接線（2026-09-18，v0.57.27）**：使用者聽板挑 hurt **甲「紙裂」**（`2026-09-18-a3-sound/ab-picks-r2.json`，四事件重挑仍是乙／甲／甲／乙，無原話）。`sfx.js` `VOICES.hurt = VOICES.hurt_a`、原版改名 `hurt_v0` 留作對照（特徵與 S1 基準 hurt 逐值相同：120ms／650Hz／−10.07）。正式閘門 `--new=hurt` 對其餘 11 支＋四支新音 **15/15 PASS**（`gate3-hurt-final-0.57.27.json`）；trace equal；`node --test` 96 綠（接線測試未重跑：只換聲部別名，觸發點與計數不變）。#5 至此：11 留未動（fnMd5 同基準）、hurt 已換。剩 #6③ 真機實玩四句。
