# 派工：《紙紮夜戰》卷 C2 前置——剪影風格樣張（2026-09-04）
目標：使用者已裁定 27 件法寶各做一張 SVG 紙紮剪影（卷 C2）。剪影風格是品味題，量產前先出 **3 種差異化風格**讓使用者挑。
產出：`assets/paper/samples/<style>/<item>.svg`，每種風格各畫 3 件（每體型一件：群體＝椅仔姑竹椅、精英＝虎爺印、作祟＝魔神仔燈，
若 POOL 裡名稱不同以 `index.html` POOL 為準，選各體型第一件）＋ `docs/experiments/2026-09-04-paperwar-C2-samples.html` 預覽頁
（單檔、inline SVG、深色底、844×390 直式手機可看、三風格並排、每張標風格名與體型）。
風格方向（要拉開差距，不要三種都像）：甲 台灣廟會紙紮（多層錯位厚剪影、金紙紋、彩紙邊）；乙 皮影戲（鏤空細線、關節分段、暖黃背光）；丙 符咒版畫（粗黑木刻線、朱砂單色點綴）。
限制：純 SVG，無外部資源、無 raster；每張 ≤12KB；三系顏色用 `assets/theme.css` 的 `--c-*`；viewBox 統一 0 0 200 260；
群體剪影要能被程式橫向複製 N 個不穿幫（左右對稱或留邊）。不動 `index.html`／`js/`／既有 assets。不 commit 不 push。
回報：預覽頁路徑＋每風格一句「辨識點」。
