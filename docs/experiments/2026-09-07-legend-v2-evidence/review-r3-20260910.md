# 請神 2.0 第三輪對抗覆審（fresh context，唯讀）

- 待審：worktree `.claude/worktrees/agent-a6e8673e38160128f`，分支 `worktree-agent-a6e8673e38160128f`，HEAD `3780746`
- 基準：`main` = `ddd2ca1`（merge-base 實為 `75e1bfd`）
- 我實跑過的東西（全部在該 worktree、埠 8897 未被占用）：
  - `node --test tests/*.test.mjs` -> 9 檔全過（legend 24 案）
  - `node tests/legend.test.mjs old-l.html`（`old-l.html` 已核對 sha1 = `b38980a:index.html`）-> 4 過 / 20 失敗
  - `node tests/tools/legend-drive.mjs <scratch>/rv-drive.json --port=8897 --seeds=1,3 --all --base=docs/experiments/2026-09-07-legend-v2-evidence/felt-base.json --slack=52` -> 通過
  - `node tests/tools/legend-gate.mjs 200 --only=G0 --old=<scratch>/main-tip.html`（main-tip = `ddd2ca1:index.html`）-> G0 綠
  - `node tests/tools/legend-gate.mjs 300 --only=G2,G3,G4,G9 --old=<scratch>/main-tip.html` -> G2 綠 / G3 紅（已知位移）/ G4 綠 / G9 +8.90pp 綠

## 結論

可合併，但合併前先處置 MEDIUM-A（版號）與 MEDIUM-B（證據檔進版控時是空的／被洗掉）。無 CRITICAL、無 HIGH。

---

## 一、前兩輪 findings 三態裁定

### 第一輪（對 e1adf6f）

**HIGH-1（#felt 直向溢出 47-77px、市集卡預覽被裁、G6 無直向門檻）-> 真的修好（在使用者同意的新門檻內）**
依據：`index.html:97-101`（`.shrine.slim` 平常收一列）／`index.html:118-126`（`.incbar` 併進 `#budget`、直式收起）／`index.html:4595-4597`（`updateBudget` 前置 `incbarHTML`）／`index.html:4482,4491,4514`（卡面單行 `.stat`、招式移到 `openSheet`）／判定治具 `tests/tools/legend-drive.mjs:134-147`。
我自己重跑（seeds 1,3、844x390、dpr=2）：第 1 夜 4 格全 0、12 個判定格全綠，最緊的 `1|3|出價` 87 <= 49+52。
殘留：第 3 夜出價頁仍有 87px（基準自己 49），報告 B4:442 已誠實揭露；原症狀「市集卡預覽被裁」在第 3 夜出價頁仍可能發生，只是落在使用者同意的 +52px 內。

**M-1（回天文案 36% 假訊息）-> 真的修好**
`index.html:4754-4757` 改成「開標之夜沒有人請得動（有資格的人都沒上香）」＋「殘留的香火 N 點散去」；局末回顧 `index.html:5489` 同步。純顯示層。

**M-2（供奉排在詛咒 drain 前）-> 真的修好**
`index.html:3585-3596`：`settleTithe` 掛在 `recordCred` / `judgeWish` 之後、`NIGHT_REGEN` 之前；`index.html:2880-2887` 跳過 `life<=0`。行為證據是 `tests/legend.test.mjs` 的「G5④神債最後收：壽命 2＋詛咒品 -1 ... 人活著」；我實跑對基準紅在行為斷言。

**M-3（G9 零戰力對照留 eff）-> 真的修好**
`tests/tools/legend-gate.mjs:270` 的 `zero` 同時做 `count:0` 與 `delete L.eff`；另印「只拿掉共鳴／只拿掉戰力」兩組記錄項（:271-272,276-277）。方向是變嚴：拿掉 eff 後對照組更弱、差值 8.85 -> 9.05pp，離上界 10 更近。

**M-4（刪了 5 個仍成立的守衛測試）-> 真的修好，且自評誠實**
五案回補於 `tests/legend.test.mjs`（傳說不進 `S.deck` / `POOL`、AI 燒香資料表驅動、`settleShrinesEnd` 冪等、回天不改寫非本輪 `history.life`、傳說招式進 `paperWar`）。我實跑基準 `old-l.html`：其中 3 案在失敗清單、2 案不在（基準即綠），與報告 A1:309 的「三紅兩綠＝回歸守衛」逐字一致。

**L4（燒香變劣勢 -6.4pp 無下界）-> 表面修好**
下界確實加了（凍結檔 §2.1 修訂三、`legend-gate.mjs:307` 的 `c1lo=d1>=-8`），屬加嚴、不違反 §2.1。但門檻設在 -8pp，而觸發這條 finding 的值是 -6.4pp（報告 A1:315 自己記 -6.00pp）——這道守衛抓不到當初讓它被提出來的那個情境，對原症狀鑑別力為 0。現行 -4.47pp，不影響本次放行。

**L7（送神鈕真瀏覽器零覆蓋）-> 真的修好（覆蓋已建立）**
`tests/tools/legend-drive.mjs:150-160` 真的 `showBag(0)` 後點 `#modalbox .tithebtn`（走產品 `giveUpLegend` -> `releaseLegend`）。我重跑 seeds 1,3 得到 `giveUp=1`。但它不進 pass 條件，見 LOW-E。

### 第二輪（對 ecbf21d）

**凍結檔 §2.1 修訂四數字錯 -> 真的修好**
凍結檔改成實測表，基準值有落檔證據 `felt-baseline.txt` / `felt-base.json`（`1|3|出價` = 49），量測條件寫死 844x390 / dpr=2 / seeds 1,3。我重跑 `--base=felt-base.json` 讀到的就是 49。

**報告自評兩處不實 -> 真的修好（報告端）／沒修到（治具與證據檔端）**
報告 A1:307、A1:309 已改正並自己講明「那是 `life<=0` 跳過造成的定義性數字」。但同一個定義性 0 原封不動留在 `tests/tools/legend-gate.mjs:165-168` 與證據 `gate-v3-10000.md`，還寫著「這就是改口徑的理由」——見 MEDIUM-C。

**殘留 30px 是燒香列不是神龕列 -> 真的修好**
`.incbar` 由 `#stage` 自成一列改成 `#budget`（在 `#south`）的行內元素（`index.html:118-126`、`4595-4597`）；熱座清場名單同步（`index.html:4341`）。實測第 2 夜四格由 30/49 回到 0。

**G2 改雙判 -> 真的修好**：凍結檔 §2.1 修訂五 ＋ `legend-gate.mjs:160,554`（`tAll` 進 `verdict.G2`）。我實跑 n=300：新口徑 2.67% / 原口徑 9.67%，兩個都判、都在帶內。

**G9 補絕對上限 85% -> 真的修好**：凍結檔 §2.1 修訂六 ＋ `legend-gate.mjs:282,288`。我實跑 n=300：71.92%。

**G3 記已知位移（丙案）-> 真的修好**：凍結檔 §2.1 修訂七；`legend-gate.mjs:586` 仍把 `verdict.G3=false` 原樣印出來，沒有改門檻、沒有把紅燈藏起來。

---

## 二、移動及格線掃描

逐檔比對過（`git diff main...HEAD`）：`docs/experiments/2026-09-07-acceptance-legend-v2.md`、`tests/tools/legend-gate.mjs`、`tests/tools/legend-drive.mjs`、`tests/tools/felt-probe.mjs`（新增）、`tests/legend.test.mjs`（含 `setup()` fixture 逐字比對）、`tests/tools/duel-drive.mjs`、`lineup-shot.mjs`、`scene-shot.mjs`、`layout-shot.mjs`、`mkt-probe.mjs`、`overflow-probe.mjs`、`legend-v2-sheet.py`、`.gitignore`。

結論：沒有同意清單外的降標。

- 加嚴（合法）：G1 加下界 -8pp；`legend-drive` 加 `--all`（六顆全跑、不提早收工）；`okPath` 多要求 `rewards>0`；新增 `okVert` 且「沒帶 --base= 一律不算通過」（`legend-drive.mjs:295`）。
- 同意清單內：G6 直向甲案、G3 丙案、G2 雙判、G9 絕對上限 85%。四條與凍結檔、治具實作逐條對得上。
- `tests/legend.test.mjs` 的 `setup()` fixture（`NIGHT_REGEN=0` 等）一字未改（用 `git show <merge-base>:tests/legend.test.mjs` 逐字比對）。舊 N1 洗牌公平性那批測試被刪，是因為擲骰整組移除（`shrineRollOrder` 不存在了），不是規避。
- 沒有 try/catch 吞紅、沒有 null 蓋掉、沒有斷言位置下移。`index.html` 沒有殘留 `INC_K` / `INC_PITY` / `shrineRollOrder` 的活引用（只剩註解）。
- 兩處「讓通過機率上升」但屬跟隨實作搬遷、已揭露、判為合法：
  1. `legend-drive.mjs:257,272` 熱座 incbar 選擇器由 `#stage .incbar` 擴成 `#stage,#south` 兩者。`sawIncbar` 因此由恆假變可真，但 `incbarAtHandoff===0` 是計數超集、反而更嚴；作者已在報告 B1:409 主動揭露為零鑑別力修正。
  2. `legend-drive.mjs:141` 的 `judged=(base!=null)`：沒有同格基準的種子只印不判，是凍結檔二版把量測條件寫死在 seeds 1/3 的直接後果。副作用見 LOW-D 與 LOW-G。

---

## 三、新 findings

### MEDIUM-A：合併後首頁版號會跳到 v0.52（本卷計畫是 v0.50）
- 觸發與證明：`index.html:1918` 是 `const VERSION="0.52"`；`git show main:index.html | grep VERSION=` 得到 `"0.49"`。
- 成因：commit `a06cbac` 的訊息寫「main 已到 0.51」，但 main 隨後被 `ddd2ca1`（版號更正 0.51 -> 0.49）修正過，這個前提已不成立。
- 影響：使用者是靠首頁那行判斷有沒有推上線；照現況合併會直接跳 0.49 -> 0.52、略過 0.50 與 0.51。報告 391-393 也寫著同一個錯誤前提，要一併改。

### MEDIUM-B：兩個關鍵證據檔進版控時是空的／被進度列洗掉的
- `git show HEAD:docs/experiments/2026-09-07-legend-v2-evidence/gate-v3-G1-10000.md | wc -c` 得到 **0**（工作區那份才有 1201 bytes 內容）。
- `gate-v3-10000.md`：committed 與 working 同為 7622 bytes，但 committed 的第 17 行是一整行空白——CR 進度列把 **G1 整張表與三條判定行洗掉了**，只有 working 版有。兩檔目前都還在未 commit 狀態（`git status` 顯示 M）。
- 影響：照現況合併，repo 裡「G1 n=10000 通過」這件事沒有證據；報告 B3 表格第二列指的就是那個 0 byte 檔。
- 我另用 python 對 committed 與 working 去 ANSI 後 diff：差異只在 G1 段與各段耗時，G2/G3/G4/G9 的數值逐字相同（同一批種子、決定性），內容本身可信。
- 修法：把工作區那兩份（或 `gate-v3-10000.clean.md`）commit 進去即可，不必重跑。

### MEDIUM-C：治具仍拿一個恆為 0 的數字當「改口徑的理由」
- 位置：`tests/tools/legend-gate.mjs:165-168`，輸出落在 `gate-v3-10000.md`：「當夜就出局 0（0.0%）... 這就是改口徑的理由」。
- 為什麼恆為 0：`settleTithe`（`index.html:2880-2887`）跳過 `life<=0`；供奉只在 `life-t>=1` 時才扣、否則改成回天，所以走完這一支的人 `life>=1`；緊接著 `NIGHT_REGEN=5` 天明回血；最後才蓋出局旗。所以「斷供當夜出局」不可達，`evSame` 恆為 0（02 §6.1 第 6 條的恆假式）。
- 為什麼算 finding：第二輪已經抓過同一件事，報告端改了、治具端沒改；而且緊鄰的另一個恆 0（`life>TITHE_WARN`）有明寫揭露，這一條沒有——同一份輸出裡一個揭露一個不揭露，讀者會把它當實測。
- 影響：不影響任何判定（G2 兩個口徑都判在真實數字上），純屬證據呈現。

### LOW-D：G6「第 1 夜幾頁」的口徑對不上
凍結檔與治具量的是「第 1 到 3 夜的盯上／出價兩頁」，也就是第 1 夜每顆種子 2 格（`acceptance-legend-v2.md` §2.1 修訂四二版；`legend-drive.mjs:136`）。派工轉述的使用者同意寫的是「第 1 夜三頁 0」。我無法從 repo 判定哪一份是使用者原話；若原話是三頁，就是量測範圍縮小＝移動及格線。請主對話核對一次。

### LOW-E：L7 的覆蓋沒有守衛，會靜默退回零覆蓋
`legend-drive.mjs:293-296`：`okPath` 只含 `taken` / `dawnShrines` / `rewards`，`rec.giveUp` 只印一行警告。日後那一顆種子的行為變了（那局沒請到傳說），「送神回天」在瀏覽器的覆蓋率就回到 0，而閘門照樣綠。

### LOW-F：giveUpLegend 的註解理由不成立
`index.html:5372-5375` 註解寫「不 refund——夜末的供奉還沒扣（袋子是出價階段開的）」。但 `#south` 的袋子鈕（`index.html:532`）與座位卡（`index.html:3706`）任何階段都按得下去，包含夜末結算之後，那時本夜供奉已經扣了。行為本身可辯（當夜已供奉、之後不再扣），但註解給的理由是錯的，會誤導下一個改這裡的人。

### LOW-G：直向門檻在 seeds 5/6 量到超標值，只是沒有基準所以不判
`drive-all6.txt`：`5|3|出價` 74、`6|3|出價` 74，上限 52（無基準所以不判）。凍結檔把量測條件寫死在 seeds 1/3 是使用者同意的，所以不構成違規；但這代表「第 3 夜出價頁溢出」在其他種子上更嚴重，B4 的試玩必調值得留著。

---

## 四、已查過、不是 finding 的項目（列出以示掃過）

- LEGENDS 兩邊合併：`m`（3D 模型鍵）與 `eff`（共鳴）並存；`buildArmy` 走 `x.ab||x.m||null`（`index.html:2997`）所以三尊吃專屬 GLB；`collectEffects` 走 `it.eff`（`index.html:1769`）所以共鳴生效。G10 單元測試對基準紅在 `facCount` 4 vs 3。
- G0 對真正的 main tip（`ddd2ca1`，不是治具用的 `old-main.html` = `75e1bfd`）我另跑一次仍逐位元組相等；兩者只差 VERSION 字串，不進 trace。
- 一人一尊：判準用 `sh.takenBy`（`hasLegend`，`index.html:2707`）而不是袋子，所以回天之後不能再搶第二尊；引擎（`resolveShrines`）、AI（`aiIncense`）、UI（`initIncense` 與 `incbarHTML`）三處共用同一支。我實跑 n=300，「三尊同一人」為 0 局。
- 同分規則 `shrineWindOrder` 加上嚴格大於 `if(h>best)` = 風位順時針第一個，與規則頁文案一致。
- 熱座：`initIncense` 每席重設（`index.html:4248` 與 `4314`）、`showHandoff` 清場含 `#south .incbar`（`index.html:4341`）、`updateBudget` 兩條分支都前置 incbar（`index.html:4595`、`4597`）。實跑熱座治具 `sawIncbar=true`、交棒當下 `.incbar` 個數 0。
- `unitRow` 對 `buildArmy` 的 swarm fallback：`pwDefaultUnit` 恆回 `count:1`，所以「取不到 team 卻回傳假預覽」不可達。
- `showTitheAsk` 在 `resolveBattles()`（`index.html:5297`）之後才 await（`index.html:5330`），死者被 `p.alive` 濾掉。
- 直式把 `#market` 與 `#budget .incbar` 收起來：`#rotateHint` 在所有 portrait（`index.html:46`，無寬度條件）都是不透明全屏蓋板，玩家看不到被收起來的東西。
- `.gitignore` 新增 `*.err`、`_rv-*`、`legend-drive.json`、`COMMITMSG.txt`、`tests/tools/_*.mjs`：都是工作檔，沒有遮掉任何證據。

---

**可合併**（先處置 MEDIUM-A、MEDIUM-B）。
