# 驗收凍結檔：角色平衡 B 小卷（2026-09-07；使用者裁「照建議」）

基準 SHA：`2c1760e`（v0.47）。VERSION 0.50（2.0 佔 0.48、可讀性 0.49；合併時再對）。依據：`docs/experiments/2026-09-07-role-balance-report.md` §7（A–E 選項）。使用者裁定：①斷手書生：門檻已退回 4（上一卷），本卷改它的 AI 出價 hook（`onAiValue`／`onAiPlan`，量法卷量到 −7.4pp 自傷）②紅衣婆婆：記仇被動接到玩家側（現在只在 AI 側生效）③收驚婆不動 ④量法不改（座位 0＝真人座位，(b) 量法沿用）。

## 範圍
- `index.html` `ROLES.duanshou` 的 AI hook（改法二選一：把「集齊同系」估值改成不超過 `ctx.val×1.5` 的加成、或把它從「每件都加」改成「只對能湊成套的那一件加」——擇一，理由寫報告）；`ROLES.hongyi` 記仇三 hook 的觸發點改成玩家與 AI 共用（`onBidEff`／`onBidSettle` 那條路徑），效果數值不變；規則頁 desc 若語意變才改；GAME_DESIGN 角色表、changelog、GUIDE §11.23 補一段。
- **不動**：其他八隻、AI 風格參數、量法、共鳴。

## 驗收條件（動手前訂；門檻不得為了過而調）
- **C0 鑑別力**：`tests/roles-balance.test.mjs` 加兩案——斷手 AI 估值在「三件同系可湊」與「不可湊」兩袋的差值變小且不為負；紅衣在玩家座位被毒標塞中時記仇 hook 真的觸發（對基準紅在行為斷言）。
- **C1 帶內**：(b) 量法 n=10000：斷手 ≥ **18%**、紅衣 ≥ **18%**；其餘八隻仍在 [18,33]（收驚婆允許維持 0.47 的 34.2±1，本卷不處理）；改前基準：斷手 15.6、紅衣 16.3。
- **C2 座位控制**：斷手、紅衣另量座位 2（n=10000），與座位 0 差 ≤3pp；否則報告標「含座位效應」。
- **C3 等價**：移除斷手與紅衣後 `trace(1..20)` 與基準逐位元組相等；`--mutate` 反面紅。
- **C4**：9 套測試綠；`duel-drive` 6 場 0 error；局長中位 10～12。
- **C5 範圍**：`git diff --stat` 只含 `index.html`（兩隻角色段）、測試、docs、報告、本檔。**最多兩輪**：第一輪未達 C1 只准在本則列的兩個改法內換一種，第二輪仍未達就停手列選項（不得動其他角色）。

什麼實作會讓 C1 假綠：順手調 aggr/spite（量法卷已定案、本卷不動）；把 (b) 量法換掉。
