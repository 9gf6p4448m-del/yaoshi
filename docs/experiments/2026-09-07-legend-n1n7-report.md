# 實跑報告：請神小卷 N1（同香火擲序洗牌）＋N7（天井免燒即可請）＋傳說預設開（2026-09-07，v0.44）

驗收凍結檔：`docs/experiments/2026-09-07-acceptance-legend-n1n7.md`（A0–A9，動手前訂、全程未動）。
基準 SHA：`ff227a7`（v0.43.1）。分支：`worktree-agent-a4d83c4bab668f0e6`。
證據落檔：`docs/experiments/2026-09-07-legend-n1n7-evidence/`。

## 0. 結論表

| 驗收 | 判定 | 一句話 |
|---|---|---|
| A0 kill switch 雙向（本卷起預設開） | ✅ | 顯式 `LEGEND_ON=false` 的 trace(1..20) 與 `ff227a7` 逐位元組相等（332125 字元）；預設（true）不相等（386483）且 `S.shrines` 3 龕 |
| A0-b 預設開的可見面 | **部分 ❌** | 三處口徑已改（VERSION 0.44、VERSION_NOTE、規則頁、GUIDE §11.20 第 1 點）；`legend-drive.mjs` 不帶 `--legend` 走預設、走到「請走」24 次，**但走不到「回天」**（預設跑法三尊必被請完），回天靠 `--burn=0` 那一跑（6 龕）。見 §4／§5.2 |
| A1 N1 公平性（n=10000） | ✅ | 四座位比值 1.013／0.986／0.990／1.013（門檻 [0.92,1.08]），χ²(df=3)=1.23（門檻 ≤11.34），並列組 7484 組 |
| A2 N1 決定性 | ✅ | 同一批種子連跑 3 次 trace(1..20) 逐位元組相等；引擎 `<script>` 段內建亂數出現次數 0＝與基準相同 |
| A3 N1 鑑別力 | ✅ | 新版 200 顆種子四家都先擲過（1639/1875/1957/2013 的同型分布），對 `ff227a7` **紅在行為斷言**「座位 0 先擲次數應 ≥1，實際 0（分布 0/0/0/200）」 |
| A4 N7 鑑別力 | ✅ | 兩案皆綠；對 `ff227a7` **紅在 `takenBy`**（預期 0、實際 null） |
| A5 N7 AI 不白燒 | ✅ | seeds 1..50 全數改拜第二順位；對 `ff227a7` 紅（仍回 `{"shrine":0,"amt":3}`） |
| A6 天井鎖死歸零（n=10000） | ✅ | 回天收攤 6331 龕，其中結清時仍握 `h≥12` 的人次＝**0** |
| A7 既有閘門重跑（n=10000） | ✅ | 判定項 L2 ✅、L4 ✅；L1″ 只報不判（狀態① 仍紅且由「兩家紅」變「四家紅」、②③ 綠＝無綠轉紅）、L3′ ✅／L5 ✅ 只報。數字見 §3 |
| A8 測試與 Playwright | **部分 ❌** | 7 套測試全綠、drive 0 console error／pageerror、請走與回天各走到（分兩種跑法）；**「橫向溢出 0」照字面紅**——但把 `index.html` 換成 `ff227a7` 跑同一支治具，溢出清單**逐筆同型同值**，本卷沒讓它變差。見 §4 |
| A9 範圍與版本 | ✅ | `git diff --stat` 只含凍結檔列出的檔（另加本報告與證據目錄，見 §5 說明）；`VERSION="0.44"`、`CFG.LEGEND_ON: true` |

## 1. 改了什麼（檔案:行號）

### `index.html`
| 行 | 改動 | 對應需求 |
|---|---|---|
| 581–587 | `CFG.LEGEND_ON: false → true`（註解改沿革口徑，`?legend=0` 仍可關） | 傳說預設開／A0／A9 |
| 1759 | `VERSION="0.43.1"→"0.44"`、`VERSION_NOTE` 換成本卷三件事 | A0-b／A9 |
| 2102–2110 | `S.shrineStat` 加 `tieGroups`／`tieFirst[]`／`tieExp[]`／`dawnPity`（純計數、零亂數） | A1／A6 的量測落在真實路徑上 |
| 2517–2522 | `aiIncense` 對已達天井的龕 `continue`（改拜下一順位的系） | N7 附帶／A5 |
| 2559–2588 | 移除 `shrineOrderKey`，新增 `shrineRollOrder(sh,rollers)`：`h` 降冪穩定排序＋同 `h` 並列組 Fisher–Yates（`S.rng()`），並列人數 1 不消耗亂數；順手記 A1 的計數 | N1 裁乙／A1／A2 |
| 2624–2637 | `rollers` 改成「本夜對這一龕燒香 `amt>0` 的活人」∪「對這一龕 `h≥INC_PITY` 的活人」，排序改呼叫 `shrineRollOrder` | N7 裁甲／N1 |
| 2669–2671 | `settleShrinesEnd` 回天前累計 `dawnPity` | A6 |
| 3410 | 神龕列 tooltip 加「已達天井・今夜免燒也必請」 | N7 UI |
| 3748 | 規則頁請神段：擲骰資格加天井者、擲序改「先後隨機決定・每一夜重洗」 | N7／N1 UI 口徑 |
| 4017–4023 | 燒香列 `incbarHTML`：天井龕改顯示「已達天井・今夜免燒也必請」 | N7 UI |
| 4325–4327 | 請神結算卡：沒有對應燒香列的天井擲標「今夜免燒也有資格」 | N7 UI |
| 5445 | 測試出口 `shrineOrderKey → shrineRollOrder` | 死碼不留 |

### 測試與治具
- `tests/legend.test.mjs`：第 16 案（風位擲序）依凍結檔 §2.1 改寫成 A3 兩案（同 h 洗牌／不同 h 恆降冪且與 seed 無關）；新增 A4 兩案、A5 一案、A2 一案 ⇒ **17 案 → 22 案**。
- `tests/tools/legend-gate.mjs`：L0 兩支對調口徑並加驗「預設載入後 `S.shrines` 3 龕」；L1″ 的單夜模型改成「資格＝燒香∪天井、順序＝對所有與 `h` 降冪相容的排列取等權平均」（不再呼叫已移除的 `shrineOrderKey`）；`DEF`（預設＝開）之外另建 `OFF`（顯式關）供 L0／L4／L5 對照；新增 **A1**、**A6** 兩節。
- `tests/tools/legend-drive.mjs`：不帶 `--legend` 改成「完全不帶 query、走 CFG 預設」（原本強制 `?legend=1`），`--legend=0` 維持關閉對照，移除 `--legend=none`。
- `docs/GAME_DESIGN.md`：changelog 一行。
- `docs/IMPLEMENTATION_GUIDE.md` §11.20：第 1 點（預設開）、第 4 點（治具清單／22 案／基準 SHA）、第 6 點（擲序改洗牌，含三條紀律與 A1 的量法）、第 9 點（L1″ 狀態① 的新診斷）改寫，新增第 13 點（天井免燒）。

## 2. 每條驗收的證據

### A0／A0-b
```
node tests/tools/legend-gate.mjs 10000 --only=L0     （落檔：evidence/gate-10000.md）
```
```
- 顯式 LEGEND_ON=false vs 基準：長度 332125/332125，逐位元組相等 ✅
- 預設（LEGEND_ON=true） vs 基準：長度 386483/332125，不相等 ✅
- 預設載入後 makeState('solo',1) 的 S.shrines：3 龕 ✅
```
A0-b 的可見面：`index.html:1759` VERSION_NOTE 已不再寫「請神預設關、?legend=1 試玩」；規則頁請神段只在 `CFG.LEGEND_ON` 時出現、內容已改口徑；GUIDE §11.20 第 1 點改成「v0.44 起預設開」。`legend-drive.mjs` 見 A8。

### A1（n=10000，顯式 `LEGEND_ON=true`，預設 AI 桌）
```
node tests/tools/legend-gate.mjs 10000       （A1 節）
```
| 座位 | 實際先擲 | 公平期望（每組 1/m） | 比值 | 門檻 [0.92,1.08] |
|---|---|---|---|---|
| 0 | 1639 | 1618.25 | **1.013** | ✅ |
| 1 | 1875 | 1902.42 | **0.986** | ✅ |
| 2 | 1957 | 1977.08 | **0.990** | ✅ |
| 3 | 2013 | 1986.25 | **1.013** | ✅ |

χ²(df=3)＝**1.23**（門檻 ≤11.34）✅；並列組總數 7484。
對照改前（v0.43 風位序，報告 3000 局）：1.183／0.770／…／χ²=37.53。

### A2
`tests/legend.test.mjs` 第 22 案：同一批種子連跑兩次 `trace(1..20)` 逐位元組相等（長度 386483），
且引擎 `<script>` 段的內建亂數出現次數＝0。`grep -c "Math.random"`：新版 0、基準 `ff227a7` 0（相同）。

### A3／A4／A5 鑑別力（對基準 SHA 實跑）
```
git show ff227a7:index.html > old-l.html
node tests/legend.test.mjs            → 22 過 / 0 失敗
node tests/legend.test.mjs old-l.html → 17 過 / 4 失敗
```
舊版四條紅的都是**行為斷言**（不是 `TypeError`）：
```
- N1 同香火洗牌：座位 0 在 200 顆種子裡先擲的次數應該 ≥1，實際 0（分布 0/0/0/200）
- N7 天井免燒：h 已達天井、壽命 1 燒不動的人，本夜應該直接請走第 0 龕：預期 0，實際 null
- N7 兩龕各自結算：龕 0（天井）應該免費被他請走：預期 0，實際 null
- N7 AI 不白燒：seed 1：已達天井的龕不得再燒，實際 {"shrine":0,"amt":3}
```
A3 的第二半（「h 不同時擲序恆為 h 降冪且與 seed 無關」）在新舊版都綠——它是不變量、不是鑑別力來源，
鑑別力由「每一家至少先擲一次」那條負責。

### A6
```
- 回天收攤的龕 6331 座（n=10000 局）；其中結清時仍握著 h≥12 的人次：0　門檻 ＝0 ✅
```

## 3. A7 既有閘門重跑（並列比對 v6＝`docs/experiments/2026-09-06-legend3-evidence/gate-10000-v6.md`）

（本節數字全部來自 `evidence/gate-10000.md`，指令：`node tests/tools/legend-gate.mjs 10000`）

### L1″（只報不判）
| 狀態 | v6（風位序） | 本卷（洗牌） | 備註 |
|---|---|---|---|
| ①局初 h 全 0 | ❌（燒滿被打敗數 南0／北3／西0／東9） | ❌（南0／北0／西0／東0） | 已知結構性紅，凍結檔明訂只報不判 |
| ②對手領先 | ✅（南155/31、北119/73、西136/53、東192/84） | ✅（南135/43、北135/43、西135/43、東192/84） | 無綠轉紅 |
| ③差一步 | ✅（南192/128、北48/144、西48/144、東48/145） | ✅（南192/128、北48/144、西48/144、東48/144） | 無綠轉紅 |

**狀態① 的變化要點**：v6 的「北、東綠」本身就是風位不公平的副產品（那一夜輪不到它們先擲）；
改成洗牌之後四家收益矩陣**逐格相同**，於是四家一起紅——這正是 N1 生效的證據（座位差異被洗掉）。
單夜快照量不到「洗牌讓長期期望均等」，所以狀態① 照樣紅；凍結檔 A7 已就此註明「裁預設開前另議多夜模型」。

### L2 活性（判定項）
| 項目 | v6 | 本卷 | 門檻 |
|---|---|---|---|
| 至少一尊被請走 | 97.55% | **97.71%** | ≥60% ✅ |
| 殘日／大士爺紙尊／有應公 | — | 85.42%／76.45%／74.82% | 各 ≥25% ✅ |

累計：燒香 115982 次共 159815 壽命・擲 109741 次中 23669（天井 211）・階段獎勵 4738 次（含小法寶 263）・回天 6331 龕。

### L3′（只報）
① 反事實位移：殘日 **+30.8pp**／大士爺紙尊 **+39.7pp**／有應公 **+55.4pp**（門檻 ≥+10pp，三尊皆過）。
② 勝者持有任一尊的局比例：v6 76.10% → 本卷 **76.41%**（門檻 ≤80%）。

### L5 節奏（只報）
中位 **11** 夜（門檻 10～12）、平均 9.94（v6 中位 11／平均 9.92）；顯式關閉對照中位 11／平均 10.07（與 v6 逐值相同）。

### L4 無支配策略（判定項）
| 策略 | v6 OFF→ON | 本卷 OFF→ON | 本卷位移 | 門檻 ≤40% |
|---|---|---|---|---|
| splitter | 20.47→22.79（+2.32pp） | 20.44→**22.96** | +2.52pp | ✅ |
| greedy | 19.35→20.67（+1.32pp） | 19.21→**20.91** | +1.70pp | ✅ |
| hoarder | 9.68→7.89（−1.79pp） | 9.57→**8.14** | −1.43pp | ✅ |
| specialist | 26.62→32.31（+5.69pp） | 27.10→**32.53** | +5.43pp | ✅ |
| incenseMax | 20.47→33.97（+13.50pp） | 20.44→**33.68** | +13.24pp | ✅ |
| incenseNever | 20.47→15.93（−4.54pp） | 20.44→**15.77** | −4.67pp | ✅ |

★**v6 的 OFF 欄不是完全可比的對照**：v6 跑在 v0.43 的請神分支上，本卷跑在 `ff227a7`（v0.43.1，已併入對決演出小卷，
`pwHaunt` 的 swap 存活檢查會外溢到 `pwTrial` 的估值）。OFF 欄 20.47→20.44、19.35→19.21 這種零點幾 pp 的位移
來自那次合併，不是本卷造成的——本卷的 OFF 路徑對 `ff227a7` 已由 A0 證明**逐位元組相等**。★

記錄項（不判）：`incenseMax` 的封籤 22384 筆（n=2000）被夾掉 179 筆（0.80%）、完全蒸發 40 筆（0.18%）；
`incenseNever` 15.77% vs `splitter` 22.96% ＝ −7.19pp（已知性質，使用者裁定不另立門檻）。

## 4. A8 測試與 Playwright

### 測試
```
for f in tests/*.test.mjs; do node $f; done
```
`aistake` 8-0／`conscap` 5-0／`duel-desync` 7 綠／`legend` **22 過 0 失敗**／`nightrules` 16 綠／`review` 28-0／`wish16` 36-0 ⇒ **7 套全綠**（既有 6 套＋請神）。

### Playwright（三次跑，落檔在 evidence/）
| 跑法 | console error／pageerror | 請走 | 天亮回天 | 熱座交棒 `.incbar` |
|---|---|---|---|---|
| `node tests/tools/legend-drive.mjs …/legend-drive.json`（**不帶 `--legend`＝走預設**） | 0／0 ✅ | 24 次 ✅ | 0 龕 | 0 ✅ |
| `… legend-drive-dawn.json --port=8861 --seeds=1,4,6,8,9,11 --burn=0` | 0／0 ✅ | 12 次 ✅ | **6 龕**（結出 1 筆階段獎勵）✅ | — |
| `… legend-drive-OFF.json --port=8863 --seeds=1 --legend=0`（對照） | 0／0 ✅ | 0（正確：`CFG.LEGEND_ON=false`、`#shrines`／`.incbar` 都不存在） | 0 | — |

A0-b 的「不帶 `--legend` 也要走到請走與回天」：**請走走到了（24 次），回天沒走到**——原因是預設跑法讓真人每夜燒滿，
三尊在 12 夜內全被請走，根本沒有「還開著的龕」可以收攤（八顆種子全部「請走 3 尊・回天 0 龕」）。
回天那條路由 `--burn=0` 那一跑覆蓋（6 龕）。**這一點照實記為「同一支治具、兩種跑法才湊齊兩條路」，不是一次跑完。**

### 橫向溢出（照字面判 ❌，但**與基準逐值相同**）
本卷特地把 `index.html` 暫時換成 `ff227a7` 再跑一次同一支治具（`--legend=1 --seeds=1`，evidence/`legend-drive-BASE-ff227a7.json`），
拿到的溢出清單與新版**逐筆同型、同值**：

| 頁面／元素 | 基準 `ff227a7`（`?legend=1`） | 本卷 v0.44（預設開） |
|---|---|---|
| 「不盯任何一件」`body`／`#table`（開局那一次） | over **19** px | over **19** px |
| 「不盯任何一件」`body`／`#table`（其餘） | over **7** px | over **7** px |
| 「蓋牌開標」`#stage`／`#market`（橫式） | over **3** px | over **3** px |
| **固定頁對照**（同一局同一頁「蓋牌開標」）橫式／直式 | `[]`／`#stage 6px`＋`#market 6px` | `[]`／`#stage 6px`＋`#market 6px` |

結論：**「橫向溢出 0」這條照字面是紅的，而且它在基準 SHA 上同樣是紅的、數值一模一樣**——來源是既有的
`#south` 底列與 `#market` 四欄格線（上一卷報告 §7.2 已診斷過），本卷沒有讓它變差。**這一條沒有為了過而調整，照實記為 ❌ 的字面判定＋不變的實測對照。**

## 5. 假設、沒做到的、範圍例外

1. **【假設，凍結檔 §範圍明訂要點名】天井者本夜封籤燒在別的龕時，兩龕各自結算**——他在天井龕免費必請、在另一龕照常擲；
   「每夜一尊」的字面沒有被破壞（封籤仍然只能選一尊燒）。實作照這個假設做，`tests/legend.test.mjs` 的
   「N7 兩龕各自結算」那一案在守它（`out.rolls` 兩筆、龕 0 免費、龕 1 照常扣壽命）。**請使用者確認這個解讀。**
2. **A0-b「不帶 `--legend` 也要走到請走與回天」只走到請走**——回天要靠 `--burn=0` 那一跑（見 §4）。
   這不是實作缺陷，是「預設跑法太會請神」；照實記錄，未修改治具去湊。
3. **A8 的「橫向溢出 0」照字面是 ❌**——與基準逐值相同，見 §4。未動門檻、未加白名單。
4. **A7 的 L1″ 狀態① 仍紅，而且從「兩家紅」變成「四家紅」**——凍結檔已明訂 L1″ 只報不判、狀態① 是單夜快照的結構性紅；
   ②③ 沒有綠轉紅。但「四家一起紅」這件事本身值得使用者知道（見 §3 說明：那是 N1 生效的副產品，不是變差）。
5. **A9 的檔案清單沒有列到本報告與 `evidence/` 目錄**——派工要求把報告與證據落檔並 commit，所以 `git status` 會多出
   `docs/experiments/2026-09-07-legend-n1n7-report.md` 與 `docs/experiments/2026-09-07-legend-n1n7-evidence/`。
   兩者都是純文件、對引擎零影響。**照實記為「超出 A9 字面清單的兩個新增項」，不自行改凍結檔。**
6. **`tests/tools/legend-gate.mjs` 的改動超出 A9 括號裡的「若需加 A1/A6 計數」**：除了 A1／A6 兩節，還必須
   ① 把 L0 兩支對調口徑（預設已改成開）② 重寫 L1″ 的單夜排序模型（原本呼叫已被移除的 `shrineOrderKey`）
   ③ 另建一個顯式關閉的實例當 L4／L5 的 OFF 欄。三項都是「規則改了、治具必須跟著改」，**門檻一個都沒動**。

## 6. 對凍結檔的意見（不自行修改，留給使用者裁）

- **A0-b 的「`legend-drive.mjs` 不帶 `--legend` 也要走到『請走』與『回天』」在預設開之後不可能一次跑到**：
  預設跑法真人每夜燒滿，三尊必然在局內被請完，回天那條路只能靠 `--burn=0`。
  建議改寫成「不帶 `--legend` 走到『請走』；`--burn=0` 走到『回天』」，但**這是使用者才能決定的凍結檔修訂，本卷沒有動它**。
- **A8 的「橫向溢出 0」是一條在基準上就紅的條件**（上一卷報告 §7.2 已診斷，來源是既有 `#south`／`#market`）。
  它現在扮演的是「不得變差」的角色，字面上卻寫成「＝0」。同樣**沒有動它**，只把成對比較的數字擺出來。

