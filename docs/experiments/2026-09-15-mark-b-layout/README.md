# B 中型盯牌與同槽排版

使用者 2026-09-15 接受建議 B；修正陰影縮放污染後，牌面恢復 W=.150、H=.190、lift=.175。舊 W=.230/H=.280/lift=.250 是在縮放故障下補償放大，正常比例後遮住拍品，因此依使用者選擇更新相應尺寸斷言。

## 實作

- 保持四枚 InstancedMesh 與原 128 枚銅錢預算。
- 同槽最多兩列、每列兩張；間距 .38/.62。外側朝桌心排，前列因透視放大再往內收。
- 每列跨槽一起排距，避免相鄰槽的牌在往內收時互疊；重新 layout／揭盅也走同一支排版。
- 產品沒有 iPhone 型號判斷；牌位是一般橫向桌面的保守空間配置。

## 驗證

`node --test tests/mark-token-scale.test.mjs`：兩項通過。新行為測試先重現四席投影重疊，再修正；追加實拍發現的前列碰側欄案例，也先紅後綠。

- 枚舉 4^4=256 個座位／槽位分配，逐枚投影無交疊，resize及揭盅後仍成立。
- 固定 852×393 的附加壓力案例，保守限制在 x227..625、y<310 的資訊窗；此為包含合成安全區的 fixture，非宣稱 iPhone 真機實测值。
- 縮放回歸涵蓋四件信物、128 枚錢、脈衝、換夜與直向比例。
- `node tests/tools/mark-layout-capture.mjs --safe`：正常 seed1 盯牌＋純3D合成四席同槽四個場景，errors=[]，已逐張讀圖。最新完成圖在 `synthetic-safe/`；前列保持在底部按鈕上方，牌面無疊字。未將3D合成擺牌當成自然賽局。
- `tests.txt`：全測37/37通過；`trace.json`：seed1..20與基準48f644c完全相等。

根目錄圖為無安全區的開發取樣；最終通用安全區與FPS開關驗證詳 `../2026-09-15-iphone-standalone/README.md`。
