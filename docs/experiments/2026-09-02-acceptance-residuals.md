# 驗收凍結：殘留處置批次（2026-09-02，使用者「都照建議」）

基準 commit：`9707cac`（origin/main 同）。順序 ③→①實驗→④→②→⑦；①實驗出數字後停下讓使用者選。

## ③ 保守標上限伺服端夾（ARCH_SPEC 待辦 20）
B1 `resolveAuction` 收標時，`type==="cons"` 且 `amt > consCapFor(p)`（以結算當下的壽命算）的標一律改以 `yaming` 結算（落標付一半而非 25%）；UI 的按鍵夾只是提示。
B2 行為斷言測試：玩家設保守標 X（在上限內）→ 放血使上限降到 X 以下 → 落標 → 實付＝押命標算式；對 `9707cac` 舊版恰紅在實付數字。另一條反面：未超上限的保守標實付不變。
B3 等價：CFG 全預設下 trace(1..20) 與 `9707cac` 逐位元組相等（AI／策略本來就不出超上限保守標；若不相等，回報差在哪一 seed 哪一夜，不得改測試）。

## ④ 規則說明併進預告框（省一列）
C1 規則夜的說明文字併入每夜都在的「明夜預告／hint」虛線框，不再另開 `.rulebar` 一列；押寶夜的一注條維持與放血條並排。
C2 844×390：同種子四組（一般夜／落魄／收祟／押寶）且玩家持獻祭刀時，`#felt` 溢出量全部 ≤ 一般夜（貼四個數字；基準：修前 0／34／25／33）。
C3 純 UI：trace 逐位元組相等；Playwright 三個規則夜截圖＋console 0 error。

## ② 押寶夜 AI（ARCH_SPEC 待辦 16）
D1 新 hook `onAiStake`（或等價的引擎點）：AI／策略在押寶夜的 X＝原本各筆金額總和，受該型態上限夾（cons 上限＝consCapFor；yaming 上限＝壽命−1），勾選集不變；型態取原本金額最大那筆。真人不受影響。
D2 只在 `singleStake` flag 為真時生效；`RULE_ON=false` 時 trace 與 `9707cac` 逐位元組相等。
D3 押寶夜單獨開（其他規則關）n=2000，座位 0 三策略勝率位移（ON−OFF）的絕對值相對修前縮小（修前 r1 量測：splitter +4.25／greedy −0.65／hoarder −0.30；以同版本重量修前值），且三策略全開位移仍 ≤1.5pp。不得調 CFG。
D4 行為斷言測試對修前版恰紅（AI 在押寶夜的 X 等於加總而非最大值）。

## ① 收祟夜棄權優勢實驗（ARCH_SPEC 待辦 15）——只出數字不改主線
E1 在隔離 worktree 實作兩個變體（各一個 CFG 開關，預設關）：
   (A) 流標詛咒品輪流塞給**所有**未出手者（壽命高者先，繞圈直到塞完；全員出手才落回壽命最高者）；
   (B) 每位未出手者夜末付一筆買路錢（BID_FEE）當看戲錢，塞袋規則維持 4′。
E2 對 現行 4′／A／B 各跑：`nr/a5-fixture.mjs` 三治具（raw／abstain／poison）n=2000，與 `nr/balance.mjs` n=2000。
E3 回報表：每個變體的「棄權 vs 毒標 vs 原樣」勝率、三策略位移、收祟夜當夜死亡數。判準（給使用者看，不自裁）：棄權 ≤ 出手、位移 ≤1.5pp、當夜死亡不暴增。

## ⑦ 文件債（隔離 worktree，只動 docs/）
F1 待辦 11／12 以 v0.6 之後數據正式結案（獻祭刀甲′條件勝率 50~58%、splitter 基準 14.15%），標「已結案」附依據。
F2 待辦 13：把「同一支現行 trace() 跑改前／改後兩份 index.html 比對（雙向）」寫成 GUIDE §7 正式規程，待辦 13 標結案。
F3 待辦 18：GUIDE §4 契約表補第 13 個 hook `onNightEndGlobal`（時機、p=null、ctx={log}），並把「12 個」字樣改對。
F4 待辦 17：確認 GAME_DESIGN §六之三 E 實作口徑欄已載明風位同分序；沒有就補。
F5 fresh read-back：另一個 agent 只讀改後的 GUIDE §4／§7，複述「等價驗證要怎麼做、第 13 個 hook 何時跑」，複述正確才算過。

## 修訂（2026-09-02，主對話依 02 §2.1 例外自行修正、事後回報）
**B3 原文錯在哪**：前提「AI／策略本來就不出超上限保守標」不成立——AI 獻祭刀在 `aiBids` 放血（甲′全體對手失血）會讓**別人**已定案的保守標在結算時超上限，這正是 ③ 要修的 bug；任何正確實作都必然改變 trace，B3 對任何實作都不可能通過。
**為什麼現在才知道**：實作 agent 在真實路徑上追到 seed 8 夜 4（pre life 13→開標時 9）。
**B3′（鑑別式）**：對 seed 1..20，逐局記錄「結算時被夾的保守標筆數」clamped(seed)。要求：trace 有差異的 seed ⇔ clamped>0；trace 相同的 seed ⇔ clamped=0（雙向、逐 seed 列表）。另補 n=2000 三策略勝率位移（ON=有夾／OFF=舊版 9707cac）只回報不設門檻。
**C2 補充做法（不改條件）**：允許把「放血條」搬進底部列（`#south` 的袋子／ⓘ 旁，緊湊按鈕含次數與成本），使該列整列消失；規則說明維持併在預告框。C2 數字門檻不變（四組皆 ≤ 一般夜）。

## 修訂（2026-09-02，使用者針對 D1 明確裁定「甲」；02 §2.1 程序）
**原標準錯在哪**：D1「X＝原本各筆金額總和」把一般夜本來會被 MAX_BIDS 裁掉的第 3、4 筆也算進預算，高估 AI 的「整夜預算」，語意不對。
**為什麼現在才知道**：n=10000 補量（report-34-r2.md §7）顯示現行公式三規則全開 splitter −1.75±0.46 超標，變體 −0.95 過；追語意才發現高估。
**D1′**：X＝原本**會進開標的前 MAX_BIDS 筆**（依金額排序，MAX_BIDS=0 時全取）金額總和，受型態上限夾；其餘不變。D3 門檻 ≤1.5pp 不動、n=10000。D4 測試斷言同步改為 D1′ 語意（對修前版仍須恰紅）。
