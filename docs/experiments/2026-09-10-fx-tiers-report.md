# 實跑報告：招式三級視覺分級（v0.54，**收尾版**，2026-09-10～11）

> 驗收凍結＝`docs/experiments/2026-09-10-acceptance-fx-tiers.md`（F0–F10，**正文門檻一字未動**；§2.1 修訂一～**七**）。
> 基準＝`adbb124`。一版 `a862bbd`(r1)、二版 `5a1220e`(r2)、三版 `6d10712`(r3)、四版 `cfb5701`(r4)、五版 `00706e0`(r5)。
> ★**收尾版＝使用者 2026-09-11 裁甲「本卷拿掉黑邊，保留 CINEMA 仰視機位與 tier 3 1400ms；
> 黑邊連同對決版面安全區留給招式可辨性卷重做」**（凍結檔 §2.1 修訂七）。五輪覆審 r1–r5 的 findings
> 與三態全部保留在下面，作為「為什麼黑邊做不進這一卷」的證據鏈。★
> ★量測用的四棵樹：`scratchpad/basefull`＝`git archive adbb124`（v0.53 基準）、
> `scratchpad/v5full`＝`git archive 00706e0`（五版，鑑別力的「壞掉版本」）、
> `scratchpad/mutfull`＝收尾版 ＋ r5 那一行「`#duelBeat` 上移 9px」的突變（第二個壞掉版本）、
> `scratchpad/headfull`＝`git archive HEAD` ＋工作區改動同步（`index.html` 與工作區逐位元組相同）。★

## 結論（給只看四行的人）
1. **黑邊整支拿掉了**（使用者 2026-09-11 裁甲）：`#lbTop`／`#lbBot`、`#duel.lbox` 的 `transform:scale(.80)`
   與 `box-shadow`、`pwLetterbox()` 與它的四個呼叫點、測試出口、`duel-drive` 的黑條 observer——
   **DOM、CSS、JS、治具裡各 0 處**。tier 3 剩下的畫面語言是 **CINEMA 低角度仰視機位 ＋ 1400ms**，兩者未動。
2. **「版面沒被動到」改成相等性斷言**：tier 3 那一拍 `#duel` 底下**所有可見子孫**的 rect
   與 v0.53 基準樹逐值相同（≥3 錨點 ×3 時點 ×2 方向，錨點分屬 3 場不同的對決）。分母不再是我列的清單，是 DOM 自己的——
   這解掉了 r2–r5 一路「列了哪些元素就只守得住哪些元素」的死結。
3. **鑑別力正反都驗**：同一組治具對**五版 `00706e0`** 紅、對 **r5 那個逃過 L1–L8 的突變**（`#duelBeat` 上移 9px）紅、
   對基準樹自己跑兩次綠、對收尾版綠（數字見 §F5）。
4. **新增一條沒過字面門檻的**（據實記紅，不調門檻）：**F6 的「draw calls 不增」**中位 +3（960 vs 957）。
   歸因做到決定性——同一棵樹只把 `?fxtier=0` 打開就回到 953–958 ⇒ 不是新的繪製來源，是節奏造成的取樣差。
   連同前三條（F3 主條 5018ms、F3 子條原文的 8 秒、F4 的 `nullCount`）一起交製作人裁。

## 收尾版做的四件事（範圍嚴格限定：使用者裁甲的四項，其他一律不碰）

### ① 移除 letterbox 全部實作，退回 v0.53 寫法
| 位置 | 移除的東西 |
|---|---|
| `index.html` CSS | `#lbTop,#lbBot{...}`／`#lbTop.on,#lbBot.on{height:8vh}`／`#duel.lbox{transform:scale(.80);box-shadow:0 0 0 100vmax #000}`（含 38 行註解） |
| `index.html` DOM | `<div id="lbTop"></div><div id="lbBot"></div>` |
| `index.html` JS | `pwLetterbox()` 整支；四個呼叫點（`doSkip`／`pwPlayBeat` 開與關／`playDuel` 收場）；測試出口 `pwLetterbox` |
| `js/camera-director.js` | 只改註解（`CINEMA` shot、`endCinema()` 四入口取消路徑、1400ms **一行未動**） |
| `tests/tools/duel-drive.mjs` | 黑條 MutationObserver 與逐場 `lboxMs`（黑條沒了它恆 0 ＝靜默恆綠，所以刪掉而不是留著） |
| `tests/tools/pace-ab.mjs` | 輸出裡的 `lbox` 欄 |
| `tests/tools/t3-shot.mjs` | `withBox`／`-nobox` 兩趟對照（順帶解掉 r5 M7，見 §④） |
| `tests/tools/lbox-probe.mjs` | L3／L4／L6／L7（都是黑條專屬）；保留 L1／L2／L5／L8，新增 L9／L10 |

`?closeup=0` 從此**只管 CINEMA 一件事**（`ys:fx-trait` 仍帶 `cinema: pwCloseup()`，`camera-director` 只在它為 true 時切機位）。

### ② 凍結檔 §2.1 修訂七（範圍變更，使用者裁定）
逐句點名蓋掉本文哪幾句、五個版本各壓到誰的對照表、「原標準錯在哪／為什麼現在才知道／改前改後／誰同意」四段
都寫在凍結檔。**F5 機械段換成三條**（tier 3 版面與 v0.53 逐值相同／CINEMA 只在 tier 3／黑條 DOM 不存在），
並要求「對五版必須紅、對收尾版必須綠」。

### ③ 報告全面對齊（r5 M5／M6／M7／M8／L1 那一批）
修訂四只留 5018 一組現值＋歷程（重複列真的刪了、「展幅 53.5–290」那句移除、結論句改成涵蓋 −586）、
修訂五整段改寫成加嚴後的綠、r2 表的 N1／N5 兩列重寫、F5 人眼段那兩句不實宣稱改掉、
「72 個可見元素」改成實測範圍、`shots-t3-real` 重拍且每張機械記錄「是不是對決畫面」。

### ④ 重跑
F0／F2／F5 新機械段／F6／F7 與既有 9 套全部重跑；F3 的數字沿用（理由見 §F3）。


## 逐條三態表（收尾版）

| 條 | 狀態 | 關鍵數字 | 證據 |
|---|---|---|---|
| F0 等價雙向 | **綠** | `trace` equal（357285）＋`--beats` 拍序列 equal（540776）；既有 9 套＋新 14 條全綠 | `trace-eq`／`fxtier.test.mjs` |
| F1 分母歸一 | **綠** | runtime 0 處重複來源；對完整基準樹 **14 紅**（全在行為斷言） | `tests/fxtier.test.mjs` |
| F2 短版原生合身 | **綠** | 收尾版重跑：`--tier=1` **27/27**（ms 全 260、rate 全 1.0、fill 0.854–0.896、acts 5–30）、`--tier=2` **30/30**、`--tier=3` **3/3**（ms 全 1400、rate 全 1.0、horizon 1290/1296/1294、fill 0.921/0.926/0.924）；重複簽章 0 | `tfx-t1/2/3.json` |
| F3 主條 | **綠（修訂四：以 5018ms 通過）** | 5018ms（[4996, 5155.5]、展幅 159.5）；基準 5604；effect −586、不重疊。★本卷之後 F3 主條不再是擋得住東西的閘門★（要換成決定性的量才變得回來） | `pace-ab.json` |
| F3 子條 | **綠（修訂五＋R4 加嚴）** | 合計 −13.7% ≤ −10% **且**逐場 −21.3%／−10.4%／−3.4% 全部比基準快 | `duel-t3.json`／`duel-base-16.json` |
| F4 可讀性不退 | **綠（修訂六）** | `deepOk` 10/10、`monoQuiet` 6/6；`nullCount` 4（記錄項）。★本卷此半無閘門★，效力由治具小卷回復 | `cu-on1.json`／`dmg-*` |
| F5 機械段（修訂七改寫） | **綠** | **L1／L2／L5／L8／L9／L10 全過**；L10＝tier 3 那一拍 `#duel` 全部可見子孫 rect 與 v0.53 基準樹逐值相同（橫式與直式各 3 錨點 ×3 時點）；鑑別力：同一組治具對五版 `00706e0` **紅**、對基準樹自己跑兩次 **綠** | `lbox.json`／`rects-*.json` |
| F5 人眼段 | **交使用者** | 兩張 sheet（27 支短版／30 套完整版）；tier 3 真實路徑 **18 張全部是對決畫面**（`duelOk` 機械判定，不再靠人眼宣稱），畫面內容＝CINEMA 仰視機位＋1400ms、**沒有黑邊** | `sheet-*.png`／`shots-t3-real/` |
| F6 fps | **主數字綠／draw calls 那半條紅** | 收尾版重跑交錯 5 輪：`rendersPerSec` 中位 283.9 vs 282.0 ＝ **1.007**（≥0.90 ✅）；**draw calls 中位 960 > 基準 957（+3）＝依門檻字面紅**，歸因＝節奏造成的取樣差（同一棵樹 `?fxtier=0` 回到 953–958） | `scratchpad/fps{A,B}1-5.json` |
| F7 Playwright 零錯 | **綠** | 收尾版重跑：`duel-rects` 10 次（5 棵樹 ×2 方向、每次 18 場）／`lbox-probe`／`t3-shot`（8 場 18 張）／`duel-perf` 13 次／`duel-new`／`duel-fxtier0`／`traitfx-drive` 三個 tier **全 0**。★二版那次 `duel-fxtier0` 的 2 個 error 逐字是 `fonts.googleapis.com … ERR_CONNECTION_TIMED_OUT`＝外部網路逾時、非受測物★ | 各 json |
| F8／F9／F10 | **綠** | GUIDE §11.27 已對齊收尾版；**28 檔**全在白名單（新增 `duel-rects.mjs`，已補進計畫檔）；acts 5–30 | — |

## r5 覆審 findings 逐條三態

> r5 判「黑邊做不進這一卷」。使用者據此裁甲把黑邊移出本卷，所以**凡是「黑條位置／黑條蓋到誰」的 finding，
> 收尾版的處置一律是「隨黑條一起移出本卷」**——不是宣稱修好了，也不是當作沒發生：它們是修訂七的證據。

| # | 標題 | 三態 | 證據／說明 |
|---|---|---|---|
| **R5-H1** | L7 的區域分母沒歸一（量 8vh 黑條，玩家看到的黑邊在縮放後的 `#duel` rect 上）；削字幕 19.2% 的突變 L7 全綠 | **連同黑條移出本卷** | L7 整條刪除。新的 F5 主條不再問「黑條蓋到誰」，改問「版面有沒有被動到」——tier 3 那一拍 `#duel` 全部可見子孫的 rect 與 v0.53 逐值相同。r5 那個突變（`#duelBeat` 上移 9px）在新判準下必紅：`y` 差 9 就是差 9 |
| **R5-H2** | `#beatLamps` 在真實 tier 3 拍有 58.5% 看不到 | **真的修好（回到基準狀態）** | 那 58.5% 是 `#duel` 縮放後被它自己的 `overflow:hidden` 吃掉的；縮放拿掉之後拍燈的 rect 與 v0.53 逐值相同（見 §F5 的 L10）。★r5 指出「基準即有」的那一半（內容溢出 4px）**本卷不處理也沒動到**——它是下一卷「對決版面安全區」的題目★ |
| **R5-M1** | `index.html:55-69` 的註解描述的是沒實作的修法 | **連同黑條移出本卷** | 那 38 行註解隨 CSS 一起刪除，一個字都沒留 |
| **R5-M2** | 「3D 妖與 DOM 版面一起等比縮放」不成立（`js/duel-figures.js:648-649`） | **連同黑條移出本卷** | 不實宣稱的出處（CSS 註解＋報告 §②）已隨黑條刪除。`js/duel-figures.js` **本卷一行未動**，妖不縮的事實留給下一卷 |
| **R5-M3** | 縮 0.8 之後 `.mvdesc` 有效字級只剩 8.8px | **真的修好（回到基準狀態）** | 沒有縮放 ⇒ 字級回到 11px（rect 逐值相同即為證） |
| **R5-M4** | letterbox 進出變成硬切 | **連同黑條移出本卷** | 沒有 letterbox 就沒有進出 |
| **R5-M5** | 修訂四「統一 5018」只做了一半（四處舊數字） | **真的修好** | ①凍結檔重複列**這次真的刪了**（`grep -c "二版（覆審員 r2 獨立量）"` ＝1）②報告「展幅 53.5–290」該句移除 ③凍結檔結論句改成「四次獨立量測 −512.5／−524.5／−526／−586，同向且全距不重疊」④報告修訂四詳表最後一列改成 repo 現況 5018 |
| **R5-M6** | 報告「修訂五」整段仍是二版敘事 | **真的修好** | §修訂五 整段重寫成加嚴後的綠（合計 −13.7% ＋ 逐場 Δ 全負），與凍結檔和逐條三態表一致 |
| **R5-M7** | `shots-t3-real` 18 張裡 6 張不是對決畫面、`-nobox` 沒有對照效力 | **真的修好** | 真因是一場塞兩趟（≈4.7 秒）比一場對決還長。黑條拿掉後 `-nobox` 對照本來就沒意義，改成**6 場 ×3 幀＝18 張**（一場一趟 ≈2.35 秒）；每張截圖當下機械記錄 `#duel` 的 display／`.on`／場上妖數／`cinemaOn`，任何一張不是對決畫面就整支紅（`duelOk`） |
| **R5-M8** | R4-M5 只修了 N1，N5 那一列沒修 | **真的修好** | r2 表的 N5 那一列整列重寫（見下方 r2 表） |
| **R5-L1** | 「72 個可見元素」是突變檔的數字 | **真的修好** | 改成實測範圍（見 §F5：每個樣本 51–74 個） |
| **R5-L2** | 修訂五的新條件沒有治具在機械判定 | **沒修到（記錄）** | 仍是人工比對兩個 JSON。範圍外；與加嚴前同級，非退步 |
| **R5-L3** | 390×844 的 L7 判定恆真（`rotateHint:flex` 直接回 true） | **真的修好** | 新的 L10 在直式**兩棵樹同樣注入** `#rotateHint{display:none!important}`，真的玩到 tier 3 拍再比 rect ⇒ 直式不再是恆真的那一半（實測直式也有 3 錨點 ×3 時點） |
| **R5-L4** | R4-M3／R4-L6 作者明記「範圍外、沒修到」 | **仍是沒修到（記錄）** | R4-M3（L8 循環論證）在黑條拿掉後**自然消失**：L8 現在只斷言 CINEMA。R4-L6（例外殘留黑條）同樣隨黑條消失 |
| **R5-L5** | N1 那一列的證據欄仍是三版敘述 | **真的修好** | 該列已重寫（見 r2 表） |

## r4 覆審 findings 逐條三態

| # | 標題 | 三態 | 證據 |
|---|---|---|---|
| **R4-H1** | `#duelMove` 被下黑條蓋 15–30%（四版修法推進去的） | **五版真的修好（r5 複驗確認）→ 收尾版連同黑條移出本卷** | 五版整塊縮放讓 `#duelMove` 對黑條與黑邊的交集都是 0（r5 親跑複現）；收尾版沒有黑條，`#duelMove` 的 rect 與 v0.53 逐值相同 |
| **R4-H2** | `#beatLamps` 被上黑條蓋 75.4%／與 `#duelBeat` 重疊 | **五版只修好四版引入的那一半（r5 H-2）→ 收尾版連同黑條移出本卷** | 四版引入的 75.4% 確實消失，但五版的拍燈**仍有 58.5% 被縮放後的 `#duel` 自己的 `overflow:hidden` 吃掉**，而五版報告寫成「拍燈全部完整」＝不實宣稱。收尾版沒有縮放，拍燈 rect 與 v0.53 逐值相同 |
| **R4-H3** | L7 元素集合錯、對 H1/H2 零鑑別力 | **元素分母歸一（五版）→ 區域分母仍錯（r5 H1）→ 連同黑條移出本卷** | 五版把元素集合改成「掃所有可見子孫」是對的方向，收尾版的 L10 沿用同一個掃法（而且連白名單都不留）；錯的是「拿什麼跟它比」——比黑條會漏掉真正的黑邊，比 v0.53 的 rect 不會。實測每個樣本 **51–74 個**可見元素（五版報告寫的「72 個」是突變檔的數字，r5 L1） |
| **R4-M1** | 修訂四數字與 repo 不同步 | **真的修好** | 標題／結論句統一 5018（repo 現況），歷史值列為歷程 |
| **R4-M2** | 修訂五改寫超出理由（合計留 1409.9ms 餘裕） | **真的修好（加嚴）** | 補「逐場不得比基準慢」第二條件；三場 Δ 全負 ⇒ 仍綠 |
| **R4-M3** | L8 的黑條斷言是循環論證 | **連同黑條移出本卷（自然消失）** | 收尾版的 L8 只斷言 CINEMA（`maxK===0`、`onCount===0`），黑條那半條已刪。r4 覆審員在真實對局上量到的旁證（同 seed 16 場、兩邊 `tiers` 都是 `{1:25,2:14,3:3}`、黑條開啟次數 3 → 0）留在這裡當歷程 |
| **R4-M4** | 報告 F5 人眼段「無重疊」不成立 | **五版宣稱修好→r5 判仍不成立→收尾版真的修好** | 五版又寫了一次「18 張…無任何元素被切」，而 r5 逐張看出 6 張根本不是對決畫面。收尾版把「是不是對決畫面」變成機械欄位（`duelOk`，每張記 `#duel` display／`.on`／場上妖數），不再靠人眼宣稱 |
| **R4-M5** | 報告 r2 三態表 N1／N5 兩列過時 | **五版只修了 N1（r5 M8）→收尾版兩列都修好** | 兩列都已重寫（見下方 r2 表）：N1 改成「連同黑條移出本卷」的歷程，N5 刪掉「黑條不跟著關」那段循環論證 |
| **R4-L1** | L7 的「反面」恆真 | **連同 L7 一起移出本卷** | 取代它的 L10 同樣守住這一點：**0 樣本、0 元素、錨點 <3 一律當不過**，而且反面是真的跑出來的（基準自己比自己＝綠、五版＝紅） |
| **R4-L2** | `pwLetterbox` 註解仍講三版被否的「內縮」 | **真的修好** | 註解已改寫成「整塊等比縮進」，並標明兩條死路別再試 |
| **R4-L3** | 凍結檔修訂四表重複列、標題與內文打架 | **真的修好** | 重複列已刪、標題與內文統一 5018 |
| **R4-L4** | 報告「展幅 53.5–290」被四版 724 打破 | **真的修好** | 該句已移除，改用逐版表 |
| **R4-L5** | `#duelResult/#duelSub` 的 lbox 規則是死碼 | **真的修好** | 那三條 absolute 規則五版已刪；收尾版連 `.lbox` 這個 class 都不存在 |
| **R4-L6** | `pwPlayBeat` 在開／關黑條之間拋例外會殘留 | **連同黑條移出本卷（自然消失）** | 沒有黑條就沒有殘留；`pwLetterbox` 整支不存在 |

## r3 覆審 findings 逐條三態

| # | 標題 | 三態 | 證據 |
|---|---|---|---|
| **H-1** | N1 沒修好（修法在因果路徑之外） | **四版宣稱修好→r4 判把別人推進黑條→五版改整塊縮放→r5 判仍切掉拍燈→連同黑條移出本卷** | 四版的「字幕改 absolute」數字（`scrollHeight` 416→390、三元素交集 0）留作歷程；它沒有解掉真因（版面沒有安全區）。最終處置見 §r5 表與凍結檔修訂七 |
| **H-2** | L7 對真正的失效模式零鑑別力 | **四版部分修→五版元素分母歸一→r5 判區域分母仍錯→收尾版換判準才真的修好** | 最終解法不是再改 L7，而是把問題換掉：不問「黑條蓋到誰」，問「版面有沒有被動到」（L10 rect 相等性）。r5 那個逃過 L1–L8 的突變在新判準下紅 |
| **H-3** | 報告把凍結的 `rate ≤1.0` 寫成 1.06 判綠 | **真的修好** | 選 r3 給的路 (b)：把三尊時間軸壓到 `rate` 全 1.0（不寫修訂）；`traitfx-drive` 的 `rateOK` 不再限縮到 tier 1（tier 1＋tier 3 都驗，tier 2 只記錄）；報告改成事實 |
| **M-1** | 修訂四數字與 repo 證據不同步 | **三版半修→r4／r5 兩度抓到殘留→收尾版真的修好** | 凍結檔與報告現在只有一組現值 **5018ms**（repo 的 `pace-ab.json`），歷史值 5302／5032.5／5009 一律標為歷程；重複列已刪、「展幅 53.5–290」該句已移除、結論句改成涵蓋 −586（r5 M-5 的四小項逐項對完） |
| **M-2** | F4 連續兩次放寬、已無及格線 | **真的修好（記錄）** | 報告 F4 那一格已白紙黑字寫「**本卷此半無閘門**，效力由治具小卷回復」 |
| **M-3** | 修訂五的新判準仍量不到 tier 3 | **真的修好（記錄）** | 凍結檔修訂五末段明寫「合計判準同樣量不到 tier 3……下一卷要換」 |
| **M-4** | `?closeup=0` 下黑條照舊的理由是循環論證 | **四版真的修好 → 收尾版連同黑條移出本卷** | 四版改成「黑條一起關」並加 `barH:[0,0]` 斷言；收尾版黑條不存在，L8 只剩 CINEMA 那半條，循環論證的前提消失 |
| **L-1** | `pace-ab` 不記樹的指紋 | **沒修到（記錄）** | 本輪的兩棵樹已人工核（`headfull/index.html` 與工作區 `index.html` 逐位元組相同）；治具加指紋留給下一卷 |
| **L-2** | `duel-drive` 系列證據沒在三版重跑 | **真的修好** | 四版重跑 `duel-new`／`duel-fxtier0`（`ver` 已是 v0.54、errors 0）；`duel-t3`／`duel-base-16` 沿用（三版四版的改動不影響 duelsMs，理由見下） |
| **L-3** | 報告 fill／acts 範圍寫窄了 | **真的修好** | 改成實測範圍（tier 1 的 `fill` 0.85–0.89、`acts` 5–30） |
| **L-4** | `dmg-base-*` 只證明「某個 v0.53 樹」 | **沒修到（記錄）** | `adbb124` ＝ v0.53 ＋ 一份 md，實務上等價；記錄即可 |

**關於 `duel-t3.json`／`duel-base-16.json` 沿用**：四版改的是 CSS 定位、三尊餘韻的 delay／ms、以及 `pwLetterbox` 的開啟條件；
五版改的是 `#duel.lbox` 一條 CSS；**收尾版是把 letterbox 整組刪掉**——三者**都不改變任何 `pwSleep` 的毫秒數**
（tier 3 仍是 1400ms、拍末仍是 300/900/1400，兩張常數表逐值未動），所以 14 場的 `duelsMs` 與 tier 分布不受影響。
`?closeup=0` 那條只影響 `?closeup=0` 的頁面，而 `duel-t3` 沒帶那個參數。

## r2 覆審 findings 逐條三態

| # | 標題 | 三態 | 證據／說明 |
|---|---|---|---|
| **N1** | 黑條 z:41 蓋掉 tier 3 那一拍的字幕（`#duelBeat` 82.4%／`#duelSub` 100%） | **連同黑條移出本卷**（三版 padding no-op → 四版 absolute 把別人推進黑條 → 五版整塊縮放仍切掉拍燈 58.5%；使用者 2026-09-11 裁甲停損） | 這一列是修訂七的主證據：四種修法各壓到不同的元素，真因是**對決版面本來就填滿到溢出（`scrollHeight 394 > clientHeight 390`）、沒有安全區**。收尾版把黑條整支拿掉，tier 3 的版面回到與 v0.53 逐值相同（§F5 的 L10），N1 描述的現象因此不存在。黑邊＋版面安全區留給招式可辨性卷 |
| **N2** | F9 轉綠是靠擴大白名單，且掛在不涵蓋它們的「修訂三」底下 | **真的修好** | 凍結檔的修訂三改寫成**完整清單**（11 個條目逐檔一句「為什麼一定會動」），並註明「這一條會讓 F9 由紅轉綠，所以走使用者明確同意」。時序與理由都留在凍結檔裡 |
| **N3** | 報告／凍結檔與 repo 現況多處對不上 | **真的修好** | ① 凍結檔修訂二殘留的 `--nullbase` 字樣改成 `--basecj=<基準產物>` ② 報告 M1 的 horizon 改用實測 **1290／1360／1380**（fill 0.921／0.971／0.986）③ F9 檔數改成 **27** ④ 證據的 `ver` 問題見下方 F9 註 |
| **N4** | `dmg-readability` 的基準沒落檔 | **真的修好** | 對基準樹跑了兩顆 seed，`pix.json` 收進 `dmg-base-s1/`／`dmg-base-s3/`：seed1 `R1:true R2:true`、`maskN:10`、空過 **0**；seed3 `R2:false`、`maskN:0`、`maskDropped {moved:3, tinyMask:2}`、`via {hitstop:3}`、空過 **1**——與新版**逐項相同**，修訂二引用的事實現在有檔可查 |
| **N5** | `?closeup=0` 關不掉 CINEMA | **真的修好** | `ys:fx-trait` 多帶 `cinema: pwCloseup()`，`camera-director` 只在 `d.cinema !== false` 時切 CINEMA（本檔一如既往不讀 `PW_FX`）。**L8** 斷言：`?closeup=0` 時 tier 3 的 `maxK===0`、`onCount===0`。★關於黑條那半條★：一版寫過「黑條不跟著關，因為它還負責讓字幕內縮」＝循環論證（r3 M-4 抓到），四版改成一起關；**收尾版黑條整支不存在，這半條斷言隨之刪除**，`?closeup=0` 現在只管 CINEMA 一件事 |
| **N6** | F6 量的是暖機後的第 2 場，而第 1 場最低 18fps | **真的修好（記錄）** | 報告 §F6 已註明「`duel-perf` 的 `onDuel` 是 `if (n !== 2) return`，量的是 shader 編完之後的第 2 場」，並把 `fpsdiag.json` 的 `perDuel:[18, 55]` 列為**記錄項**：玩家開第一場對決會看到 1 秒視窗最低 18fps，這件事被排除在 F6 的量測位置之外 |
| **N7** | `-nobox` 不是同幀 | **連同黑條移出本卷** | `-nobox` 那一組整組刪除（黑條沒了就沒有「加不加黑條」的對照）。順帶解掉 r5 M-7：五版之所以有 6 張不是對決畫面，真因就是「一場塞兩趟」比一場對決還長。★仍然沒有的★：人眼段拿不到「CINEMA 機位有沒有變」的同幀 A/B——要做得先把演出凍結（`dmg-readability` 的虛擬時鐘那一套），成本高，留給下一卷 |
| **N8** | `traitfx-drive`／`duel-perf` 在 worktree 找不到 playwright | **真的修好** | 兩支都改成兩段候選路徑的退路（與 `duel-drive`／`lbox-probe`／`t3-shot`／`pace-ab` 同一寫法），找不到才 throw 並提示 `NODE_PATH` |
| **N9** | `duel-drive` 的 TDZ 地雷（`lboxWatch` 用了後面才宣告的 `now`） | **真的修好** | 改用 `Date.now()` 並就地寫明理由 |
| **N10** | 四支重寫短版留下新舊兩段註解 | **真的修好** | 掃描 `SHORT` 區塊裡「兩個註解區塊中間沒有程式碼」的情形，`zuling.js` 3 處、`xianghuo.js` 1 處已刪掉舊的；重寫工具也改成「往前吃掉緊鄰的註解區塊」，不會再疊 |
| **N11** | 凍結檔寫「`--tier=1` 30 套」但實作是 27 套 | **沒修到（字面，記錄）** | 凍結檔正文不動（門檻一字未動的紀律）。實質沒有洞：三尊沒有 `SHORT`，`--tier=1` 跑 27 支、`--tier=2` 跑 30 套；「短版缺席會怎樣」由邊界實測補（強制三尊跑 `--tier=1` → **0/2 pass、rate=180**） |


## 三條修訂的改前改後（詳版在凍結檔 §2.1 修訂四／五／六）

### 修訂四：F3 主條以實測值通過
| 版本／來源 | 中位的中位 | 全距（展幅） | 對基準的效果 |
|---|---|---|---|
| 基準 `adbb124`（作者二版量） | 5558.5ms | 135.5 | — |
| 基準（r2 覆審員獨立量） | 5574.5ms | 209.5 | — |
| 基準（作者三版量） | **5521.5ms** | 73.5 | — |
| 一版（一次 4 場中位，無重複量測） | 5302ms | — | −271.5ms |
| 二版（作者） | 5032.5ms | 58.5 | −526ms |
| 二版（r2 覆審員獨立量） | 5050ms | 290 | −524.5ms |
| 三版（作者） | 5009ms | 53.5 | −512.5ms，`overlap:false` |
| **repo 現況（四版＝五版＝收尾版，`pace-ab.json`）** | **5018ms** | **159.5**（基準側 724） | **−586ms**（基準 5604ms），`overlap:false` |

四次獨立量測的效果同向且量級一致（**−512.5／−524.5／−526／−586**），四組全距都不重疊
⇒「v0.54 比基準快」已在噪音之外。效果幅度本身會隨當次基準值浮動（基準 5521.5～5604），
所以結論句寫的是「同向、不重疊」，不是「效果恆等於某個數」。
**5000 這條線比量法本身的展幅還細**（受測值離門檻 9–50ms，而同組態的展幅在 53.5～290 之間、
基準側最大到 724），量得出「快多少」、量不出「過不過」；使用者 2026-09-11 裁甲以實測值通過。
**門檻 5000 一字未動。**

### 修訂五：F3 子條改「合計 ≤ 基準合計 −10%」**且逐場不得比基準同場次慢**——**綠**
> r5 M6：這一段在五版之前一直留著二版的敘事（「#13 ❌紅／據實記紅／判準不動」），
> 與凍結檔現行的修訂五、與本報告逐條三態表的「綠」互相矛盾。整段依現行判準重寫。

**現行判準**（凍結檔 §2.1 修訂五，使用者 2026-09-11 裁甲＋R4 加嚴，兩條是**連言**）：
① 有 tier 3 出場的那批對決**合計** ≤ 基準同批合計 −10%；② **逐場都不得比基準同場次慢**（Δ ≤ 0）。

| 場次 | 基準 `adbb124` | v0.54 | 逐場 Δ | 逐場判準② | 招式數（新／基準） | 拍級分布 |
|---|---|---|---|---|---|---|
| #10 | 16087ms | 12662ms | **−21.3%** | ✅ | 8／8 | {1:1, 2:1, 3:1} |
| #12 | 14728ms | 13193ms | **−10.4%** | ✅ | 8／8 | {1:1, 2:1, 3:1} |
| #13 | 7096ms | 6855ms | **−3.4%** | ✅ | **3／3** | {1:1, 2:1, 3:1} |
| **合計** | **37911ms** | **32710ms** | **−13.7%** | 判準① ✅ | — | — |

**判定：綠**——合計 −13.7% ≤ −10% ✅ **且**三場 Δ 全為負 ✅。

**原判準（「逐場 −10%」）為什麼換掉**：對招數少的短場**數學上不可達**。
設 k ＝落在 tier 1 拍的招數，則 Δ ≈ −100（拍末 300+900+1400 ＝2600 vs 基準 2700）＋500（tier 3 的 1400 比 900 多花）−640k。
代進三場：#10 Δ=−3425 → k=5.98；#12 Δ=−1535 → k=3.02；#13 Δ=−241 → k=1.00（三場都落在整數上，模型成立；
r3 覆審員獨立重推同一模型逐項吻合）。#13 要達 −10%（709.6ms）需 k ≥ 1.73 ⇒ k ≥ 2，
而它只有 3 支招、其中 1 支落在 tier 1 拍 ⇒ **無論實作多好都過不了**，那是恆假的一半，不是驗收條件。

**加嚴那一步（R4 M-2，自行記錄，不需再同意）**：只看合計會留下 1409.9ms 的餘裕——
「只把短場弄壞」的實作（#13 退步到 8264ms、比基準 7096 慢 16.5%）合計照樣綠。
補上條件②之後餘裕壓到 241ms，pass set 嚴格縮小。

**仍然沒被量到的**（凍結檔修訂五末段同樣記著）：合計判準量的是「這一批場次有幾支招被縮短」，
**tier 3 的 +500ms 只是被動減分**。原條件的用意（防「大招把對決拖長」）在新判準底下仍然沒有被量到，留給下一卷。


### 修訂六：F4 改判「有樣本部分全過、`nullCount` 降記錄項」
| | 基準 `adbb124` | 一版 | 二版／三版 |
|---|---|---|---|
| n | 10 | 10 | 10 |
| `deepOk` | 10/10 | 10/10 | **10/10** |
| 有樣本的 `monoQuiet` | 8/8 | 8/8 | **6/6** |
| `nullCount`（記錄項） | 2 | 2 | **4** |

四個 null 全是 hit 類、`quiet=0`（punch 排光靜幀）；**沒有任何一筆從 `true` 變 `false`**，
基準那兩筆通過時的 `quiet` 只有 3 和 4（正好卡在 `need` 地板）。成因交治具小卷。
`dmg-readability` 那一半（修訂二口徑）：seed1 空過 0 ＝基準 0、seed3 空過 1 ＝**基準 1**，基準證據已落檔（N4）。

## F5 人眼段的前置：六支短版重寫（盲讀第三輪，使用者 2026-09-11 裁定；收尾版一行未改）
讀者 D 歸納的低分共同特徵是「**效果只發生在攻方自身、無指向性、受方無反應**」；
二版重寫成功的雷女之火（短 5/4 vs 完整 3/3）做法是「法寶本體提前到 78ms 出現、燒滿整段」。
六支各補「本體出現＋指向對手／受方反應」：

| 招 | 上一輪分數 | 改了什麼 |
|---|---|---|
| 祖靈之眼 `wardFirst` | 短 1/2 vs 完整 3/3 | 眼白球＋瞳球提前到 35ms 現形（原本只有眼瞼骨骼在動）；注視光束改兩條疊起來、燒滿 95→215ms（原本只有 70ms）；**被盯到的那一隻退縮＋邊光暴亮**（受方反應） |
| 王爺劍 `eliteCleave` | 短 2/2 vs 完整 4/3 | 補讀者 C 點名的「**紅刃衝刺拖出寬軌跡**」：整尊前衝距離 0.13→**0.34**，另加三條錯開淡出的軌跡帶從自己拉到對面；對面四隻依序退縮 |
| 送王船 `wardAbsorb4` | Q2 掉 ≥2 | 改以「**船前衝＋撞開一道水牆打向對面**」為主角（前衝 0.16→0.30，水牆環＋水花盤指向對面、對面三隻退縮）；金罩降為薄薄一層配角（讀者 C：「淡白半圓罩與地上光環互相分不出」） |
| 福壽綿長 `wardRegen1` | Q2 掉 ≥2 | 燈本體改成一顆 35ms 就現形、脹到 **1.9×** 的大火球；暖火飛行拉長到 88ms 並在落點爆一次；同伴腳下加落點環 |
| 五營旗 `swarmRally` | Q2 掉 ≥2 | 補**旗面本體**（一片會隨手臂翻轉的布＋旗桿），舉旗時現形、落旗時劈到前方；五方光陣外再加兩道往外推的令波 |
| 媽祖令旗 `wardAtkAll1` | Q2 掉 ≥2 | 同上補**旗面本體**（掛在 `FlagMast` 上、隨甩動前後擺），兩道令波推過本方整排 |

六支重寫後仍過 F2／F10：全部 `t1/260ms`、`rate=1.0`、`clean`、`fill` 0.869–0.885、`acts` 10–30。
兩張 contact sheet 已用同一組幀位（20%／45%／75%）重產。**我沒有做盲讀、沒有改量表、沒有改切圖程序。**

---

## r1 覆審 findings 逐條三態

| # | 標題 | 三態 | 證據／說明 |
|---|---|---|---|
| **C1** | CINEMA 無取消路徑 | **真的修好** | 加 `endCinema()`（照 `endFocus` 走 outMs 回位段），掛 `onTraitCancel`／`onDuelEnd`／`onTable`／`onEnd`。`lbox.json` 的 `cancel` 三條路徑 `kAtCancel=1.0`、`onAfter300=0`、`lastOnT=316ms`（<400）。**突變驗紅**：拿掉 `onTraitCancel` 那一行 → L5 FAIL、`onAfter300` 0→18、`lastOnT` 684ms（`scratchpad/lbox-mut.json`），還原用 `scratchpad/cd-before-mutation.bak` 副本 |
| **H1／M7** | `duel-perf perf` 靜默吃掉 `--root` | **真的修好** | perf 改吃 `--root`＋加 `--seed`；`KNOWN_FLAGS` 對不支援的旗標 **throw**。F6 重量在真基準樹上：`fps-ab.json` |
| **H2** | 黑條在真實畫面上幾乎看不見 | **二版真的修好 → 連同黑條移出本卷** | 二版把 `z-index:-1` 改成 41、加 L6 自解 PNG 驗像素（on 時上下帶亮度 0.00，off 29.38／40.76，中段對照 Δ0.76）——黑條**看得見了**，然後 r2 就抓到它蓋掉字幕 82.4%。這一列與 N1 是同一個故事的兩端。收尾版黑條不存在，L6 一併刪除 |
| **H3** | 「黑條僅 tier 3」沒有斷言＋不實宣稱 | **二版真的修好 → 收尾版連同黑條移出本卷** | 二版補上 `duel-drive` 的 `MutationObserver` 與逐場 `lboxMs`（14 場：無 tier 3 的 11 場全 0、有 tier 3 的 3 場 5771／4888／2687，留作歷程）。收尾版黑條拿掉後那個 observer 會永遠掛不上、`lboxMs` 恆 0 ＝**靜默恆綠的治具**，所以整段刪除而不是留著 |
| **H4** | tier 3 人眼交付物不含 CINEMA（原文還提到黑條） | **真的修好（收尾版再修一次）** | `t3-shot.mjs` 走 `index.html` 真實路徑，在 `duel-drive` 的 `onDuel` 掛點（對決**演出進行中**）派 tier 3 事件後截圖。收尾版拿掉 `-nobox` 對照（黑條沒了就沒有對照的意義），改成 6 場 ×3 幀＝18 張，並加 `duelOk` 機械欄位（r5 M-7）。一版的 `sheet-t3-legend.png` 保留但降級為「短版對照用」 |
| **H5／M6** | F3 量測噪音大於效果／seeds 沒落檔 | **真的修好** | 新治具 `pace-ab.mjs`：**交錯**跑（A,B / B,A 交替）、每組 5 次、輸出中位的中位與全距、seeds 與 url 落檔。實測全距 135.5／58.5，**不重疊**；`duel-drive` 也把 `url`（含 seed）寫進輸出 |
| **H6** | 工作區在覆審期間持續改動／`--nullbase` 是自由數字 | **真的修好（口徑部分）** | `--nullbase` 移除，改 `--basecj=<對基準樹跑出來的 json>`，由**同一支判官**對基準跑一次取 `nullCount` 當上限（`nullBaseSrc` 欄位記來源）。§2.1 只有修訂一／二／三，三條都是主對話轉達的使用者裁定。**「工作區是移動中的目標」這一點屬實**——二版起所有量測都跑在 `scratchpad/headfull`（`git archive HEAD` ＋工作區同步）或直接對 worktree，報告每個數字都標明來源 |
| **H7** | F0 的假綠條件成立（trace 不含 `war.beats`） | **真的修好** | `trace-eq` 加 `--beats`：對 old／new 做**同一個注入**序列化 `[beat,kind,side,trId,target]`。基準樹 vs 二版 **equal＋`injected:true`**。**鑑別力**：把 `pwRec` 記的 `tgt.id` 改成 `tgt.id+100` → 預設 `trace-eq` 仍 equal、`--beats` **不等**（`first diff @959`），證明它比預設嚴 |
| **M1** | tier 3 的 1400ms 有三分之一是空的 | **真的修好** | 三尊各補一段**只在 `st.tier===3`** 的餘韻（tier 2 走同一支函式、行為逐項不變）：horizon 929／1067／1013 → **1290／1296／1294**（fill 0.921／0.926／0.924，收尾版實測）。★中間值 1290／1360／1380 是二版的，三版為了把 `rate` 壓回凍結的 ≤1.0 又壓縮過一次（r3 H-3）★。`traitfx-drive` 加 `fillOK`（≥0.85）當常設閘門 |
| **M2** | 「`TRAITS[].tier` 是上限」在拍級規則下失效 | **真的修好** | 新增 `pwMoveTier(trId, beatTier) = min(拍級, 招的上限)`，普通招上限 2 → tier 3 的拍裡只有傳說招走 1400＋CINEMA。單元測試 7 條斷言覆蓋「普通招與傳說招同拍」。`--tier=3 --only=<三支普通招>` 現在會紅在 `fillOK`（`tfx-t3-mixed.json`）——那正是「這個組合在實作上不會發生」的反面證據 |
| **M3** | `need` 地板 3 是自由參數 | **真的修好** | 就地寫下推導：`mono()` 判反轉至少要兩個相鄰差 ⇒ **至少 3 個取樣點**，2 點只有 1 個差、斷言恆真（零鑑別力）。比例部分維持「8 幀 × 視窗/基準視窗」，上界 8 |
| **M4** | F1 那一列要加限定語 | **真的修好** | 本報告三態表改寫成「**0 處重複來源**」，並在 §F1 點名 `PW_FX` 那三張表就是唯一事實來源 |
| **M5** | F9 超白名單是 3 個不是 1 個 | **真的修好** | 計畫檔第 1 節補列 `js/renderer.js`、`lbox-probe.mjs`、`traitfx-sheet.mjs`，連同二版新增的 5 支治具與凍結檔本身，共 9 列（見 §F9） |
| **M8** | 鑑別力複驗要用完整基準樹 | **真的修好** | 本報告開頭與 §F1 都寫明「一律對 `git archive adbb124` 的完整樹跑」，指令原文附在 §F1 |
| **L1** | `acts` 可被空 tween 灌水 | **沒修到（記錄）** | 指標本身沒改。27 支實測 5–24 都是真動作（覆審員親讀 9 支確認）。要當長期閘門得加「該 tween 有掛 mesh 或骨骼」的條件——留給招式可辨性卷 |
| **L2** | 測試 fixture 餵了引擎產不出來的值 | **真的修好** | `warWin` 的 `{ war: { tie: false } }` 保留（`paperWar` 勝方分支確實沒有 `tie` 欄位，但 `pwBeatTier` 讀的是 `!f.war.tie`，`undefined` 與 `false` 同義）——**改成 `{ war: {} }`**，貼近真實回傳 |
| **L3** | 證據檔的 `ver` 停在 v0.53 | **真的修好** | 二版所有證據都在 `VERSION="0.54"` 之後跑，`duel-*.json` 的 `ver` 已是 `v0.54・…` |
| **L4** | tier 2「完整版」本來就常塞不進 900ms | **沒修到（記錄）** | v0.53 既有行為（`rateMax` 2.2），與本卷無關。`tfx-t2.json` 的 `maxRate` 分布最高 2.14；報告不再用「從容」形容 tier 2 |
| **L5** | 同一拍兩支招時黑條會閃一下 | **二版真的修好 → 連同黑條移出本卷** | 二版把黑條改成拍級（拍開始開、拍末關）；收尾版黑條不存在。★CINEMA 本身仍是招級★（只有 `TRAITS[].tier===3` 的傳說招觸發），那一條沒變 |

---

## F0 等價雙向
**收尾版重跑**：
```
node tests/tools/trace-eq.mjs scratchpad/old.html index.html          （old.html＝git show adbb124:index.html）
→ {"seeds":"1..20","bytesOld":357285,"bytesNew":357285,"equal":true}
node tests/tools/trace-eq.mjs scratchpad/old.html index.html --beats
→ {"mode":"beats","bytesOld":540776,"bytesNew":540776,"equal":true,"injected":true,"verdict":"拍序列逐位元組相等 ✅"}
node tests/tools/trace-eq.mjs index.html --mutate
→ {"mutation":"CFG.ROUNDS 12 -> 11","bytesSrc":357285,"bytesMutant":341041,"differs":true,"verdict":"突變驗紅 ✅"}
```
既有 9 套（收尾版逐套重跑）：`aistake` 8／`conscap` 5／`duel-desync` 7／`legend` 32／`lineup-order` 8／
`nightrules` 16／`review` 28／`roles-balance` 32／`wish16` 36 全綠，**0 紅**，diff 為空（沒有新增任何毫秒斷言）。
`tests/fxtier.test.mjs` **14 綠**（對完整基準樹仍 **0 綠 14 紅**，見 §F1）。

**`--beats` 的鑑別力**（一版的 F0 只證明「勝負與扣血沒變」，凍結檔自己列的假綠條件本來就成立）：
把 `pwRec` 記的 `tgt.id` 改成 `tgt.id+100`（只動記錄、不動勝負）→ 預設 `trace-eq` **仍 equal**，`--beats` **不等**（`first diff @ 959`，`[1,"hit","A","",0]` vs `[1,"hit","A","",100]`）。

**`?fxtier=0` 的等價**：`trace()` 不讀 URL，那一半用真實路徑證明——`FXC.tiers = {1:0, 2:12, 3:0}`（12 拍全 tier 2＝v0.53 行為）、4 場中位 5643.5ms（基準 5558.5ms 同一量級）。
（二版同一批證據裡的「`lboxMs` 全 0」隨黑條移除而不再存在，不再引用。）

## F1 分母歸一
分母（我自己 grep，不抄評審）：**runtime 命中 10**（招式時長語意 **4**：`index.html:3976 TRAIT_MS`／`js/trait-fx.js:366 ||900`／`js/camera-director.js:74 LEAN.ms`／`index.html:3973 BEAT_MIN_MS`；排除 6）、**治具命中 47**（語意 **17**；排除 30，其中 19 處是 `setTimeout(r,900)` 開服等待）。

改後 runtime **0 處重複來源**——`index.html` 仍有三個 900 字面值，就是 `TRAIT_MS_BY_TIER`／`BEAT_MIN_MS_BY_TIER`／`TIER_BASE_MS` **這三張表本身**（唯一事實來源，不是散落的複製品）；測試的 allow 清單就地寫了理由。
`js/trait-fx.js` 的 `TFX.baseMs` 也在二版前收掉了，改由事件帶 `detail.baseMs`。

**鑑別力（必須對完整基準樹跑）**：
```
git archive adbb124 | tar -x -C scratchpad/basefull
node tests/fxtier.test.mjs --html=scratchpad/basefull/index.html   → 0 綠 14 紅
```
14 條全紅且都紅在行為斷言（`||900` 退路還在／`LEAN.ms` 退路還在／runtime 還有 4 處／`pwBeatTier` 未匯出／`pwMoveTier` 未匯出／`有 tier 的招：得到 "[]"`）。
★只給一份 `base-index.html`（沒有 `js/`）會有 3 條紅在 `ENOENT ... js/trait-fx.js`＝旁枝錯誤，`02 §6.1` 第 1 條明文禁止——所以一律用完整樹。★

## F2 短版原生合身
**收尾版重跑**（黑條的移除不碰 `js/trait-fx/*`，但照樣重跑一遍，不沿用）：
```
node tests/tools/traitfx-drive.mjs <out> --tier=1 --port=9582   → 27/27 pass
node tests/tools/traitfx-drive.mjs <out> --tier=2 --port=9583   → 30/30 pass
node tests/tools/traitfx-drive.mjs <out> --tier=3 --port=9581   → 3/3 pass
```
- tier 1：`run.ms` **全 260**、`maxRate` **全 1.0**、`clean` 全過、`fill` **0.854–0.896**、`acts` 5–30、重複簽章 0。
- tier 3：三尊 `run.ms` 全 1400、`maxRate` **全 1.0**、horizon **1290／1296／1294**（fill 0.921／0.926／0.924）、acts 12／12／30。
  （★r3 H-3 的教訓★：三版之前這裡是 1290／1360／1380、rate 1.04–1.06，**超過凍結的 `rate ≤1.0`** 卻被報告寫成綠；
  三版把三支餘韻壓進 1.0，`rateOK` 也不再只限 tier 1。現在三個 tier 的數字都在門檻內。）
- 防假綠四道：`msOK`（防「`--tier=1` 其實還在跑 900」）、`rateOK ≤1.0`（防靠加速硬擠）、`clean` 判準未放寬、**`fillOK`（新增，防演完之後乾等）**。
- **邊界實測**（「短版缺席退回完整版不是恆綠退路」）：拿沒有 `SHORT` 的三尊強制跑 `--tier=1` → **0/2 pass、`rate=180`**。

## F3 節奏（主條依修訂四以實測值通過、子條依修訂五綠）
### 主條：4 場 duelsMs 中位 ≤5 s
量法改成 `pace-ab.mjs`：**交錯**跑（第 i 輪 A→B、第 i+1 輪 B→A）、同 seeds、各 5 次，取「每次 4 場中位」再算中位與全距。
```
node tests/tools/pace-ab.mjs <out> --a=scratchpad/basefull --b=scratchpad/headfull --seeds=7 --duels=4 --runs=5
```
| 組 | 中位的中位 | 全距 | 展幅 | 量於 |
|---|---|---|---|---|
| A＝基準 `adbb124` | **5604ms** | — | 724 | 四版 |
| B＝v0.54 | **5018ms** | [4996, 5155.5] | 159.5 | 四版 |

（歷次量測全部保留在 §修訂四 的逐版表；上表只留 repo 現況那一組，這是 r4 M-1／r5 M-5 要求的「不要兩套數字」。）

**兩組全距不重疊**（`overlap:false`）⇒ 效果 −586ms **大於**這台機器的噪音——這解掉了 r1 H5「噪音大於效果、量測不可信」那一條。
但 5018 **仍 >5000**（差 18ms，−0.36%）→ **F3 主條在門檻上是紅的**，依修訂四由使用者裁甲以實測值通過。

**★收尾版沿用這組數字的理由★**（`02 §6.1` 第 4 條：沿用要說得出為什麼不必重量）：
收尾版只刪除 letterbox 的 CSS／DOM／JS 與治具的黑條觀察器，**沒有動到任何一個 `pwSleep` 的毫秒**
（`TRAIT_MS_BY_TIER` 與 `BEAT_MIN_MS_BY_TIER` 兩張表逐值未改、`pwPlayBeat` 的等待式未改），
而 `pwLetterbox()` 本來就不是被 `await` 的（它是同步的 class 切換）。
`git diff 00706e0..收尾版 -- index.html` 裡與時序有關的行數 ＝ **0**。
**歸因**：拍級分布已翻成 `{1:8, 2:4, 3:0}`（修訂一生效，一般擊殺拍回到 260ms 短版＋300ms 拍末）。
剩下的時間大宗**不再是拍末等待**，而是逐筆事件（`EV_BUDGET_MS 1050`）、拍首字幕（`BANNER_MS 360`）、進退場（`ENTER_MS 480`＋`END_MS 1000`）——那些常數在本卷「不做什麼」裡明列不動。

### 子條（現行判準＝修訂五：合計 ≤ 基準 −10% **且**逐場不得比基準同場次慢）——**綠**
14 場實測（`seed=7`，玩到第 7 夜前後三尊才上場）：
| 場次 | v0.54 | 基準 `adbb124` 同場次 | 差 | 逐場 Δ% |
|---|---|---|---|---|
| #10 | **12662ms** | 16087ms | −3425 | −21.3% |
| #12 | **13193ms** | 14728ms | −1535 | −10.4% |
| #13 | **6855ms** | 7096ms | −241 | −3.4% |
| **合計** | **32710ms** | **37911ms** | **−5201** | **−13.7%** |

**判定：綠**（合計 −13.7% ≤ −10% ✅ 且三場 Δ 全為負 ✅）。逐段推導與加嚴的理由見 §修訂五。
**原文的「≤8 s」為什麼換掉**：它量到的是「第幾場」不是 tier 3——基準（完全沒有分級、招式全 900ms）
在同一組場次是 16087／14728／7096ms，**自己也是 3 場中 2 場超標**；每場的 tier 3 只有 1 拍（1400ms），佔 12.6 秒的一成。
**這兩場仍然超過 8 秒**（12.7／13.2 秒），只是原因是後期夜晚部隊變大、事件數變多，與本卷無關——據實記在這裡。

## F4 可讀性不退（**紅**：兩半中的一半）
### `dmg-readability`（符合修訂二的新口徑）
| seed | R1 | R2 | 空過數 | 基準空過數 | 關鍵數字 |
|---|---|---|---|---|---|
| 1 | PASS | **PASS** | 0 | 0 | 遮罩中位 62.72（≥25）、`maskGe25Ratio` 1.0、`back200MaskAbsMax` **2.21**（≤5）、字級 27.2／35.36 |
| 3 | PASS | FAIL（空過） | 1 | **1** | `maskN=0`、`maskDropped {moved:3, tinyMask:2}`、`via {hitstop:3}` — 與基準**逐項相同** |

新口徑是「每 seed 的空過數 ≤ 基準」→ **這一半通過**。
★誠實記一筆抖動★：seed 1 的 R2 在二版中途曾量到 `back200max=5.77`（紅），重跑為 2.21（綠）。同組態兩次落在門檻兩側，這個指標本身有抖動；本報告採用最後一次完整重跑的數字，並把兩次都記在這裡。

### `closeup-judge`（**不符合新口徑**）
```
node tests/tools/closeup-judge.mjs <cu-on1.json> --basecj=<cu-base1.json>
→ nullCount 4 / nullBase 2（來源：cu-base1.json 實測）；n=10、deepOk 10/10、monoQuiet 6/6
```
| | nullCount | 那幾筆 | deepOk | 有樣本的 monoQuiet |
|---|---|---|---|---|
| 基準 `adbb124` | **2** | rows 6／8（`quiet=0`） | 10/10 | 8/8 |
| v0.54 一版 | 2 | 同上 | 10/10 | 8/8 |
| **v0.54 二版** | **4** | rows 0／2／6／8（全部 `quiet=0`） | 10/10 | 6/6 |

**4 > 2 ⇒ F4 紅。** 歸因：四筆全是 hit 類的 `quiet=0`——命中拍的 punch 把靜幀排光（`closeup-judge` 檔頭 v0.45 就記過這個現象）。
二版把一般擊殺拍縮到 260ms 之後，同樣次數的 punch 擠在更短的拍裡，focus 視窗能取到的靜幀更少，於是從 2 筆變 4 筆。
**是「可判樣本變少」不是「可讀性變差」**：`deepOk` 仍 10/10（推近深度全過）、有樣本的 `monoQuiet` 6/6 全過。
依 `02 §6.1` 第 5 條「樣本數不足以區分有／沒有時不構成放行理由」——所以照實記紅，不主張它其實沒問題。
成因（punch 排光靜幀）依修訂二的裁定另開治具小卷，本卷不修。

## F5 三級可辨（機械段依 §2.1 修訂七改寫）
### 機械段之一：CINEMA 只在 tier 3（`lbox-probe.mjs` 的 L1／L2／L5／L8）
```
VERDICT L1=PASS L2=PASS L5=PASS L8=PASS L9=PASS L10=PASS err=0 → PASS
tier1 {ms:260,  samples:53,  onCount:0,   maxK:0}   ← 反面
tier2 {ms:900,  samples:91,  onCount:0,   maxK:0}   ← 反面
tier3 {ms:1400, samples:121, onCount:104, maxK:1, onDuring:85, onAtTail:0}   ← 正面
cancel  ys:fx-trait-cancel / ys:duel-end / ys:table：kAtCancel 1.0／onAfter300 0／lastOnT 317・318・317ms（全 <400）
closeup0 {closeupOn:false, maxK:0, onCount:0}
```
- **L1 反面**：tier 1／2 期間 `cinemaOn()` 一幀都沒有 true。
- **L2 正面**：tier 3 期間 `maxK` 到頂，招式結束＋outMs 之後回到 false（不卡住）。
- **L5**（R1 覆審 C1）：三條取消路徑都在 `CINEMA.outMs(320)`＋餘裕內收乾淨。
- **L8**（R2 覆審 N5）：`?closeup=0` 時 tier 3 的 `maxK===0`、`onCount===0`。
  ★收尾版把這條的「黑條照舊開得起來」那半條刪掉★——黑條不存在了，`?closeup=0` 只管 CINEMA。

### 機械段之二：黑條在 DOM 與原始碼裡都不存在（L9）
```
lbGone {dom:[false,false], api:"undefined", src:{lbox:0, pwLetterbox:0, ids:[0,0]}}
```
`#lbTop`／`#lbBot` 兩個 id 在 DOM 中 `getElementById` 都回 null；`window.__yaoshi.pwLetterbox` 是 `undefined`；
`index.html` 原始碼裡 `.lbox` 規則 0 處、`pwLetterbox` 0 處、兩個 id 字面值各 0 處。
（只驗 DOM 不夠：改成 `display:none` 的 div 也會過，所以連原始碼一起掃。）

### 機械段之三（★本節主條★）：tier 3 那一拍的版面與 v0.53 逐值相同（L10）
**判準**（凍結檔 §2.1 修訂七）：真實對局裡三尊大招那一拍（錨點＝`ys:fx-trait` 且 `trId` 是三尊大招），
錨點 **+200／+400／+600ms** 三個時點，`#duel` 底下**所有可見子孫**的 rect 與 v0.53 基準樹同錨點逐值相同
（x／y／w／h 取到小數一位）＋`#duel` 自己的 `class`／`transform`／`box-shadow`／`overflow`／`scrollHeight`／`clientHeight`；
**與基準配對成功的錨點 ≥3 個**（分屬 3 場不同的對決）、橫式與直式都要；白名單一個都沒有；
樣本或元素數為 0、或有頁面錯誤，一律當不過。量測＋比對在 `tests/tools/duel-rects.mjs`。

**三個錨點確實分屬三場不同的對決**（`seed=7`、18 場）：對戰雙方分別是
`西家 vs 東家・東風`／`東家 vs 北家`／`南家 vs 東家`，可見元素數 74／70／54。

```
node tests/tools/duel-rects.mjs <out> --root=scratchpad/basefull --duels=18   # v0.53 基準（兩次）
node tests/tools/duel-rects.mjs <out> --duels=18                              # 收尾版
node tests/tools/duel-rects.mjs <out> --root=scratchpad/v5full --duels=18     # 五版 00706e0
node tests/tools/duel-rects.mjs <out> --root=scratchpad/mutfull --duels=18    # r5 那個「治具漏掉」的突變
```

| 比對 | 橫式 844×390 | 直式 390×844 | 判定 |
|---|---|---|---|
| **收尾版 vs 基準** | 錨點 3／樣本 9／逐值比過 **594 個元素**／**diffs 0** | 錨點 3／樣本 9／逐值比過 **594 個元素**／**diffs 0** | **綠** |
| 基準 vs 基準（同一棵樹跑兩次） | 錨點 3／樣本 9／594 元素／**diffs 0** | 錨點 3／樣本 9／594 元素／**diffs 0** | **綠**（量測本身不抖） |
| **五版 `00706e0` vs 基準** | **diffs 40（比對上限截斷）**：`#duel` 的 class `on pw lbox`≠`on pw`、`transform` `matrix(0.8,0,0,0.8,0,0)`≠`none`、`box-shadow` `0 0 0 844px #000`≠`none`，以及 `#duel` 自己與底下每一個元素的 rect | 同左（`transform`／`box-shadow`／class 三項與每一個元素的 rect 全不同） | **紅** |
| **r5 突變（`#duelBeat` 上移 9px）vs 基準** | 錨點 3／樣本 9／594 元素／**diffs 15**：`#duelBeat` 與它的 `span` 的 `y` **−4.1 ≠ 基準 4.9（整整 9.0px）**，3 錨點 ×3 時點全中 | 錨點 3／樣本 9／594 元素／**diffs 15**：`#duelBeat` 的 `y` **200.9 ≠ 基準 209.9（9.0px）** | **紅** |

- **活性**（相等性斷言必附，`02 §6.1` 第 1 條）：橫式與直式各 **3 個錨點（分屬 3 場不同的對決）×3 個時點 ＝9 個樣本**、逐值比過 **594 個元素**、頁面錯誤 **0**、unmatched **0**。
  判定式本身就把活性寫進去：**配對成功的錨點 <3、配對樣本 < 錨點 ×3、任一樣本掃到 0 個元素、頁面有錯誤**——
  四者任一成立就不算過（`lbox-probe.mjs` 的 `l10()`）。兩邊一起空不會變成「逐值相同」。
- **鑑別力正反都驗**：把程式換回五版 → 紅；把系統換到健康狀態（基準自己比自己）→ 綠。
  ★特別驗 r5 那個突變★：r5 用「`#duelBeat` 上移 9px、切掉 19.2%」的突變證明五版的 L1–L8 **全綠**（零鑑別力）；
  同一個突變在新判準下紅在 `#duelBeat` 的 `y`，因為判準問的是「版面有沒有被動到」而不是「黑條蓋到誰」。
- ★量測紀律（`02 §6.2`：先定位才准處置，三次歸因都寫在這裡）★
  這支量具一開始**基準樹自己跟自己跑兩次也會紅**，逐筆攤開之後有三個來源，沒有一個是版面差異：
  1. **CSS 動畫相位**：`#beatLamps i.cur` 掛著 `lampPulse .9s infinite alternate`（`index.html:409-410`）——
     同一個中心、寬高差 0.1–0.8px。處置：讀 rect 前定格。
  2. **Web Animations 相位**：招式字幕 `#duelMove`／`.mvline`／`.mvdesc` 的進場是 `pwRise()` 用
     `el.animate()` 播的（`index.html:5120-5126`）——**CSS 的 `animation:none` 關不掉它**；
     徵狀是同一個中心、寬度 ×0.9958（`scale(.96)→none` 還沒跑完）。
     處置：改用 `document.getAnimations()` 定格（有限的 `finish()` 跳終態、無限的 `pause()` 歸零，量完 `play()` 放回去）。
  3. **計時器邊界**：原本的第三個取樣點 +800ms 離 `#actorCard` 的收卡計時器（`PW_FX.ACTOR_CARD_MS=700`）只有 100ms，
     機器一忙收卡晚幾十毫秒，掃到的元素集合就多一組（出手卡＋兩個子節點）。
     實測收尾版 3 個錨點裡**剛好 1 個踩中**（`#duel.n` 74 vs 71），另外 2 個與基準逐值相同 ⇒ 抖動不是差異。
     處置：三個時點全部退到 700 之前（+200／+400／+600，最近的也留 100ms 餘裕），出手卡在每一個樣本裡都在場。
  **三個處置都不會掩蓋要抓的東西**：`transform:scale(.80)`／`box-shadow`／`padding`／`overflow` 是靜態宣告不是動畫，
  而「元素被移走／被挪位」在新時點下照樣是差異——五版與突變樹在這組設定下仍然紅（見上表）。
- **直式不再是恆真的一半**（r5 L3）：`#rotateHint` 是 `position:fixed;inset:0` 的蓋板，會讓 drive 點不到按鈕。
  **兩棵樹同樣注入** `#rotateHint{display:none!important}`（對稱注入，同 `trace-eq --beats` 的做法），
  直式因此真的玩到 tier 3 拍、真的比得到 rect。

### 人眼段（交使用者挑）
- `sheet-t1-short.png`（27 支短版）／`sheet-t2-full.png`（30 套完整版）：幀位 20%／45%／75%，依 tier 換算。
  （短版本身收尾版一行未改，兩張 sheet 沿用五版重產的那一組。）
- **`shots-t3-real/`（重拍）**：tier 3 從**真實對決路徑**截，**6 場 ×3 幀 ＝ 18 張**＋`meta.json`。
  實跑：`{shots:18, duelOk:true, cinemaOk:true, clean:{cinema:false}, errors:0}`（exit 0）。
  ★「是不是對決畫面」現在是機械欄位★（r4 M-4／r5 M-7）：每張截圖當下記 `#duel` 的 `display`、`.on`、
  場上 3D 妖數、`cinemaOn()`／`cinemaK`，任一張不是對決畫面就整支紅（`duelOk`）。
  五版那組 18 張裡有 6 張是「第 N 夜・戰況」總結面板，真因是一場塞兩趟（`withBox`＋`-nobox` ≈4.7 秒）比一場對決還長；
  黑條拿掉後 `-nobox` 對照本來就沒有意義，改成一場一趟（≈2.35 秒）穩穩落在對決裡面。
  出招方不是三尊本尊（袋子裡有沒有三尊逼不出來）——這一組驗的是「**CINEMA 仰視機位＋1400ms 在真實畫面上長什麼樣**」。
- `sheet-t3-legend.png`（一版產物）保留，但**降級為短版／完整版的對照**，不再當 tier 3 的人眼交付物。


#### 四支短版的改動（使用者 2026-09-11 裁定，盲讀 r1 掉分的那四支）
盲讀 r1 的教訓是「讀者認得出的招都是**本體直接出現**」，所以這四支各補一件看得見的法寶本體，其餘 23 支一格不動：
- **王爺劍 `eliteCleave`**（4/3→2/2，讀者 B：「沒拔劍」）：加一把 0.66 長的光刃＋劍柄掛在右手上，蓄勢時現形、揮砍時跟著劈過去，收勢才淡出。
- **祖靈之眼 `wardFirst`**：眼位疊一顆眼白球＋一顆瞳球，睜圓＝眼白暴脹到 1.35×、瞳孔縮成一點（原本只有眼瞼骨骼在動，畫面上沒有「眼睛」）。
- **雷女之火 `boltGamble`**：三道雷改成 78ms 就開始、各燒 92ms、間隔 26ms（原本只在 150ms 後閃 62ms，取樣幀常錯過）。
- **巴冷公主珠鍊 `eliteArmor`**：沿「心口→頭頂」的弧線串九顆珠，由內往外一顆一顆亮（原本只有骨骼縮放＋一顆珠，看不出「鍊」）。

四支重寫後仍過 F2／F10：`t1/260ms`、`rate=1`、`clean`、`fill` 0.85–0.92、`acts` 7／10／7／19。

## F6 fps（主數字**綠**；draw calls 那半條**未通過字面門檻，據實記紅**）
量在**真基準樹**上（r1 H1：一版的 `fps-base.json` 帶有只有新版才有的 `fxc.tiers`，那是新版跑的）：
```
node tests/tools/duel-perf.mjs perf <out> --root=scratchpad/basefull --seed=7 --port=<P>   # A＝基準 adbb124
node tests/tools/duel-perf.mjs perf <out> --root=scratchpad/headfull --seed=7 --port=<P>   # B＝收尾版
```
**收尾版重跑，交錯五輪**（A,B / B,A / A,B / B,A / B,A）：
| 組 | rendersPerSec（5 次） | 中位 | draw calls | triangles | visible |
|---|---|---|---|---|---|
| A＝基準 `adbb124` | 279.7／282.0／284.2／282.0／277.1 | **282.0** | 957／955／959／957／964（中位 **957**、全距 [955, 964]） | 353,100–353,342 | 16 |
| B＝收尾版 | 294.3／282.8／285.4／283.9／280.0 | **283.9** | 960／960／930／960／960（中位 **960**、全距 [930, 960]） | 344,910–353,166 | 16（一次 15） |

- **主數字 `rendersPerSec` 比值 283.9 / 282.0 ＝ 1.007**（≥0.90 ✅）。全距重疊，但方向不是變慢。
- ★**draw calls 那半條：中位 960 > 957，依門檻字面（「不增」）算紅，據實記紅、不調門檻**★。
  歸因（`02 §6.2`：先定位才准處置，這次定位得出決定性答案）——
  **同一棵收尾版的樹、同一份程式碼，只把 `?fxtier=0` 打開**（`--seed="7&fxtier=0"`）跑三次：
  **958／953／955（中位 955）**，正好落回基準的區間。
  ⇒ 那 3 個 draw call **不是新的繪製來源**（黑條本來就是 DOM、CINEMA 只動相機、tier 不新增 mesh），
  而是**節奏造成的取樣差**：`duel-perf` 取的是對決第 2 場某一幀，而本卷改的正是「那一瞬間有哪些演出 mesh 還活著」。
  兩組全距也重疊（A 自己的 964 就比 B 的中位 960 高）。
  ★五版報告寫的「新版 926 < 基準 958–965」不成立★：那三次 B 的 `visible` 是 15（少一尊在場），
  與 A 的 16 不是同一個場面；收尾版這次把 `visible` 一起印出來，才看得出 930 那一次也是 15。
- ★**決定性的同幀對照沒有了**★：五版用 `lbox-probe` 的 L4（同一頁面同一幀只切黑條、`calls` 逐值相同）
  證明黑條不進 renderer；黑條移出本卷後那條斷言一併刪除，本卷**沒有**替代的同幀對照。
  這一格留給下一卷（要嘛讓 `duel-perf` 在固定的演出時點取樣，要嘛改量「每一幀的 calls 分佈」）。
- ★`rafMedianFps` 兩邊都是 59.9＝**撞 vsync 天花板，零鑑別力**★，比值恆為 1.00，本報告不拿它當結論。
- ★**量的是第 2 場，不是第 1 場**★（R2 覆審 N6）：`duel-perf` 的 `onDuel` 是 `if (n !== 2) return`，
  註解自陳「第 1 場已把 shader 編掉」——量的是暖機後的穩態。**記錄項**：同一批證據的 `fpsdiag.json` 記著
  `perDuel: [18, 55]`，也就是**玩家開第一場對決會看到 1 秒視窗最低 18fps**（shader 首編），
  這件事被排除在 F6 的量測位置之外（`02 §6.1` 第 5 條：代理指標沒響只在它的量測位置上有效）。
- ★訊號展幅★：`rendersPerSec` 在這台機器上受背景負載影響很重（二版曾在殘留 `http.server`／chrome 沒清乾淨時
  量到 A 155.3／B 104.7，歸因＝量測環境污染，照 `02 §6.2` 留在歷史數據裡不刪）。
  收尾版這五輪跑在乾淨環境下，兩組各自的展幅只有 7.1（A 277.1–284.2）與 14.3（B 280.0–294.3），
  比值取中位＝1.007；即使取最保守的組合（B 最低 280.0 對 A 最高 284.2）也是 0.985 ≥0.90。


## F7 Playwright 零錯（**綠**）
收尾版重跑的每一支都印出 `errors`：
| 治具 | 規模 | errors |
|---|---|---|
| `duel-rects`（5 棵樹 ×2 方向） | 每次 18 場 | **0**（10/10 次） |
| `lbox-probe`（含 L10 的兩次 18 場 drive） | 1 次 | **0** |
| `t3-shot` | 8 場、18 張 | **0** |
| `duel-perf perf` | A5＋B5＋`?fxtier=0` 3 次 | **0**（13/13 次） |
| `duel-drive`（`duel-new`／`duel-fxtier0`） | 各 4 場 | **0** |
| `traitfx-drive`（tier 1／2／3） | 27＋30＋3 套 | 每案 `err=0` |

★二版曾記到 `duel-fxtier0` 有 2 個 error，逐字是 `fonts.googleapis.com … ERR_CONNECTION_TIMED_OUT`
＝**外部網路逾時、非受測物**；收尾版重跑為 0。兩次都記在這裡（`02 §6.2`：波動先歸因再放行）。★

## F8 文件（**綠**）
GUIDE §11.27 已對齊收尾版（第 6 條改成「Tier 3 ＝ 完整版＋CINEMA 機位」並寫明黑邊為什麼做不進這一卷、
第 7 條拿掉「無黑條」、量測紀律那條換成「列舉誰被蓋到永遠會漏掉下一個元素 ⇒ 用 rect 相等性」、
三條沒過的閘門數字改成 5018／18ms）；`VERSION="0.54"` 與 `VERSION_NOTE` 首段已改寫（黑條那句改成
「經五輪覆審後由使用者裁定移出本卷」）；`ART_BIBLE`／`GAME_DESIGN` 未出現在 diff。

## F9 範圍（**綠**，修訂三補列後）
```
git diff --stat adbb124..HEAD -- . ':(exclude)docs/experiments/2026-09-10-fx-tiers-evidence'
→ 28 files changed, 3645 insertions(+), 77 deletions(-)
```
共 **28 個檔**（五版 27 ＋ 收尾版新增的 `tests/tools/duel-rects.mjs`），全部在計畫檔第 1 節
（修訂三的完整清單 ＋ 修訂七補列的 `duel-rects.mjs`，逐檔理由見計畫檔）：
`index.html`／`js/trait-fx.js`／`js/trait-fx/{zuling,xianghuo,yinqi}.js`／`js/camera-director.js`／`js/renderer.js`（+1 行治具出口）／
`tests/fxtier.test.mjs`／`tests/tools/{fx-consts,duel-rects,traitfx-drive,traitfx-preview,traitfx-sheet,cam-drive,cam-unit,closeup-cam-unit,closeup-judge,lbox-probe,fpsdiag-probe,pace-ab,t3-shot,duel-perf,trace-eq,duel-drive}`／
`docs/IMPLEMENTATION_GUIDE.md`／凍結檔／計畫檔／報告＋證據。
★收尾版沒有動 `.gitignore`、沒有動任何不在清單上的檔★（`scratchpad/` 一律不進 commit）。
- 收尾版新增 `tests/tools/duel-rects.mjs`（F5 機械段主條的量測與比對），已補進計畫檔第 1 節（修訂七補列）。
- `TRAITS` 的 diff **只有三行**（三尊各加 `tier:3`）。
- 引擎函式（`paperWar`／`pwSide`／`pwClash`／`pwPrep`／`pwBolt`／`pwHaunt`／`pwStrike`／`pwRec`／`buildArmy`／`collectEffects`／`applyHooks`）diff 為空——**這一點是靠 diff 證明的**，不是靠 `trace-eq`（r1 H7）。

## F10 短版品質下限（**綠**）
27 支非 flinch 動作數 5–24，全部 ≥2。最小值 5（`eliteVsSwarm`）。

---

## 仍然沒解決的事（留給製作人與下一卷）
1. **F3 主條差 18ms**：量測已經可信（全距不重疊），但門檻沒過。要往下走只能動 `EV_BUDGET_MS`／`BANNER_MS`／`ENTER_MS`／`END_MS`，那是本卷「不做什麼」明列不動的常數。
2. **F3 子條的絕對秒數**：現行判準（修訂五）綠，但那兩場仍是 12.7／13.2 秒。成因是後期夜晚的對決本來就長（基準同場次更慢），與 tier 3 無關；要不要另立「基礎戰／後期戰」兩套秒數由製作人裁。
   另外，**合計判準量的仍不是 tier 3**（tier 3 的 +500ms 只是被動減分），原條件的用意沒有被量到。
3. **F4 的 `closeup-judge nullCount` 4 > 2**：拍變短讓可判樣本更少。成因（punch 排光靜幀）依修訂二另開治具小卷。
4. **★黑邊＋對決版面安全區（本卷移出，整包交招式可辨性卷）★**（凍結檔 §2.1 修訂七）：
   - `#duel` 現在是「填滿到溢出」（`scrollHeight 394 > clientHeight 390`）＋`overflow:hidden`，**沒有安全區**，
     所以任何黑邊都會壓到元素；`#beatLamps` 在**基準版就已經**有一部分被裁掉（r5 量到縮放後 58.5%，未縮放時的基準值下一卷要量）。
   - 3D 妖**不跟著 DOM 版面縮放**（`js/duel-figures.js:648-649` 的 `figScale` 只讀 `FIG.pixelH`／`FIG.creaturePx` 與相機距離，
     `realign()` 只用 DOM rect 算水平 offset），版面一縮就會脫節。
   - `.mvdesc` 沒有字級下限（五版縮 0.8 之後掉到 8.8px，F4 沒有任何一條在量字級）。
   - 這三件要一起做，才做得出「有內距、3D 與 DOM 同步縮放、字級有下限」的對決版面。
5. **F6 的 draw calls 那半條**：中位 +3（960 vs 957），依門檻字面是紅。歸因已做到決定性
   （同一棵樹 `?fxtier=0` 回到 953–958 ⇒ 不是新的繪製來源，是取樣那一幀有哪些演出 mesh 還活著）。
   要讓它變回可判定的閘門，得讓 `duel-perf` 在**固定的演出時點**取樣，或改量整場的 calls 分佈——下一卷。
   ★五版那條「同一頁面同一幀切黑條、calls 逐值相同」的決定性對照隨黑條一起移除，本卷沒有替代品。★
6. **`acts` 可被空 tween 灌水**（r1 L1，記錄未修）與 **修訂五的逐場條件沒有治具在機械判定**（r5 L2）：兩條都留給下一卷。
