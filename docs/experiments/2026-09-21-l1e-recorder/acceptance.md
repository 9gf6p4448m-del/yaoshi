# L1e 曾持有記錄器：執行前契約

基準 d2250a8。承接 protocol 的 ever-held 契約，本卷只做開發量測觀測，不調遊戲數值／玩法／UI，不啟動正式平衡樣本。

- opt-in headless 記錄；預設不啟用，不新增玩家可見資訊。
- 每局、每席、每組記錄首次成套 round、phase、mutationSequence；已記錄不因失去、死亡或重得而清除，局與局隔離。
- 只觀測真實玩家袋子的實際變更，不觀測預覽、試算或AI估值副本；觀測不消耗亂數。
- 涵蓋所有實際袋子增刪／移轉入口；同夜成套後失去仍記錄，多副本不重複，對手也記錄。不得僅夜末掃描或借用UI演出旗標。
- 實際取得後立即觀測；給phase與全局單調mutationSequence，記錄器不能影響原結算先後。
- 回歸含同夜成套後失去、失而復得、多席、多副本、持有後死亡、空持有、預覽不記、局間清空；每種真實mutation接點須有盤點與對應驗證。
- RED→GREEN、新增核心函式覆蓋至少80%、語意突變可驗紅；既有相關chain/hook/event測試通過。
- 基準與新預設 trace seeds1..20逐位元組相同；新記錄器off/on的playPolicyGame結果去除純記錄欄位後逐位元組相同，並核對下一個玩法RNG值。既有探索1400局不重跑。
- 獨立架構與JS覆審無未解HIGH/MEDIUM。即使本卷通過，formalStatus仍incomplete，H9正式桌、千眼情報策略、六之四與萬局尚未完成。
