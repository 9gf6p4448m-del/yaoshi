# 千眼私函關閉鈕＋「千眼多看」徽章：凍結驗收（2026-09-25）

**依據**：[千眼知情補充報告](../2026-09-24-eyes-informed-supplement/report.md)第 37、44、45 行（手機私函關閉鈕低於初始視窗；四件預告沒標出額外第四件）。使用者 2026-09-25 裁定照建議做（冷讀細節未補，照「拿不準就直接加徽章」）。**純介面，不改規則或數值。**

## 驗收（跑前凍結，跑後不改）

- **A1 關閉鈕首屏可見**：真千眼已覺醒的本席在 844×390 與 1280×720 開袋子（`showBag`），不捲動時「關閉」鈕的 `getBoundingClientRect()` 完整落在視窗內（top ≥ 0、bottom ≤ innerHeight），且視窗中心點 `elementFromPoint` 取到的是該按鈕。**須先在改動前（`f76ad39`）於 844×390 量到不符合（紅），改動後兩尺寸皆符合（綠）**；若改動前就綠，代表量測沒重現問題，停手回報。
- **A2 徽章只標第四件**：本席真千眼已覺醒、預告 4 件時，出價畫面的預告列恰有 1 個「千眼多看」，緊接在第 4 件名稱之後；普通千眼（3 件、未覺醒）與沒有千眼的席位為 0 個。
- **A3 不遮主鈕**：844×390 出價畫面，主鈕中心點 `elementFromPoint` 取到主鈕本身。
- **A4 規則不變**：`tests/tools/trace-eq.mjs` 以 `f76ad39` 為基準，逐局相等。
- **A5 回歸**：`node --test tests/*.test.mjs` 全數通過（不得改既有斷言來遷就）。
- 探針用練習場「千眼天命（冷讀）」情境走真實開標覺醒路徑，不直接改 `destinyAwakened`。
