# 普通版雙虎買路錢加稅候選：試跑凍結規則（2026-09-24）

**性質**：探索試跑，只用來挑候選；不是正式驗收，不改磁碟上的 `index.html`、不改 H9／H1 判準、不覆寫任何既有 raw。

## 使用者裁定（2026-09-24）

- 依[真版改善](../2026-09-24-true-boost/acceptance.md)的使用者順序，長明、神王之後做「雙虎（普通買路錢）」。
- [第一輪部隊加強](../2026-09-24-h9-boost/report.md)對 H9 無效；當時刻意不加買路錢以免對手避標。本輪改動買路錢，避標風險**只記錄、不作挑選條件**（使用者裁定）。
- 候選值 S＝+2、M＝+3；判準＝普通 H9＋H1（與血祭那輪同口徑）；真版雙虎焦點不在本輪範圍。

## 候選（只在試跑記憶體內替換，每個替換須恰好命中 1 處）

| 候選 | 替換 | 現值 → 候選值 |
|---|---|---|
| toll-S | `index.html:3438` `bidFee()` 盯上稅 | `(tigerMarked?1:0)` → `(tigerMarked?2:0)` |
| toll-M | 同上 | `(tigerMarked?1:0)` → `(tigerMarked?3:0)` |

說明文字（`index.html:2048`「買路錢 +1」）在試跑中不改；選出的候選進正式驗收時一併改。

## 臂與種子

- 種子 `30001..40000`（與正式 `10001..20000` 不重疊、與第一輪試跑相同），每臂 10,000 局，全部完成才分析。
- base（現行來源，含 `0592512` 血祭與 `1f59b10` 真版改動）：`h9-zero-twinTiger`（候選共用；第一輪的 zero raw 來自改動前來源，不沿用）、`h9-normal`、`h1-splitter`、`h1-twinTiger`（後三者只作對照記錄）。
- toll-S、toll-M：`h9-normal`、`h1-splitter`、`h1-twinTiger`。
- 策略、AI、`playPolicyGame` 選項與第一輪 [pilot.mjs](../2026-09-24-h9-boost/pilot.mjs) 相同。

## 指標（同第一輪 analyze.mjs）

- H9：`h9-normal` 與 `h9-zero-twinTiger` 的持有者條件勝率差（pp），需 `+3 ≤ 差 ≤ +10` 且 normal 絕對率 ≤ 85%。
- H1：`h1-twinTiger` 與 `h1-splitter` 座位 0 勝率差（pp），需 `−8 ≤ 差 ≤ +5`。

## 挑選規則（跑前凍結，跑後不改）

1. S／M 中找 H9 與 H1 都符合的候選；兩者都符合選 S。
2. S／M 都不符合 → 停手回報使用者，不自行加大幅度或換旋鈕。
3. 選出的候選才進入正式驗收：改 `index.html`（含說明文字）、以正式種子 `10001..20000` 重跑，判準照原口徑。

## 只記錄、不作挑選條件

- 各臂持有局數、H9 點估計的 95% 配對 bootstrap 區間（2,000 次，同第一輪）。
- **避標**：每夜拍賣結算開頭（AI 出價已產生、夜規前置處理前），對每位存活且有 `markTax` 的持有人所盯拍品，記「存活對手數」與「其中對該件出價的對手數」；報告各臂的對手出價率（出價數／機會數），base 與候選並列。只讀記錄，不耗亂數、不改結算。

## 可信度核對（跑前）

- 包裝腳本用 base 在 60 個種子上跑四種臂，勝者與雙虎持有人須與第一輪 [pilot.mjs](../2026-09-24-h9-boost/pilot.mjs) 的 base 逐局相同（證明記錄注入不改對局）。
- toll-M 須在少量種子內至少改變 1 局勝者或避標計數（證明替換真的生效）。
- toll-S／toll-M 的 `h9-zero-twinTiger` 在 60 局中須與 base 完全相同（替換不影響 zero 臂，故可共用）。
