# 驗收凍結：招式短版盲讀（0.54 F5 人眼段的機械化，2026-09-11）

> 使用者 2026-09-11 提議「派 fresh agent 對招式顯示是否明顯評分、修到通過（5 分量表 ≥4）」；主對話加三條：讀者與修者分開、完整版當對照組、上限 3 輪。本檔訂下後即凍結（`02 §2.1`）。**只量「看得出是哪招、看得清楚」，不量美感**（`03 R6` 品味題最後由使用者一眼裁）。

## 材料（每輪重產，程序固定）
- 從 `traitfx-drive` 的 contact sheet（`sheet-t1-short.png` 短版 260ms／`sheet-t2-full.png` 完整版 900ms，各 27 列 × 出招後 20%／45%／75% 三格）切成 54 條，**去掉左側招名欄、遮掉每格頂端的代號列、放大 2×**，短版與完整版**混洗成匿名編號** `strip-01..54`；對應表 `r{n}-mapping-HIDDEN.json` 讀者不得看。切圖腳本：`scratchpad/blindread`（程序寫死：seed 20260911、遮頂 16px、2×）。
- 讀者拿到：54 條圖＋27 個招名（打亂）＋「這是深夜妖怪市集紙紮夜戰的招式演出；左邊是出招方」一句背景。**不給辨識元素表、不給短版／完整版標記、不給對話史。**

## 量表（每條圖兩題）
- **Q1 辨識**：從 27 個招名選一個（可答「認不出」）。對＝1、錯或認不出＝0。
- **Q2 明顯度 1–5**：1 看不到動作／2 有動靜但不知道在做什麼／3 看得出攻擊或防禦但不知是哪招／4 看得出招式特色／5 一眼就是這招。**必附一句「看到了什麼」**（防丟骰子）。

## 及格線（兩位 context-free 讀者，opus，各自獨立）
- 每支短版：**兩位讀者 Q1 皆對、Q2 皆 ≥4、且 Q2 不比同一讀者對該招完整版的評分低超過 1**。
- 對照組規則：完整版本身兩位讀者 Q1 皆錯或 Q2 皆 ≤3 的招 → 記「完整版本來就不可辨」，短版不因此判紅，另列清單給使用者（那是 C3 招式卷的既有問題）。
- 全體：27 支中 ≥24 支過（≥89%）才算本段通過；未過的每支列名。

## 迴圈
- 第 n 輪未過清單 → 交**修者**（0.54 作者 agent，讀者不得修），只准改該招短版時間軸（`js/trait-fx/*.js` 的 SHORT 段），不得改切圖程序、不得改量表、不得改 F2 治具。改完重跑 F2（27/27 clean、rate≤1.0）→ 重產 sheet → 重切 → **兩位新的 fresh 讀者**（不重用前輪讀者）。
- **上限 3 輪**：第 3 輪仍有未過的就停手，列清單交使用者裁（記 §2.1 或簽字上線）。
- 什麼實作會讓它假綠：切圖挑最好看的幀（幀位寫死 20/45/75%）；讀者拿到辨識元素表（禁）；讀者知道哪些是短版（混洗）；修者改治具或量表（禁）；只用一位讀者（要兩位）。

## §2.1 修訂紀錄
（無）
