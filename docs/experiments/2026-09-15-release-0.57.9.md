# v0.57.9 發布候選與手機驗收入口

狀態：已推送並驗證正式站，commit `48374527f584e9266fe89031bda5531feaecd70b`（2026-09-15）。

使用者已明確授權本次推送，並要求後續驗證完成的試玩更新直接發布，不必逐次詢問；覆蓋前次不得自行 push 的限制。

正式站驗證：遠端 main SHA 相符；HTTP 200 的 index.html、安全區 CSS、table-props／camera-director／duel-figures 五檔內容與本機一致（僅正規化換行）。Chromium 公開頁實載顯示 v0.57.9、3D 入口完成、三個模組 query 均為 v0.57.9、page errors 0。已檢視首頁截圖。證據存 `scratchpad/release-0.57.9/http-final.json`、`browser-final.json`、`public-home-final.png`。這是桌機瀏覽器送達檢查，仍不代表手機實測通過。

## 發布內容

- 三種高光轉場與存活判斷修正。
- B 中型盯牌、縮放污染修正、同槽分列與跨槽排距。
- Tier 1 每拍一次 300ms 短推鏡；關閉近鏡／減少動態時不推。
- 通用安全區與主畫面「？」內的即時效能開關。
- VERSION 與 RELEASE_VERSION 同步 0.57.9，修正舊主畫面更新探針仍停在 0.56e 的問題；摘要與手冊同步。

## 驗證

- 新更新測試修正前 2/2 失敗：雙版本不同、舊主畫面沒有新版提示。修正後 2/2 通過，包含同版與降版不提示。
- `node --test tests/*.test.mjs`：39/39 通過，0 failed。
- `git diff --check`：通過，僅 LF/CRLF 提示。
- 先前本輪視覺驗證：256 種盯牌分配、6 組 18 幀滿編短推、25 項模擬版面檢查；見對應 evidence 目錄。版本字串修正未改幾何與鏡頭。
- 手機實體 Safari／主畫面生命週期尚未驗證，不能以桌機 Chromium 取代。

## 發布後的手機步驟

1. 從原主畫面圖示開啟，等待更新提示並點它；首頁確認 **v0.57.9**。若沒有提示，先回背景再切回，仍不更新才從 Safari 開正式頁的 `?v=0.57.9` 入口確認。
2. 以橫向遊玩，觀察左右盯牌、底部主鈕與出價抽屜；側欄下方拍品可捲動點選。
3. 在「？」開啟效能資訊，確認顯示「主畫面模式」；完成對決後記錄上一場最低 fps、當時部隊隻數及體感卡頓。
4. 橫直轉向後回到遊戲，切背景再回來，檢查畫面、音訊與當局狀態。

這是裝置觀察清單，沒有自行新增 fps 通過線。原 P4 三對 fresh 讀者、至少 2v2 的效果／對象／系別盲讀門檻與已知失敗紀錄仍保留。

## 保留事項

有應公 M-A1 未過、滿編遮擋、公開揭盅飛行模型碰頂仍未解。連鎖／幽靈 PVP 等 D2／D3。發布這份候選不代表上述項目通過。
