# 驗收凍結：招式三級視覺分級（ROADMAP_V2 Top 2 第一步，2026-09-10，目標版號 v0.54）

> 基準＝`main` `4b7dadd`（v0.53）。依據 `docs/proposals/2026-09-10-roadmap-v2-review.md` §1（技術可行性）、§15（純演出卷驗法）。使用者 2026-09-10 裁定 **D4 丙**：不走通用短招，**27 支專屬招各自寫一條 Tier 1 短版時間軸（260ms）**，完整版留 Tier 2，三尊大招 Tier 3 走新機位＋黑邊。使用者原話：「想做遊戲大作、不想做出垃圾遊戲、野心大一點、看 Fable 5.1 極限」——模糊處一律往「更有大作感、個性不丟」取捨，不往「省事」取捨。本檔訂下後即凍結（`02 §2.1`），要改只有「原標準錯在哪、為什麼現在才知道」＋使用者逐條同意一條路。

## 範圍
- **純演出卷**：對決引擎（`playDuelWar`／`war.beats`）、`TRAITS` 規則欄位、AI、拍賣、請神、共鳴一格不動；`TRAITS[]` 只**新增** `tier` 欄位。
- **分母先歸一（`02 §6.1` 第 7 條）**：評審數出 20 處 `900`（runtime 4：`index.html:3976` `PW_FX.TRAIT_MS`、`js/trait-fx.js:366` `Number(det.ms)||900`、`js/camera-director.js:74` `LEAN.ms` 與 `:18` table shot；治具 16：`traitfx-drive.mjs:54,111-114`、`cam-drive.mjs:62,90`、`cam-unit.mjs` ×12、`closeup-cam-unit.mjs:78`、`closeup-judge.mjs:116`）。動手前 agent 自己再 grep 一次寫下分母 N（不得憑評審數字），runtime 收成 **`PW_FX.TRAIT_MS_BY_TIER` 單一來源**，治具改讀共用 `tests/tools/fx-consts.mjs`（其值再對頁面常數斷言）。事件一律帶 `detail.ms`，`trait-fx.js` 與 `camera-director.js` 的 `||900` 退路**刪掉，沒帶就 throw**。
- **三級定義（拍級，不是招級）**：`pwBeatTier(beat, war)`＝ `LEGENDS` 三招 → 3；該拍**擊殺**（目標 hp→0）或該場對決的**決定性最後一拍** → 2；其餘 → 1。`TRAITS[].tier` 是該招的**上限**（預設 1 的招也能因擊殺升 2；`LEGENDS` 三招 3）。數字表 `PW_FX.TRAIT_MS_BY_TIER={1:260, 2:900, 3:1400}`。
- **Tier 1 短版**：27 支（`js/trait-fx/{zuling,xianghuo,yinqi}.js` 各 9）每支新增一條 260ms 時間軸（同檔同函式加 `det.tier===1` 分支或 `short` 變體，介面由 agent 在計畫檔第 2 節寫死），**必須保留該招的辨識元素**（eliteBlind 的餘暉、hauntAnswer 的回應等；辨識元素清單 agent 從現有 27 支註解／視覺各抄一句進計畫檔，短版對照表交人眼）。短版**原生塞進 260ms**：`rate` 不得 >1.0（不靠縮放硬擠），`flinchMs`／`atReserve`／`endMargin` 三常數改成**隨 `det.ms/900` 等比**（`trait-fx.js` 一處改）。
- **Tier 2**：現有完整版原樣（900ms）。**Tier 3**：三尊現有三招函式＋`camera-director.js` 新 `CINEMA` 機位（低角度仰視、dist 拉近）＋兩條 DOM 黑條 letterbox（與 `#vignette` 同層、不進 shader）＋1400ms；只在 tier 3 出現。
- **拍末等待**：走 3D 舞台時等 `TRAIT_MS_BY_TIER[tier]`（取代單一 `TRAIT_MS`）；`BEAT_MIN_MS` 900→依 tier（tier 1 ≈ `EV_MAX_MS` 預算內並行，取 260～300；確切值 agent 計畫檔寫死）。`closeup-judge.mjs:116` 分析視窗改**讀拍長**，「靜幀 ≥8 幀不足回 null」那條要先改成依拍長換算幀數，否則 tier 1 會靜默 null（恆綠）。
- **kill switch**：`?fxtier=0` → 所有拍走 tier 2（＝v0.53 行為：900、無 CINEMA、無黑條）；預設開。
- **不動**：`ART_BIBLE`、`GAME_DESIGN`（純演出）、GLB 資產、`bloom.js`、對決引擎、所有規則測試的斷言。

## 驗收條件（動手前訂；門檻不得為了過而調）
- **F0 等價雙向**：`trace-eq` 對基準 `4b7dadd` `trace(1..20)` 逐位元組相等（預設與 `?fxtier=0` 都相等，純演出）；**突變驗紅**：把任一規則常數改壞（改壞前先備份副本，用副本還原）→ 不相等。`tests/duel-desync.test.mjs`／`lineup-order.test.mjs` 與其餘 9 套全綠、零毫秒斷言新增。
- **F1 分母歸一**：agent 動手前寫下 N（grep 指令原文＋清單）；改後 runtime（`index.html`、`js/`）語意為招式時長的 `900` 字面值＝**0 處**（`font-weight:900`、開服 `setTimeout(r,900)` 不算，清單要列出排除的每一處）；治具 16 處全改讀 `fx-consts.mjs`；單元測試：`fx-consts.mjs` 與頁面 `PW_FX.TRAIT_MS_BY_TIER` 不一致→紅；事件缺 `detail.ms` → throw（測試對基準紅在行為斷言）。
- **F2 27 短版原生合身**：`traitfx-drive --tier=1` 30 套（27＋三尊 tier 3）`onTime` 全過**且 `clean`（`cut===0 && fused===0`）全過、`rate ≤1.0`**（短版不得靠加速擠進去）；`--tier=2` 27 套與基準同結果（回歸）；`--tier=3` 三尊 1400ms clean、CINEMA 觸發、黑條 DOM 存在。治具讀 `fx-consts.mjs` 不得寫死。什麼實作會讓它假綠：短版只是把完整版 `rate` 拉到 3.46×（`rate` 上限 1.0 擋）；把 `clean` 判準放寬。
- **F3 節奏**：`duel-drive` 4 場（seeds 固定）`duelsMs` 中位 **≤5 s**（8v8 基礎戰），有 tier 3 出場的對決 ≤8 s；基準 `4b7dadd` 同 seeds 數字一併印出（預期 5–10 s）。
- **F4 可讀性不退**：`dmg-readability` seeds 1/3 R1（字級 ≥1.6×）／R2（方案 A：中位 ≥+25 且 ≥25 比例 ≥70%＋Δ200 單向 ≤+5）**維持綠**，門檻一字不動；`closeup-judge` P 系列在 tier 1 拍**不得回 null**（治具讀拍長；null 數印出、必須 0）。
- **F5 三級可辨（人眼＋機械）**：機械——黑條僅 tier 3 期間 `visible`（tier 1／2 期間 0 次，Playwright 斷言）；CINEMA 僅 tier 3。人眼——844×390 contact sheet：①27 支短版中段幀各一格（與完整版中段幀並排、標招名與辨識元素）②tier 2 擊殺拍 3 格 ③tier 3 三尊大招各 3 幀（含黑條）。**交使用者挑**：使用者點名「看不出是哪一招」的短版回頭改，最多兩輪。
- **F6 fps**：桌機 `?fps=1` 對決期間 fps 比值（新／基準，同 seeds）**≥0.90**、draw calls 不增（黑條是 DOM）；iPhone `?fps=1` 由使用者回填（記錄項，不擋合併）。
- **F7 Playwright 零錯**：`duel-drive` 4 場＋`traitfx-drive` 全套 0 console error／pageerror／requestfailed；`?fxtier=0` 4 場同樣 0 error。
- **F8 文件**：`IMPLEMENTATION_GUIDE.md` 新 §11.27（三級定義、`TRAIT_MS_BY_TIER` 單一來源、`fx-consts.mjs`、「`||900` 退路已刪、事件必帶 ms」、`?fxtier=0`）；`VERSION="0.54"`＋`VERSION_NOTE` 首段寫本卷；`ART_BIBLE`／`GAME_DESIGN` diff 為空。
- **F9 範圍**：`git diff --stat 4b7dadd..` 只含計畫檔第 1 節列出的檔；`TRAITS` 的 diff 只有 `tier` 欄位；引擎函式 diff 為空（以 diff 證明）。
- **F10 短版品質下限（機械）**：每支短版時間軸至少含 2 個以上非 flinch 的 tween／fly／fade／grow（防「只剩一個閃光」的偷懶短版）；由 `traitfx-drive --tier=1` 統計並印表。

什麼實作會讓 F2 假綠：`--tier=1` 其實還在跑 900 的完整版（治具要斷言 `run.ms===260`）。什麼實作會讓 F3 假綠：把拍與拍之間的等待砍到 0 讓人看不清（F4 R1/R2 與 F5 人眼擋）。什麼實作會讓 F0 假綠：trace 沒把 `war.beats` 的 kind/side/trId 全部序列化（trace-eq 現有欄位不得縮）。

## §2.1 修訂紀錄

### 修訂一（三級定義變更，**不是門檻變更**）——使用者 2026-09-10 裁甲
**變了什麼**：`pwBeatTier` 的 tier 2 條件從「該拍有擊殺（目標 hp→0）**或**該場決定性最後一拍」
收窄成「①該場決定性最後一拍 ②這一拍燒掉了傳說三尊之一」兩種；**一般的擊殺拍改走 tier 1**
（260ms 短版＋300ms 拍末下限）。tier 3 的條件（該拍有 `TRAITS[].tier===3` 的招）不變。
**F3 的門檻 5 秒一字沒動**，動的是三級定義本身。

**為什麼**（一版實測的歸因）：一版 4 場 12 拍的分布是 `FXC.tiers = {1:4, 2:8, 3:0}`——
8v8 基礎戰三分之二的拍都有紙紮被燒，於是「該拍有擊殺→tier 2」把拍末下限大多留在 900ms，
招式本身一局又只出現 4 次（`FXC.trait=4`，900→260 總共只省 2560ms）。
F3 中位因此只從基準的 5573.5ms 降到 5302ms，離門檻 5000 還差 302ms。
收窄之後 `tiers` 翻轉成 `{1:8, 2:4, 3:0}`，一般擊殺拍回到短版。

**改前改後（`duel-drive` 4 場，seed=7，同一組 seeds）**：

| 版本 | 4 場 duelsMs | 中位 | `FXC.tiers` |
|---|---|---|---|
| 基準 `adbb124`（v0.53） | `[5222, 5925, 4697, 8634]` | 5573.5ms | —（沒有分級） |
| 一版（擊殺拍即 tier 2） | `[4642, 5889, 4715, 8716]` | 5302.0ms | `{1:4, 2:8, 3:0}` |
| **二版（本修訂）** | `[4916, 5355, 4756, 7590]` | **5135.5ms** | `{1:8, 2:4, 3:0}` |

比基準快 438ms（−7.9%），比一版再快 166.5ms。**仍未達 ≤5000 的門檻**（差 135.5ms），F3 續紅——
剩下的時間大宗已經不是拍末下限，而是逐筆事件（`EV_BUDGET_MS 1050`）、拍首字幕（`BANNER_MS 360`）、
進退場（`ENTER_MS 480`＋`END_MS 1000`），那些常數在本卷的「不做什麼」裡明列不動。

**誰同意**：使用者 2026-09-10 裁甲（主對話轉達）。本修訂只改「哪些拍算重要」，不放寬任何數字門檻。

### 修訂二（F4 口徑修訂）——使用者 2026-09-10 明確同意
**原標準錯在哪**：F4 寫「`closeup-judge` P 系列在 tier 1 拍**不得回 null**（null 數印出、必須 0）」，
但基準 `adbb124`（v0.53、沒有任何 tier 分級）跑同一組 seeds **本來就有 2 筆 null**
（`rows 6/8`，`quiet=0`）；`dmg-readability` seed 3 的 R2 在基準上同樣是空過（`maskN=0`、
`maskDropped {moved:3, tinyMask:2}` 逐項相同）。
成因是**命中拍每 45–260ms 一次 punch 把靜幀排光**（`closeup-judge` 檔頭 v0.45 就記過），
與 tier、與分析視窗長度都無關。也就是說「必須 0」這條**無論實作對錯都不可能通過**＝恆假，
不是驗收條件（`02 §2.1` 例外條的判準：這個修正不會讓一份壞掉的實作變成通過）。

**為什麼現在才知道**：v0.53 的 `closeup-judge` 把 `monoQuietOk===null` 當「不算過」但也不計數，
`PASS` 只看有樣本的那幾筆（`mq.every(...)`）——null 被靜默吞掉。
本卷第一次把它升成明確紅並印出 `nullCount`，才把既有缺陷照出來。

**新口徑**：
- `closeup-judge`：**`nullCount` ≤ 基準同 seeds 的 `nullCount`（＝2）**，且非 null 樣本的 `deepOk`／`monoQuiet` 全過。
  治具的旗標是 `--basecj=<對基準樹跑出來的 cu-*.json>`（**不是自由數字**，R2 覆審 H6）：
  由同一支判官對那份基準產物跑一次、取它的 `nullCount` 當上限；沒帶就是 0（最嚴）。跑法寫在治具檔頭。
- `dmg-readability`：**每個 seed 的空過數 ≤ 基準**（seed 3 ＝1，seed 1 ＝0），有樣本的 seed 其 R1／R2 維持綠。

**改前改後**：

| 量 | 基準 `adbb124` | 一版 | 二版 |
|---|---|---|---|
| `closeup-judge` nullCount | 2（rows 6/8，`quiet=0`） | 2（同兩筆） | 2（同兩筆） |
| 非 null 樣本 `monoQuiet` | 8/8 | 8/8 | 8/8 |
| `deepOk` | 10/10 | 10/10 | 10/10 |
| `dmg-readability` seed 1 | R1 PASS／R2 PASS | R1 PASS／R2 PASS | R1 PASS／R2 PASS |
| `dmg-readability` seed 3 | R1 PASS／R2 空過（`maskN=0`） | 同左 | 同左 |

**成因不在本卷修**：punch 排光靜幀是量測治具的問題（要嘛改量測位置、要嘛在 punch 期間另找對照幀），
另開治具小卷處理。本卷只把口徑改成「不比基準差」，不碰 `dmg-readability` 的 R1／R2 門檻一個字。

### 修訂三（計畫檔第 1 節補列：`js/renderer.js` ＋ 8 支治具 ＋ 凍結檔本身）——使用者 2026-09-11 認可
**★R2 覆審 N2★**：一版的修訂三文字只寫了 `js/renderer.js` 與 `?fps=1` 那一段，
但最後一個 commit 又往計畫檔加了 9 列並全部標成「修訂三補列」——**修訂三並不涵蓋它們**，
而 F9 的判準是「diff 只含計畫檔第 1 節列出的檔」＝自我指涉，改計畫檔就能讓 F9 變綠。
現在把時序與理由完整寫下來，逐檔一句：

| 檔 | 為什麼一定會動 |
|---|---|
| `js/renderer.js` | +1 行：把既有的 `director` 掛上 `window.__yaoshi3d`。F5 的探針要讀 `director.cinemaOn()` 才驗得了「CINEMA 只在 tier 3」；渲染路徑一格未動（覆審員逐 token 核過只多 `director,`） |
| `tests/tools/fx-consts.mjs`（新） | **F1 明文要求**「治具改讀共用 `tests/tools/fx-consts.mjs`」——這是驗收條文直接指定的新檔 |
| `tests/tools/lbox-probe.mjs`（新） | **F5 機械段明文要求**「Playwright 斷言」黑條與 CINEMA 只在 tier 3；既有治具沒有一支能驗這件事 |
| `tests/tools/traitfx-sheet.mjs` | 幀位改成依 tier 換算之後，檔名不再是固定的 `-8/-22/-36`；不改這支，F5 的 contact sheet 會抓不到圖（一版就抓空過） |
| `tests/tools/duel-drive.mjs` | R1 覆審 H3：F5 的「tier 1／2 期間黑條 0 次」要在**真實對局**上驗，只能在這支掛 MutationObserver；順帶補 `url`（含 seed）落檔（R1 M6） |
| `tests/tools/duel-perf.mjs` | R1 覆審 H1：`perf` 模式**靜默吃掉 `--root`**，F6 的「基準」因此是新版跑的。不修這支，F6 無法量在真基準上 |
| `tests/tools/trace-eq.mjs` | R1 覆審 H7：凍結檔自列的 F0 假綠條件（`trace` 不含 `war.beats`）本來就成立，加 `--beats` 才補得起來 |
| `tests/tools/pace-ab.mjs`（新） | R1 覆審 H5：F3 的一次量測噪音大於效果，`02 §6.2` 要求先歸因；這支是「交錯跑、多次取中位、印全距」的量測驅動器 |
| `tests/tools/t3-shot.mjs`（新） | R1 覆審 H4：F5 人眼段的 tier 3 交付物必須含黑條與 CINEMA，而 `traitfx-preview.html` 那條路**不可能**拍到 |
| `tests/tools/fpsdiag-probe.mjs`（新） | 使用者追加的「`?fps=1` 對決最低 fps」欄位要有斷言（只斷言「文字含某字串」是零鑑別力的，這支把數字解出來比大小） |
| `docs/experiments/2026-09-10-acceptance-fx-tiers.md` | 凍結檔本身：§2.1 的修訂紀錄（正文門檻一字未動，`git diff` 對凍結檔**零刪除行**，覆審員逐字核過） |

**共同點**：這 11 個條目沒有一個碰渲染或引擎，全部是「為了讓某一條驗收真的驗得到東西」而生。
**誰同意**：使用者 2026-09-11 認可（主對話轉達：「F1 要求治具改讀 `fx-consts.mjs` 的必然產物，原計畫漏列」）。
**這一條會讓 F9 由紅轉綠**，所以照 `02 §2.1` 走使用者明確同意，不由作者自行認定。

### 修訂四（F3 主條：以 **5018ms** 通過）——使用者 2026-09-11 裁甲
**F3 主條的門檻文字一字不改**（仍是「`duelsMs` 中位 ≤5 s」），這一條只記「**以 5018ms 通過**」
——★數字以 repo 現況（`docs/experiments/2026-09-10-fx-tiers-evidence/pace-ab.json`）為準★，
歷史值（一版 5302／二版 5032.5／三版 5009）保留在下表當歷程，不再出現在標題或結論句（R4 M-1）。

**原標準錯在哪**：5000 這條線**比量法本身的展幅還細**。二版換成「交錯 A/B、每組 5 次、取中位的中位」之後，
同一組設定的 5 次中位展幅是 **58.5ms（作者）／290ms（覆審員 r2）**，而受測值離門檻只有 32.5ms／50ms——
也就是說這個量法量得出「快了多少」（−526／−524.5，兩人獨立複現到 ±1.5ms），**量不出「過不過 5000」**：
換一天跑很可能是綠的（作者 5 次裡有 2 次落在 5000 以下，覆審員最低 4996）。
一個信賴度低於自身量測的門檻，不構成有效的驗收條件。

**為什麼現在才知道**：一版只印「一次 4 場的中位」（5302），沒有任何重複性數字；
是二版照 `02 §6.2` 換成交錯 5 次的量法之後，**才第一次看得到展幅**。

**改前改後**：

| 版本／來源 | 中位（的中位） | 全距 | 對基準的效果 |
|---|---|---|---|
| 基準 `adbb124`（作者量） | 5558.5ms | [5540.5, 5676]，展幅 135.5 | — |
| 基準 `adbb124`（覆審員 r2 獨立量） | 5574.5ms | [5533.5, 5743]，展幅 209.5 | — |
| 一版（一次 4 場中位） | 5302ms | 無重複量測 | −271.5ms |
| **二版（作者）** | **5032.5ms** | [4993, 5051.5]，展幅 58.5 | **−526ms**，`overlap:false` |
| 二版（覆審員 r2 獨立量） | 5050ms | [4996, 5286]，展幅 290 | −524.5ms，`overlap:false` |
| 三版 | 5009ms | [4993.5, 5047]，展幅 53.5 | −512.5ms（基準 5521.5ms），`overlap:false` |
| **repo 現況（四版＝五版＝收尾版，`pace-ab.json`）** | **5018ms** | **[4996, 5155.5]，展幅 159.5** | **−586ms**（基準 5604ms，展幅 724），`overlap:false` |

（上表「二版（覆審員 r2 獨立量）」只有一列；r5 抓到它被重複貼了兩次，已刪掉重複的那一列。）

四次獨立量測的效果同向且量級一致（**−512.5／−524.5／−526／−586**），
四組全距都不重疊（`overlap:false`）⇒ **「v0.54 比基準快」這件事已經在噪音之外**。
效果幅度本身會隨當次基準值浮動（基準 5521.5～5604），所以結論句寫的是「同向、不重疊」，
不是「效果恆等於某個數」。

**誰同意**：使用者 2026-09-11 裁甲。
**這一條不放寬任何數字**：門檻 5000 沒動、量法沒有變鬆（一版用同一個門檻也是紅，二版量得更準之後仍是 5032.5），
記的是「以這個實測值通過」。要把它變回可機械判定的閘門，得換成決定性的量（例如把 `pwSleep` 排定的總毫秒加總），
那是下一卷的事。

### 修訂五（F3 子條：「合計 ≤ 基準合計 −10%」**且逐場不得比基準同場次慢**）——使用者 2026-09-11 裁甲＋R4 加嚴
**原文**：「有 tier 3 出場的對決 ≤8 s」。**二版曾改成「逐場 ≤ 基準同場次 −10%」，本輪再改為「合計 −10%」。**

**原標準錯在哪**（兩層）：
1. 「≤8 s」量到的不是 tier 3，是「第幾場」——基準 `adbb124`（完全沒有分級、招式全 900ms）在同一組 seed
   的同一場次是 16087／14728／7096ms，**基準自己也是 3 場中 2 場超標**；tier 3 那一拍只佔 12.6 秒的 11%。
2. 「**逐場** −10%」對招數少的短場**數學上不可達**。設 k ＝落在 tier 1 拍的招數，
   則 Δ ≈ −100（拍末：300+900+1400 ＝ 2600 vs 基準 3×900 ＝ 2700）＋500（tier 3 的 1400 比 900 多花）−640k（每支 900→260）。
   代進三場：#10 Δ=−3425 → k=5.98；#12 Δ=−1535 → k=3.02；#13 Δ=−241 → k=1.00——**三場都落在整數上，模型成立**
   （覆審員 r3 獨立重推同一個模型，逐項吻合）。#13 要達 −10%（709.6ms）需 `−100+500−640k ≤ −709.6` ⇒ **k ≥ 1.73 ⇒ k ≥ 2**，
   而它只有 3 支招、其中 1 支落在 tier 1 拍 ⇒ **無論實作多好都過不了**。
   逐場判準因此對「短場」是恆假的一半，不是驗收條件。

**為什麼現在才知道**：一版根本沒有 tier 3 樣本（4 場對決三尊沒上場）；二版跑到 14 場才第一次拿到三場、
才做得了與基準同場次的對照；**第三場攤開之後**才看出逐場判準與招數綁死。

**改前改後**（`seed=7`、14 場，三場都是 `tiers Δ={1:1, 2:1, 3:1}`、3 拍）：

| 場次 | 基準 | v0.54 | 逐場 Δ | 招式數（新／基準） |
|---|---|---|---|---|
| #10 | 16087ms | 12662ms | −21.3% | 8／8 |
| #12 | 14728ms | 13193ms | −10.4% | 8／8 |
| #13 | 7096ms | 6855ms | −3.4% | **3／3** |
| **合計** | **37911ms** | **32710ms** | **−13.7%** ✅ | — |

**★R4 覆審 M-2 的加嚴（自行記錄，不放寬故不需再同意）★**：只看合計會留下一個洞——
合計門檻是 37911×0.9 ＝ 34119.9ms，現值 32710 ⇒ 餘裕 1409.9ms，
**一份只把短場弄壞的實作（#13 從 6855 退步到 8264、比基準 7096 還慢 16.5%）合計照樣綠**。
所以判準補上第二個條件：**逐場都不得比基準同場次慢**（Δ ≤ 0）。
這是加嚴（把通過機率壓低），依 `02 §2.1` 自行記錄即可。

**新判準下：綠**——合計 −13.7% ≤ −10% ✅ **且** 逐場 −21.3%／−10.4%／−3.4% **三場都比基準快** ✅。
三場逐場的數字全部保留在報告裡，不因為改判準而消失。

**誰同意**：使用者 2026-09-11 裁甲。
**已知的殘留（下一卷要處理）**：合計判準**同樣量不到 tier 3**——它量的是「這一批場次有幾支招被縮短」，
tier 3 的 +500ms 只是被動減分。原條件的用意（防「大招把對決拖長」）在新判準底下仍然沒有被量到。

### 修訂六（F4：`closeup-judge` 的判準改寫）——使用者 2026-09-11 裁甲
**新判準**：`closeup-judge` 判「**有樣本的部分 `deepOk`／`monoQuiet` 全過**」；
`nullCount` **降為記錄項**（新版 4／基準 2），成因交治具小卷。

**原標準錯在哪**：`nullCount ≤ 基準` 量的是**量具的樣本數**，不是可讀性。
四個 null 全是 hit 類、`quiet=0`——命中拍每 45–260ms 一次 punch 把靜幀排光；
拍縮短之後同樣次數的 punch 擠在更短的拍裡，可取到的靜幀更少。
逐列比對（覆審員 r2 獨立複現）：**沒有任何一筆從 `true` 變 `false`**，`deepOk` 兩邊都 10/10，
而且基準那兩筆通過時的 `quiet` 只有 **3 和 4**——正好卡在 `need` 地板（3 點才是單調性斷言的最小有效樣本），
所以「8/8 → 6/6」是**兩筆極限邊緣樣本掉成零樣本**，不是可讀性從好變壞。

**為什麼現在才知道**：v0.53 的判官把 `monoQuietOk===null` 當「不算過」但**不計數**，null 被靜默吞掉；
本卷第一次把它升成明確紅並印出 `nullCount`，拍縮短後才讓這個既有量具缺陷浮出來。

**改前改後**：

| | 基準 `adbb124` | v0.54 一版 | v0.54 二版 |
|---|---|---|---|
| n | 10 | 10 | 10 |
| `deepOk` | **10/10** | 10/10 | **10/10** |
| 有樣本的 `monoQuiet` | 8/8 | 8/8 | **6/6** |
| `nullCount`（記錄項） | 2（rows 6/8） | 2 | **4**（rows 0/2/6/8，全 `quiet=0`） |

**誰同意**：使用者 2026-09-11 裁甲。
**成因不在本卷修**：punch 排光靜幀是量測治具的問題（要嘛改量測位置、要嘛在 punch 期間另找對照幀），
另開治具小卷處理。`dmg-readability` 那一半的口徑（修訂二）不變，且基準證據已補齊落檔
（`dmg-base-s1/pix.json`、`dmg-base-s3/pix.json`）。

### 修訂七（**範圍變更**：黑邊 letterbox 移出本卷；F5 機械段改寫）——使用者 2026-09-11 裁甲
**裁定原話**：「本卷拿掉黑邊，保留 CINEMA 仰視機位與 tier 3 1400ms；黑邊連同對決版面安全區留給招式可辨性卷重做。」

**這條蓋掉本檔哪幾句**（逐句點名，免得留下兩套說法）：
- 〈範圍〉「**Tier 3**：……＋兩條 DOM 黑條 letterbox（與 `#vignette` 同層、不進 shader）＋1400ms」→ 刪掉「兩條 DOM 黑條 letterbox（……）＋」，其餘不變。
- 〈範圍〉kill switch「＝v0.53 行為：900、無 CINEMA、無黑條」→「＝v0.53 行為：900、無 CINEMA」。
- 〈F2〉「`--tier=3` 三尊 1400ms clean、CINEMA 觸發、黑條 DOM 存在」→ 刪掉「、黑條 DOM 存在」。
- 〈F5〉機械段「黑條僅 tier 3 期間 `visible`（tier 1／2 期間 0 次，Playwright 斷言）」→ 整條移除，換成下面的新機械段。
- 〈F5〉人眼段「③tier 3 三尊大招各 3 幀（**含黑條**）」→「③tier 3 三尊大招各 3 幀（CINEMA 仰視機位）」。
- 〈F6〉「draw calls 不增（黑條是 DOM）」→ 括號內理由失效，門檻「draw calls 不增」一字不動。
- 首段「三尊大招 Tier 3 走新機位＋黑邊」→「走新機位」。

**原標準錯在哪**：凍結檔假設「上下各壓一條黑邊」是可以獨立於版面加上去的框飾。
**對決版面本來就填滿畫面、沒有任何安全區**——真實對局 `#duel` 的 `scrollHeight 394 > clientHeight 390`，
內容在垂直方向本來就已經溢出。所以任何黑邊都一定壓到元素，五輪補丁只是**把壓到的對象換一個**：

| 版本 | 做法 | 實測結果 |
|---|---|---|
| 一版 `a862bbd` | 黑條 `z-index:-1` | 黑條在 `#duel`（z:40，半透明）底下，上緣帶平均亮度只從 27.27 掉到 17.31（Δ−9.96/255）＝灰紫帶，不是黑條（r1 H2） |
| 二版 `5a1220e` | 黑條 `z-index:41` | 真的黑了，然後蓋掉 tier 3 那一拍的 `#duelBeat` **82.4%**、`#duelSub` **100%**（r2 N1） |
| 三版 `6d10712` | `#duel{padding:8vh 0}` 讓內容內縮 | **完全的 no-op**：`justify-content:center` ＋ 內容溢出 ⇒ 居中對齊的是內容盒中心，上下加等量 padding 之後中心點不變（(31.2+358.8)/2 == (0+390)/2 == 195），幾何逐值與改前相同（r3 H-1） |
| 四版 `cfb5701` | `#duelBeat`／`#duelResult`／`#duelSub` 改 absolute 挪出黑條 | 只救了三分之一，而且**把別的元素推進黑條**：`#duelMove` 被蓋 **15–30%**、`#beatLamps` 被蓋 **75.4%**（r4 H-1／H-2） |
| 五版 `00706e0` | `#duel.lbox{transform:scale(.80);box-shadow:0 0 0 100vmax #000}` 整塊縮 | 黑邊改由「縮放後的 `#duel` 邊界」構成（上下 7.8px、左右 84.4px），`#beatLamps` 仍被 `#duel` 自己的 `overflow:hidden` 切掉 **58.5%**；3D 妖**不跟著縮、只內移**（`js/duel-figures.js:648-649` 的 `figScale` 只讀 `FIG.pixelH`／`FIG.creaturePx` 與相機距離），`wardGuardAll-45.png` 裡一隻妖被下緣切掉；`.mvdesc` 有效字級 11px→**8.8px**；而且**一行把字幕削掉 19.2% 的突變照樣 L1–L8 全綠**（r5 H1／H2） |

五個版本共通的錯：判準都是「黑條有沒有蓋到**我列的那組元素**」，而那組元素是自己列的分母——
每補一個入口就擠到下一個，正是 `02 §6.1` 第 7 條說的「防線按已知入口寫」。

**為什麼現在才知道**：前四輪的證據都是「某幾個元素有沒有被蓋到」，
**r5 才第一次量到區域分母**（縮放後 `#duel` 的實際邊界 rect 與 `overflow:hidden` 的裁切線，
而不是 8vh 那兩條 div），也才第一次跑「削字幕 19.2% 的突變」去測 L1–L8 的鑑別力。
量到分母的那一刻才看得出：要的不是「黑條位置調對」，而是**對決版面本來就欠一個安全區**——
那是版面重做，不是本卷（純演出、版面一格不動）能塞進去的東西。

**改前改後**：`#lbTop`／`#lbBot` 兩個 div、`#lbTop.on,#lbBot.on{height:8vh}`、
`#duel.lbox{transform:scale(.80);box-shadow:0 0 0 100vmax #000}`、`pwLetterbox()` 與它的四個呼叫點
（`doSkip`／`pwPlayBeat` 開與關／`playDuel` 收場）、測試出口 `pwLetterbox`、
`duel-drive` 的黑條 MutationObserver 與逐場 `lboxMs`——**全部刪除，退回 v0.53 寫法**。
`?closeup=0` 從此只管 CINEMA 一件事。CINEMA shot、`endCinema()` 四入口取消路徑、tier 3 ＝1400ms **不動**。

**F5 機械段（新）**——三條都要綠：
1. **★tier 3 那一拍的版面與 v0.53 逐值相同★**：真實對局裡三尊大招那一拍（錨點＝`ys:fx-trait` 且
   `trId ∈ {eliteBlind, wardGuardAll, hauntAnswer}`），錨點 **+200／+400／+600ms** 三個時點
   （★取樣點必須避開會決定元素存在與否的計時器★：`#actorCard` 在錨點後 `ACTOR_CARD_MS=700` 被收掉，
   離它太近的取樣點會讓元素集合變成計時器抖動的函數——實測 +800 就踩中一次，歸因與數字寫在報告 §F5），
   `#duel` 底下**所有可見子孫**（`display` 非 none、`visibility` 非 hidden、`opacity >0.01`、面積 >0）
   的 `getBoundingClientRect()` 與 v0.53 基準樹同錨點**逐值相同**（x／y／w／h，取到小數一位），
   外加 `#duel` 自己的 `transform`／`overflow`／`scrollHeight`／`clientHeight`。
   **≥3 個錨點（分屬 3 場不同的對決）× 3 個時點**、**844×390 與 390×844 兩種方向**都要跑。
   白名單一個都沒有（容器本身也算）；樣本數或掃到的元素數為 0 一律當**不過**（相等性斷言要有活性證據）。
   治具：`tests/tools/duel-rects.mjs`（量測與比對）＋ `tests/tools/lbox-probe.mjs` 的 L10（判定）。
2. **CINEMA 只在 tier 3**：`lbox-probe` 的 L1／L2／L5／L8 保留（tier 1／2 零幀、tier 3 出現且收得乾淨、
   三條取消路徑、`?closeup=0` 時 maxK＝0）。
3. **`#lbTop`／`#lbBot` 在 DOM 中不存在**（`lbox-probe` L9），且 `index.html` 原始碼裡
   `.lbox` 規則、`pwLetterbox`、兩個 id 字面值各 0 處。

**鑑別力**（`02 §6.1` 第 1 條，正反都驗）：新機械段對**五版 `00706e0`** 必須紅（第 1 條 rect 不同、第 3 條黑條還在），
對收尾版必須綠；另外用「基準樹連跑兩次自己比自己」證明這個量測本身不抖（不然逐值相同是運氣）。
三組數字寫進報告。

**誰同意**：使用者 2026-09-11 裁甲（原話見本節開頭）。
**留給下一卷**：黑邊＋對決版面安全區（`#duel` 從「填滿到溢出」改成「有內距的版面」，3D 妖的
`figScale` 要跟著版面縮放，`.mvdesc` 的字級下限）——招式可辨性卷一起重做，不在本卷。
