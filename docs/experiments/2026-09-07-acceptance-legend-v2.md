# 驗收凍結檔：請神 2.0「神債暗標」＋神龕版面（2026-09-07）

基準 SHA：`bffcb34`（派工時的 main；收尾前須再併入最新 origin/main 重跑）。規格＝`docs/proposals/2026-09-07-legend-v2-debt-auction.md` §二（九條）。使用者裁定：採 2.0；請神夜固定 4／7／10、尊→夜每局洗牌並公開；版面依「北家正中、三龕搬進中央面板法寶卡上方、四家香火一行、盯上說明第二夜起收一行、修 19px 溢出、教學卡加請神」。VERSION 0.47（或派工時的下一號）。

## 範圍
- `index.html`：`makeState` 洗牌尊→夜；`resolveShrines` 改為請神夜結算（h 最高、同分風位順時針、h 全 0 回天）；移除擲骰／`INC_K`／`INC_PITY`／`shrineRollOrder`／天井文案；階段獎勵改以本龕最高 h 比例；一人一尊封籤鎖；供奉（夜末 −`INC_TITHE`，付不出回天、不重開）；AI 追價啟發式；神龕卡／規則頁／教學卡／回顧口徑；版面搬動與溢出修正；`CFG.SHRINE_NIGHTS`／`INC_TITHE`／`INC_MAX` 常數化。
- **傳說共鳴**（使用者裁定）：三尊在 `facCount` 視為 2 件（沿用雷女之火／五營旗的 `onFacCount +1`），共鳴 CAP 3 不動。
- **袋子部隊預覽＋招式**（使用者裁定）：袋子面板逐件顯示 隻數／atk／hp／出手拍／**招式名＋一句效果**（招式取 `TRAITS` 表的名稱與說明，同一事實來源，不另抄），總計一行；**市集法寶卡也加同一行**（隻數／atk／hp／拍／招式），讓出價前就能規劃部隊；傳說三尊同樣顯示。
- 治具：`tests/legend.test.mjs` 重寫（舊擲骰案移除、依 G5 新案）、`tests/tools/legend-gate.mjs` 改成 G0–G4、`legend-drive.mjs` 依新流程。
- **不動**：對決引擎、共鳴、心願、其他規則。

## 驗收條件（動手前訂；門檻不得為了過而調）
- **G0 kill switch 雙向**：`?legend=0`（`LEGEND_ON=false`）下 `trace(1..20)` 與基準的 OFF 路徑逐位元組相等；預設開不等且 `S.shrines` 三龕各帶 `night∈{4,7,10}` 且互異。
- **G1 優勢策略（n=10000，六策略：splitter／greedy／hoarder／specialist／incenseMax／incenseNever／追價 AI）**：**`−8pp ≤ incenseMax − splitter ≤ +5pp`**（下界為 §2.1 修訂三**加嚴**：只有上界時「把燒香做成純虧損」照樣綠）；`incenseNever ≥ splitter − 8pp`；任一策略座位 0 ≤ 40%。基準（1.0）數字：incenseMax 33.68／splitter 22.96／incenseNever 15.77。
- **G2 活性**：≥80% 的局至少一尊被請走；~~供奉回天發生在 1%～20% 的局~~ → **供奉回天的兩個口徑「都」要落在 1%～20%**：①「斷供之後仍活到局末」的局比例（§2.1 修訂一）
  ②「任何付不出供奉都算」的局比例（§2.1 **修訂五**把它從記錄項改回一起判，加嚴）；三尊被同一人請走的局 = 0（一人一尊）。
- **G3 節奏**：預設 AI 桌局長中位 10～12 夜；三策略位移 ≤ ±2pp（相對 0.44 基準）。
  ★本卷結果：中位 11 夜 ✅、位移 splitter −2.50／greedy −1.85／hoarder −3.40，**兩格落在門檻外**；
  門檻與數值都不動，經使用者同意記為**已知位移**（§2.1 修訂七）。★
- **G4 消耗戰**：每個請神夜，燒香總量前三名的平均投入 ≤ 9 壽命；落空者平均淨損（燒掉 − 階段獎勵退回）≤ 6 壽命。什麼實作會讓它紅：AI 追價沒有停損、三家互追到死。
- **G5 單元測試（對基準必紅在行為斷言）**：①請神夜 h 最高者得、同分風位順時針 ②h 全 0 回天且不重開 ③一人一尊：已請者的封籤被拒且 UI 鎖 ④供奉：夜末 −1、壽命 ≤1 時回天並移出袋；「送神回天」鈕當夜起不再扣且龕不重開；危急提示只在付後壽命 ≤`TITHE_WARN` 時出現且同一尊只一次（headless 走預設「要」） ⑤階段獎勵依本龕最高 h 比例 ⑥尊→夜洗牌用 `S.rng`、seed 固定可重現、三夜互異 ⑦OFF 路徑零 rng 消耗。
- **G6 Playwright（`legend-drive.mjs` 新流程，seeds ≥6）**：請走／回天／落空獎勵各至少一次；0 console error；**橫式橫向溢出 0**（本卷把 `#shrines` 溢出修掉，門檻回到 0）；**直向門檻**（§2.1 修訂四**二版**，使用者裁定 G6 甲）：844×390、dpr=2、seeds 1 與 3，第 1～3 夜的出價頁與盯上頁——
  第 1 夜每頁 `#felt.scrollHeight − clientHeight ＝ 0`、其餘頁 ≤ 基準 `578b2de` 同格 ＋52px、且本卷新增元素不得讓第 1 夜任一頁由 0 變 >0；直式蓋板行為不變。
- **G7 版面截圖（人眼，844×390）**：北家卡在頂端正中、西東對稱；三龕整排在中央面板法寶卡正上方、四家香火一行；盯上說明第二夜起一行；第 1 夜與請神夜前一夜各一張；10v10 對決截圖與 0.45 一致（不受影響）。
- **G9 持有者優勢帶（n=10000，預設 AI 桌）**：~~「請走任一尊者最終獲勝」的局比例落在 [45%, 60%]~~
  → **「持有者局勝率」減掉「零效果對照的同一條指標」落在 [+3pp, +10pp]**（2026-09-07 依 §2.1 修訂二改寫，使用者裁定）。
  兩邊的分子都是「持有者贏的局」、分母都是「至少一尊被請走的局」；**零效果對照＝三尊 `unit` 不上場**且拿掉 `eff`（傳說共鳴）
  （`{body:'ward',count:0,atk:0,hp:0}`）、其餘完全相同**（同一批種子、同一份規則），扣掉的就是選樣混淆。
  零效果對照值仍當**記錄項**印出來（覆審 MEDIUM-3 修正了對照組定義，見 §2.1 修訂二末段）。
  **且**持有者絕對局勝率 **≤85%**（§2.1 修訂六加嚴）。
  低於 +3pp → 傳說本體太弱、供奉降回 0；高於 +10pp → 供奉 2 或本體下修，任一調整都要使用者裁、不得自調。
- **G10 傳說共鳴閘門**：`resonance-gate.mjs` R2′／R3 n=10000 重跑仍綠（門檻不動）；單元測試：持殘日＋2 件祖靈 → `facCount("zuling")=4`，對基準紅在該斷言。
- **G11 部隊預覽正確性**：單元測試對 5 個袋子（含詛咒品、傳說、共鳴件）預覽的隻數／atk／hp／拍序／招式名逐項等於 `buildArmy` 展開與 `TRAITS` 表；27 件＋3 尊在市集卡上的招式一行皆非空且與 `TRAITS` 對得上；Playwright 開袋子與市集各截圖一張人眼（手機橫式不溢出、字不小於現行卡片說明）。
- **G8 文件與範圍**：GAME_DESIGN §五請神段改寫＋changelog；GUIDE 新一節（含「1.0 擲骰已移除，別回頭引用」）；ART_BIBLE 不動；`git diff --stat` 只含上述檔。

什麼實作會讓 G1 假綠：拿掉 incenseMax 策略或改它的定義；量法固定用 `legend-gate.mjs` 現有策略池加「追價 AI」。什麼實作會讓 G4 假綠：把落空獎勵調高到補足損失（那會反過來讓 G1 紅——兩條互相牽制，訂的當下已確認）。

## §2.1 修訂紀錄

> 程序：`02 §2.1`——動手後要改驗收條件，只有一條路：先寫明「原標準錯在哪、為什麼現在才知道」，
> 再取得使用者針對**這一條**的明確同意（加嚴不需同意，但要附改前／改後的實測數字）。以下七筆都附了。
> **這七筆只動門檻與量法，產品碼的實作沒有為了過而改**（修訂一／二 改寫當下 HEAD＝`e1adf6f`；修訂四～七＝第二輪覆審）。

### 修訂一（2026-09-07，G2 的「供奉回天 1%～20%」）——**使用者裁定同意（由主對話轉述）**

- **原標準**：「供奉回天發生在 1%～20% 的局」。
- **原標準錯在哪**：它想守的是「神債要有牙、但不能動不動就斷」——也就是**「養不起神」這個決策**發生的頻率。
  但實作出來之後量到的分布顯示，它實際上把**死亡螺旋的副產品**也算了進去：玩家壽命被對決打到 1 點、
  供奉付不出來，尊跟著走、人也跟著走——那一筆跟「養不養得起神」這個判斷無關。
- **為什麼現在才知道**：1.0 沒有供奉這個機制，這個分布**這一卷才第一次被量到**；訂門檻的當下沒有分布可看。
- **實測（n=2000，`shrineStat.titheLostLog` 逐事件回推）**：949 筆「付不出供奉」裡，
  **555 筆（58.5%）發生在玩家出局的當夜**、196 筆（20.7%）在下一夜、75 筆更晚出局、只有 **123 筆（13.0%）**
  的持有者活到局末 ⇒ **79% 落在出局當夜或次夜**。
- **改後標準**：「供奉回天**且斷供之後仍活到局末**的局比例落在 1%～20%」；原口徑改列記錄項、不判。
- **改前／改後數字**：原口徑 **42.51% ❌**／新口徑 **7.26% ✅**（治具改寫後同一支 `legend-gate.mjs 10000 --only=G2`
  一次跑出兩個口徑，n=10000 同一批種子；裁定當下引用的 6.2% 是 n=2000 的估計，同一方向）。
  同一份輸出的逐事件分布：5013 筆「付不出供奉」→ 當夜出局 2973（59.3%）／次夜 953（19.0%）／更晚 334／
  **活到局末 753（15.0%）**，與裁定時 n=2000 的 58.5%／20.7%／13.0% 一致。

### 修訂二（2026-09-07，G9 的「持有者勝率帶 [45%,60%]」）——**使用者裁定同意（由主對話轉述）**

- **原標準**：「請走任一尊者最終獲勝」的局比例落在 **[45%, 60%]**（絕對值）。
- **原標準錯在哪**：兩層。
  ① **無資訊基準線被「一人一尊」推高了**：一人一尊之後每局平均 **2.25 位**持有者
  （10000 局的分布：0 人 258／1 人 1762／2 人 3730／3 人 4250），
  「贏家在四席之間完全隨機」的期望值就是 **56.38%**——門檻上限 60% 只比純亂猜高 3.6pp。
  ② **選樣混淆**：燒得起香的人本來就有壽命餘裕、也活得久，所以「持有者比較會贏」有一大塊跟傳說強不強無關。
  這與 1.0 的 L3「請到者 vs 未請到者相對帶」被判為選樣混淆、依 §2.1 改寫成 L3′ 是**同一型**的問題
  （見 GUIDE §11.20 第 5 點）。
  合起來的後果是這條門檻**恆假**（`02 §6.1` 第 6 條的不變量掃描）：把三尊改成**零戰力**（完全不上場）之後，
  這條指標仍然是 **67.57%**——「傳說毫無作用」這個極端都過不了 60% 的上限，再怎麼調實作也到不了帶內。
- **為什麼現在才知道**：①「一人一尊」是這一卷才加的，訂門檻時三尊可以集中在同一人手上、持有者人數分布與
  基準線都不同；②「零戰力地板 67.57%」要**跑完實作**才量得到（凍結檔訂在動手前）。
- **改後標準**：「**持有者局勝率 − 零效果對照（三尊 `unit` 不上場且拿掉 `eff`、其餘完全相同）**」落在 **[+3pp, +10pp]**；
  零效果對照跑**同一批種子**，扣掉的正是上面那兩層。絕對值另由 §2.1 修訂六的 ≤85% 守（不再是純記錄項）。
- **改前／改後數字**：原口徑 **74.25% ❌**（門檻 [45,60]）／新口徑 **+7.82pp ✅**
  （74.25% − 66.42%，治具改寫後兩邊都跑 **同一批 n=10000 種子**）。
  裁定當下引用的 +6.68pp 是 74.25%（n=10000）減 67.57%（n=2000）的混合估計，同一方向、同樣在帶內；
  以同 n 的 **+7.82pp** 為準。
- **不是降難度的理由**：新帶 [+3,+10] 的寬度只有 7pp，比舊帶的 15pp **窄**；而且它扣掉了
  「不管傳說強不強都會有的」那 67.57%，量的是傳說本體真正搬動了多少——舊口徑量不到這件事。
- **對照組的定義後來又修過一次（覆審 MEDIUM-3，2026-09-07，不動門檻）**：一版的「零戰力對照」只把三尊的
  `unit` 換成不上場，**忘了拿掉 `eff`**——傳說共鳴（`facCount` 視為 2 件）在對照組裡照樣生效，
  等於「零效果」還帶著一個真效果、差值被低估。現在 `unit` 與 `eff` 一起拿掉才是真的零，
  治具另印「只拿掉共鳴」「只拿掉戰力」兩組當記錄項，讓兩個效果各自的貢獻看得見。

### 修訂三（2026-09-07，G1 ① 加下界）——**加嚴，自行記錄；使用者裁定同意（由主對話轉述）**

- **原標準**：`incenseMax − splitter ≤ +5pp`（只有上界）。
- **問題**：上界擋得住「燒香是優勢策略」，卻擋不住**反過來壞掉的那一半**——
  把燒香做成純虧損（沒有人該燒香、請神變成陷阱），這一格照樣綠。單邊門檻對「做壞成另一個方向」無感。
- **改後標準**：`−8pp ≤ incenseMax − splitter ≤ +5pp`（下界與 ② 同一個數字，對稱）。
- **改前／改後數字**：現行實作 **−4.94pp**（n=10000，合併 v0.47 前），兩邊都過；
  也就是加下界之後**現行實作仍然綠**，但「燒香純虧損」那類壞法從此會紅。**這是加嚴，不是移動及格線。**

### 修訂四（2026-09-07，G6 加直向門檻）——**加嚴，使用者裁定（由主對話轉述）；二版已依實測改寫**

- **原標準**：G6 只判「**橫式橫向**溢出 0」。
- **問題**：神龕列搬進 `#felt` 之後，844×390 實測 `#felt` 的**直向**溢出把市集卡的部隊預覽整行推到看不見
  （`#felt` 是 `overflow-y:auto`，捲得動不代表玩家看得到）。橫向門檻對這件事完全無感。
- **一版（絕對 ≤0）錯在沒量基準**：訂的時候寫「基準 `69df086` 為 0」，但**沒有真的量過**。
  第二輪覆審逐版重量之後才知道：**基準自己在第 3 夜的出價頁就有 49px**，而且會隨當夜抽到的卡與規則列而變
  ——「六頁全部 ≤0」這條門檻**連把本卷整個還原都達不到**，是恆假的。
- **為什麼現在才知道**：一版是照覆審轉述的數字訂的，沒有基準對照；第二輪覆審逐版重量才量出來。
- **改後標準（二版，使用者裁定 G6 甲）**：量測條件寫死＝**844×390、`deviceScaleFactor`（dpr）＝2、seeds 1 與 3**，
  取第 1～3 夜的「盯上宣告」與「出價」兩頁（每顆種子 6 格、兩顆共 12 格）：
  1. **第 1 夜的每一頁 `#felt.scrollHeight − clientHeight` ＝ 0**；
  2. 其餘各頁 **≤ 基準 `578b2de` 同種子同頁的值 ＋ 52px**（52＝神龕列展開成兩列的高度）；
  3. **本卷新增的元素不得讓第 1 夜任何一頁從 0 變成 >0**。
  治具：`felt-probe.mjs --seeds=1,3 --json=` 產出基準表，`legend-drive.mjs --base=<json> --slack=52` 判定
  （**沒帶 `--base` 就沒有基準可比，一律不算通過**）。
- **改前／改後數字（全部實測，844×390 dpr=2；順序＝n1盯上/n1出價/n2盯上/n2出價/n3盯上/n3出價）**：

  | 版本 | seed 1 | seed 3 |
  |---|---|---|
  | 基準 `578b2de` | 0 / 0 / 0 / 0 / 0 / **49** | 0 / 0 / 0 / 0 / 0 / 0 |
  | 本卷・修前（神龕列進 `#felt`、卡面帶招式、燒香列自成一列） | 6 / 19 / 2 / 49 / 47 / 133 | （同型，未逐格量） |
  | 本卷・修後（神龕列收一列、卡面只留數字、**燒香列併進底列**） | **0 / 0 / 0 / 0 / 31 / 87** | **0 / 0 / 0 / 0 / 18 / 18** |

  第 1 夜四頁全 0 ✅；其餘八格對「基準＋52」的上限全部落在門檻內（最緊的是 seed 1 第 3 夜出價 87 ≤ 49+52＝101）。

### 修訂五（2026-09-07 第二輪覆審，G2 撤回「改口徑」改成雙判）——**加嚴，使用者裁定（由主對話轉述）**

- **修訂一當時**把原口徑（任何「付不出供奉」都算）降級成記錄項，理由是它 42.51%、遠在帶外。
- **後來發現那個 42.51% 是排序造成的**：M-2 甲把供奉從「夜末最前面」移到「所有帳之後、天明回血之前」
  並跳過已被打到 `life<=0` 的人之後，同一格已經落回帶內（n=200 實測 10.00%、n=1000 11.00%）。
  既然它自己就過，沒有理由再把它降級。
- **改後標準**：G2 的供奉那一格**兩個口徑一起判**，都要落在 1%～20%
  ——新口徑（斷供後仍活到局末）守「養不起神」這個決策的頻率，原口徑守斷供的總量。**這比修訂一嚴。**
- **改前／改後數字**：修訂一（只判新口徑）→ 現在（兩個都判）；現行實作 n=200 是新 2.00%／原 10.00%，兩者都過。

### 修訂六（2026-09-07 第二輪覆審，G9 補絕對上限）——**加嚴，自行記錄**

- **原標準**（修訂二）：只有「差值 ∈ [+3pp, +10pp]」。
- **問題**：差值帶守「傳說本體搬動了多少」，守不到「持有者整體強到不像話」——
  若選樣混淆與本體強度一起變大，差值仍可能落在帶內。
- **改後標準**：差值帶**且**持有者絕對局勝率 **≤85%**。
- **改前／改後數字**：現行 72.55%（n=10000）／74.36%（n=200），離 85% 還很遠；1.0 是 76%。**兩邊都過，是加嚴。**

### 修訂七（2026-09-07，G3 記為已知位移）——**門檻與數值都不動；使用者裁定同意（由主對話轉述）**

- **現況**：G3 的「三策略位移 ≤ ±2pp」在本卷落在門檻外——n=10000（位移用前 2000 顆種子）
  splitter **−2.50pp**、hoarder **−3.40pp**（greedy −1.85pp 過）；n=1000 是 −2.30／−3.60，同號同量級。
- **歸因（第二輪覆審量的）**：位移的主體是 **1.0→2.0 機制本身**（hoarder 相對「完全沒有請神」是 −2.55pp），
  **供奉的落點只值 0.3～0.6pp**；`INC_TITHE` 0／2、`INC_MAX` 2 都救不回來。
- **裁定**：**門檻不動、實作也不調**（調 `INC_MAX`／`INC_TITHE` 是策略數值，硬規則 3），
  由使用者同意**記為已知位移**：請神 2.0 讓「囤積壽命不花」這種打法的機會成本變高，是設計上想要的方向。
