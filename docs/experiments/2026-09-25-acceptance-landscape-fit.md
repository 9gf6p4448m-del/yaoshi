# 橫向版面完整入框：驗收凍結檔 v1.0

凍結：2026-09-25，基準 `main 51c986e / v0.58.0`，早於任何修正。起因：使用者 iPhone（截圖 2000×923，比例同 iPhone Pro 橫向 852×393）進第 1 章，右側座位、請神列、「不盯任何一件」鈕被切掉；左側留出約一個瀏海安全區寬。使用者要求：①鎖只能橫向 ②檢查所有畫面轉場後會不會跑掉 ③有沒有畫面被切。

規則同 `02 §2.1`：提交即凍結；讓通過機率上升的改動須先寫明原標準錯在哪並取得使用者同意。

證據目錄：`docs/experiments/2026-09-25-landscape-fit/`。

## 量測矩陣

Chromium 無法模擬 `env(safe-area-inset-*)`；`assets/safe-area.css` 的 `--safe-*` 自訂屬性本來就是為合成治具留的，治具以覆寫這四個變數模擬安全區（這是重建模型，不是真機；真機另見 #6）。

| 代號 | 視口（CSS px） | 安全區 上/右/下/左 | 代表 |
|---|---|---|---|
| V1 | 852×393 | 0/59/21/59 | iPhone 15 Pro（使用者機型推定） |
| V2 | 932×430 | 0/59/21/59 | Pro Max |
| V3 | 844×390 | 0/47/21/47 | iPhone 12–14 |
| V4 | 667×375 | 0/0/0/0 | iPhone SE |
| V5 | 1280×720 | 0/0/0/0 | 桌機對照 |

## 畫面清單（每格都要量）

首頁、選角、夜行錄選單、引言卡、殘卷卡、第 1 夜盯上、出價、揭盅／開標、紙紮夜戰、夜末、異事夜抉擇、請神夜、袋子（sheet）、角色 ⓘ、規則 ？、局末卡、本局回顧。一般單人局與第 1 章各走一次（畫面相同者可共用，但兩種模式都要至少走到出價與局末）。

## 條件

**#1 入框**：每格畫面上所有可見的文字容器與可點元素（按鈕、座位卡、拍品卡、tab、對話泡、提示條），外框須落在「視口扣掉安全區」的範圍內（容差 1px）；若在可捲動的祖先裡，捲到後須入框。`documentElement.scrollWidth ≤ innerWidth`。3D canvas 與純背景不在此限。
- 鑑別力（必須先成立才准修）：量測治具在基準 `51c986e` 的 V1「第 1 夜盯上」與「出價」畫面**必須判紅**，且紅的元素包含右側座位或主按鈕（對應使用者截圖）。做不到＝治具量的不是使用者看到的現象，停手回報。
- 變紅：任何畫面有元素超出可視安全範圍或捲不到。

**#2 可點**：每格的主要按鈕 `scrollIntoView` 後，`elementFromPoint` 在其中心命中按鈕本身（沒被其他層蓋住）。
- 變紅：按鈕被切、被蓋、點不到。

**#3 轉向與轉場不跑版**：在出價、夜戰、局末、引言卡四個畫面各做一次「直式 393×852 → 橫式 V1」與「V1 → V3」尺寸切換，切換後重量 #1、#2，且 3D canvas 尺寸等於新視口（容差 2px）。另把整局從首頁走到局末的每個畫面切換之後都量一次 #1（即「轉場後」）。
- 變紅：切換後版面停在舊尺寸、canvas 沒跟著變、元素跑出框。

**#4 規則不變**：`node tests/tools/trace-eq.mjs <51c986e 的 index.html> index.html` 相等（純版面修正）。

**#5 全套不退步**：改動前後各跑一次 `node --test tests/*.test.mjs`，改動前通過的每一案改後仍過。

**#6 橫向鎖定**：依查證事實分兩層報告，不混稱——(a) 能真正鎖定的平台（實作並說明生效條件）(b) 不能鎖定的平台（iPhone Safari 等）的處理方式由使用者裁定後實作。使用者真機確認允許暫緩，暫緩＝未通過。

**#7 送達**：版本 bump、快轉推 main、Pages built、線上 curl 核對版本。

## 修訂紀錄

- **2026-09-25 #6 使用者裁定「維持轉向提示＋補強」**（查證：iOS Safari 分頁與主畫面 web app 均不支援 `screen.orientation.lock()`，manifest `orientation` iOS 不遵守；Android 在全螢幕或已安裝 PWA 下支援）。#6 細化為：(a) 開局手勢時呼叫 `screen.orientation.lock('landscape')`，不支援／被拒時靜默、不影響遊戲；(b) 直式（393×852）下 `#rotateHint` 在本檔畫面清單每一格都位於最上層（`elementFromPoint` 取畫面中心與四角命中提示層）；(c) 提示顯示期間盤點會自動推進遊戲的計時器，若有則列出並處理或照實記錄。
