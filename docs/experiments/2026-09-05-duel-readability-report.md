# 演出可讀性小卷——驗收報告（2026-09-05 晚）

凍結檔 `2026-09-05-acceptance-duel-readability.md`（基準 `fae1eec`）。證據 `2026-09-05-readability-evidence/`。

## 做了什麼
- `js/duel-figures.js`：n≥3 的列陣分排（n=3 一排三尊；n≥4 每排兩尊 → 4/6/8 尊各 2/3/4 排，排距 0.55），整排不再釘在 DOM 欄位中心，而是在「中線＋0.22 ～ 桌心 2.15」的可用區間內擺，用該排最內／最外兩尊各自的腳印當邊界、排內最小間距 0.55，塞不下才縮 step；奇數排往外錯半格。**n≤2 走原路徑**（R-4 逐尊 Δ＝0）。
- `index.html`：招式字幕改 `pwMoveCap`——「**出招方名**・法寶：招名」＋第二行效果說明（`TRAITS.desc`），class 帶系色（`f-*`，字色）與側別（`side-A|B`，靠出招那一側）；VERSION 0.34。
- `js/trait-fx.js`／`js/renderer.js`：招式期間戲台燈組往出招方質心移 0.6 倍（約 0.7 單位，指數收斂 150ms 內到位），收工回位；SKIP／cancel 立刻回位。
- 治具：`duel-perf.mjs lineup`（合成 NvM，用不在真對決裡的兩個座位避開撞擊位移；量腳印投影出桌、兩側間隙、最遠 r、同側最近距）；`duel-drive` 錄音機記每筆招式的字幕字／class／`#duel` 捲動高度；`traitfx-drive` 加 `focus` 判定；治具頁 `positions()`。

## 逐條證據（844×390、AMD 780M；最終數字＝覆審修正後 `lineup-lunge-*.json`，靜態取樣排除被真對決燒掉隱藏的尊）
| 條 | 結果 | 證據 |
|---|---|---|
| R-1 不交錯（靜態 gap ≥0.30） | ✅ | 8v8 0.842／6v6 0.472／4v4 0.647／3v3 0.906／5v7 0.680／8 精英 0.601（780×330：0.622）／8 虎爺 0.591（780×330：0.562）（基準 −1.566／−1.026／−0.740／−0.151） |
| R-2 不踩桌緣（r ≤2.20、出桌 0） | ✅ 含撞擊 | 靜態最遠 r：8v8 1.778／6v6 1.55／4v4 1.592／3v3 1.386；**合成撞擊 power 2.0 逐幀最大 r**：8v8 1.987／6v6 1.963／4v4 1.890／3v3 1.818／5v7 1.952／8 精英 1.966／8 虎爺 1.695；**同牌組前後對照** 2v2 fushou＋boartusk：修前（覆審員量 303ac93）maxR 2.082、腳印外緣 2.796 → 修後 1.479／2.150；腳印外緣全部 ≤2.15；offTable 全 0。量法比凍結時更嚴（凍結時同座位取樣只撈到當下那一幀的撞擊；現在是 power 2.0 全程逐幀最大值），覆審 H-3 後改的 |
| R-3 同側不疊（≥0.50） | ✅ | 8v8 0.549／6v6 0.55／4v4 0.549（連跑 3 次皆 0.549）／3v3 0.554／5v7 0.549／8 精英 0.55（780×330：0.55）／8 虎爺 0.549（780×330：0.52；第 3 輪前 0.146／0.000）；全母體 |
| R-4 n≤2 不變 | ✅ | 治具頁（固定時鐘、無撞擊）對 `fae1eec` worktree 逐尊比：1v1／2v2／2v1／1v2 maxΔ 全 0.00000（`r4-compare.mjs`；worktree 只有治具頁加了 `positions()`） |
| C-1 字幕誰丟＋效果 | ✅ | 同種子 4 場 7/7 筆含出招方名＋法寶＋招名＋desc、class f-*／side-*；**字的實際中心**（Range 量）A 側 262、B 側 582 vs 兩欄中心 317／527（覆審 H-1 前是 446／398，方向相反）；`--no3d` 同樣 7/7 |
| C-2 出招側打光 | ✅ 27/27 | `traitfx-all-v034.json` 每套 focus=true；cancel 後下一幀 rig=0 |
| C-3 不退步 | ✅ | traitfx-drive 原編制／滿編／reduced／cancel 全綠；同種子 4 場 traitFig 7/7、errors 0、時長差 −10／+50／+21／+11ms（上限 +80） |
| C-4 390px 不裁字 | ✅ | 7 筆招式當下 `#duel` scrollHeight/clientHeight 皆 390/390 |
| D 送達 | 見文末 | |

## 使用者追加（09-05 晚，「妖怪會不會被擋住」）
- **小的前排、大的後排**：站位按 `bodyScale` 排序（群體 0.6 → 作祟 0.82 → 護法 0.86 → 精英 1.15），同體型保留名冊序；只動站位不動 j／unit id。
- **排距 0.55 → 0.75**：俯角下深度差＝畫面高度差，後排的頭露出來。
- 隨之修一個 bug：單排交錯原本用名冊 j 的奇偶，排序後相鄰兩尊可能同奇偶站同深度（3v3 實測 0.478）→ 改用排內位置 k。
- 重量：8v8 gap 0.842／r 1.852／minPair 0.539；6v6 0.441／1.529／0.549；4v4 0.702／1.594／0.544；3v3 0.44／1.525／0.776；5v7 0.68／1.744／0.543；R-4 四組 Δ 全 0；traitfx-drive 原編制／滿編 27/27。仍有部分遮擋（不動鏡頭下避不掉），但後排頭部可見（`lineup-new-8v8.png`）。

## 中途修過的三處（第一版沒過）
1. 第一版把整排釘在欄位中心、用全側最大腳印夾寬：欄位中心只有 0.90／1.08、腳印 0.16–0.66 → 排被壓到 minPair 0.21。改成可用區間＋每排內外腳印。
2. 第二版每排兩尊但排內距離只剩 step0（n=8 時 ≈0.36）→ 加 rowMinStep 0.55。
3. R-1／R-4 的量法被真對決的撞擊位移污染（1v1 差 0.11）：合成對決改用不在對決裡的座位；R-4 改在治具頁量。

## 對抗覆審
**第 1 輪**（fresh opus，`2026-09-05-readability-review-round1.md`）：CRITICAL 0／HIGH 3／MEDIUM 3／LOW 4。
| # | finding | 處置 |
|---|---|---|
| H-1 | 字幕靠側方向相反：`#duel` 是置中 column flex，`#duelMove` 縮到內容寬，padding 把字推向對面（實測 A 側字心 446、B 側 398，比置中還遠） | **修**：元素 62% 寬、`align-self` 靠出招側、字在其中置中 → A 262／B 582；錄音機改量字的 Range 框 |
| H-2 | R-3 在真牌組不成立：三精英同排 `s` 被 `W/2` 壓到 0.341；極端視窗 W=0 兩尊同座標 | **修**：排數自適應（哪排塞不進 rowMinStep 就多一排）；三精英 minPair 0.554、780×330 boat×3 0.679 |
| H-3 | R-2 量法在凍結後改成排除撞擊，方向是提高通過率；撞擊正是把最外尊推出去的機制（power 2.0 時 r 2.43） | **修實作＋量法加嚴**：n≥3 的撞擊位移夾在徑向 rimMax−腳印內（規劃時每排用同一條幾何式算橫向上限、總深度上限 2.0）；`lineup --lunge=2` 逐幀取最大 r，六組 ≤1.99 |
| M | 報告數字與 json 對不上；R-1（腳印外緣）與 R-3（中心距）座標系不同；perRow=2 遮擋無驗收 | 表格重寫為最終數字；座標系差異寫明；遮擋屬品味留真機（使用者已追加小前大後＋排距） |
| L | 4 條風格 | 未動 |
另修一個量測污染：真對決的 `ys:fx-burn` 會打到合成名冊、被燒掉隱藏的尊位置停住 → 靜態與撞擊取樣只算 `group.visible` 的尊。
**第 2 輪**（`2026-09-05-readability-review-round2.md`）：H-1 真的修好；H-2／H-3 表面修好。
| # | 反駁 | 處置 |
|---|---|---|
| H-2 | 排數一路加到 n 時排距 2.0/(n−1) 沒人守：8 精英 minPair 0.286（一路縱隊） | **修**：排數上限 4（排距 ≥0.667）；仍塞不下改整側等比縮小（fit 1→0.5 逐級）；腳印從幾何×已知縮放算（不讀上一幀）；排數／fit 一場只選一次（鏡頭 punch 改 pxWorld 會讓逐幀重選翻面）。8 精英 0.549、8 虎爺 0.55 |
| H-3 | n≤2 沒夾（2v2 精英撞擊 r 2.092）；`visible` 過濾把母體改小（每組少一尊） | **修**：夾限對所有 n 生效但只削「推出去的部分」、永不拉到靜態站位以內（R-4 逐尊 Δ 仍 0）；量測恢復全母體。2v2 撞擊 1.887 |
| 未改 | 勝方內衝時兩側腳印壓過中線（lunge.minGap 負）——這是一拍・撞的碰撞本身，R-1 量靜態 | 寫明，不改 |
**第 3 輪**（`2026-09-05-readability-review-round3.md`）：H-3 與量測母體真的修好；**H-2 沒修到且退步**——`rowsFit` 在第一幀鎖住，那時 3D 妖影子幾何仍是預設半徑 0.42（GLB 載完才換 0.69），用錯腳印選了「不縮」整場鎖死：8 虎爺 0.146（修前 0.286）、780×330 兩對座標相同。另指出報告「8 虎爺 0.55」重現不出、2v2 前後對照換了牌組。
| # | 處置 |
|---|---|
| H-2 | **修**：鎖點改在該側所有 GLB 就位（`ready()`）之後；鎖住的排法若之後塞不下（`!ok`）、重選找得到塞得下的才換，找不到守住不翻面。8 虎爺 844×390 0.549、780×330 0.52；8 精英 0.55／0.55 |
| M-1／M-2 | 表格數字全部換成本輪實測且有 committed json；2v2 前後對照改用同牌組 fushou＋boartusk（修前數字取覆審員對 303ac93 的量測 `review5-2v2-fushou-boartusk-before.json`） |
| 量測污染 | 真對決時間軸的 `ys:fx-burn` 會在合成名冊 GLB 就位前打到同 id 單位、凍在暫定排法上（4v4 0.095 就是它）；真遊戲不會（時間軸等 `det.ready`、未就位走 DOM 退路不隱藏）。lineup 模式在 capture 階段攔掉該事件；4v4 連跑 3 次 0.549 |
**第 4 輪**（使用者選乙：修完再冷讀一輪）：見下。

## 教訓（候選）
- 站位量測要避開 lunge：同座位的合成對決會吃到真對決的 `ys:fx-lunge`。
- 「用最大腳印夾整排」對體型差大的隊伍過度保守；邊界要看排端那兩尊。

## 不在本卷／待裁
- 鏡頭（後處理卷，使用者裁定乙）、燒毀規則（甲維持）、慢動作（不做）、`PAPERWAR_ON` 預設。
- 4 排深的 8v8 在對決機位下後排會被前排遮一部分——真機再看；若覺得太深可把 perRow 調 3（3 排）但同排間距會回到 0.5 邊緣。
