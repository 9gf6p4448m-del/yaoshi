# 驗收凍結：心願牌庫餘 16 張（v0.8）

- 凍結時間：2026-09-02，基準 commit `365230a`，使用者裁定「都照建議」（提案四題：清單照提案／鎖定對手類寫進牌面顯示對手名／閘門如下／版本 v0.8）
- 規格來源：GAME_DESIGN §六之三 D 只寫 8 張，餘 16 張為本次提案（清單見同目錄 `2026-09-02-wish16-proposal.md`）

## 驗收條件（一經訂定即凍結，02 §2.1）

| # | 條件 | 什麼實作會讓它變紅 |
|---|---|---|
| W1 | `WISH_ON=false` ⇒ `trace(1..20)` 與 `365230a` 逐位元組相等；`WISH_ON=true` ⇒ 必不相等 | 新統計欄位或抽卡路徑在關閉態仍消耗亂數／改結算；或新牌根本沒進牌局 |
| W2 | 執行期刪掉新 16 個 `WISHES` 鍵（只留原 8 張）⇒ `trace(1..20)` 與 `365230a` 逐位元組相等 | 動到原 8 張的 check/hook，或新增統計改變了結算數值 |
| W3 | `tests/wish16.test.mjs`：16 張 × 達成／未達成 ＝ 32 案全綠；對 `365230a` 版跑必紅且紅在行為斷言（不是 TypeError） | 任一張的判定式與牌面描述不符；或測試只驗新匯出存在性 |
| W4 | 平衡 n≥10000（`aiLike` 座位 0）：每張達成率 15%~80%；每張持有者條件勝率 45%~65%；三策略（splitter/greedy/hoarder）位移 ≤1.5pp | 出現恆真／恆假牌、抽到就贏或抽到就輸的牌、或牌庫擴充改變策略均衡 |
| W5 | `grep -c "Math.random" index.html`＝0；正常頁跑 2 局＋`?sim=1` console 0 error；844×390 心願列（最長 desc）無溢出 | 引入非種子亂數、執行期例外、UI 溢出 |
| W6 | `VERSION="0.8"`；push 後 `curl` 線上 index.html 抓到 `VERSION="0.8"` | 沒推上去或首頁字串沒改 |

**W4 執行註記（2026-09-02，02 §2.1 例外・自行修正、事後回報）**：條件勝率「45%~65%」是從獻祭刀時代（座位 0 用 splitter、基準約 59%）抄來的絕對帶，
在本批的量測設定（座位 0 用 aiLike，四人局基準 23.8%）下**無論實作對錯都不可能通過**（上限 65% 要求持牌者勝率是基準 2.7 倍）。
改寫為相對帶：條件勝率／無條件基準 ∈ [0.7, 1.5]（抽到就輸／抽到就贏的偵測目的不變）。這個修正不會讓壞掉的實作變成通過——
它仍抓得到 ×0.5 或 ×2 的牌。**三策略位移 ≤1.5pp 一條未過且不能由我改**，見 `2026-09-02-wish16-balance.md` 與 ARCH_SPEC 待辦 21。

W4 不過的牌：調 `CFG.WISH_T`／`CFG.WISH_REWARD` 門檻（本批凍結前已載明「不過的牌調門檻而不是拿掉」，屬設計迭代非移動及格線）；達成率仍出 15~80% 範圍者列入試玩必調並如實回報。

**結案（2026-09-02）**：W1/W2/W3/W5/W6 過；W4 逐張達成率與條件勝率過、三策略位移未過（+7.23／+3.88／+1.62pp）。使用者裁定甲：接受位移、上線，AI 預設打法另開批（ARCH_SPEC 待辦 22）。
