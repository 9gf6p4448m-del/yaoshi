# A2 紙紮夜戰與資產標竿：驗收凍結 v0.1（2026-09-17）

建立時基準：v0.57.17（main `3aac488`）。計畫：[plans/2026-09-17-a2-benchmarks.md](../../plans/2026-09-17-a2-benchmarks.md)。一經訂定不得為了通過縮案例、降門檻或以新名字覆蓋舊失敗；要改依 `02 §2.1`。S0 拷問未答的欄位（詛咒件）以「待填」標示，填入即凍結，不另開修訂。

**沿用不重列的門檻**：A1 凍結檔 G1（規則不變、trace 相等、測試全綠）、G4（構圖／HUD 不相交）、G6（perf32 逐輪 ≥ .40、calls／tris 上限、dispose 不增）；M-A1 原門檻（概念類別 2/2、特徵 ≥3/5、「可愛／玩具」0）；P4 原門檻與讀者規程（`2026-09-13-zuling-b2-report.md` §8 的 18 讀者制）。

| # | 條件 | 什麼實作會讓它變紅 |
|---|---|---|
| 1 | **每件標竿先出方案再定稿**：實作前有 contact sheet（≥2 方案 × 三情境各一張：英雄視角、實際桌面大小 844×390 hover、滿編 8v8）與使用者挑選紀錄（原話或「都照建議」）；沒有挑選紀錄不得進入盲讀。 | 直接做一版就送盲讀 |
| 2 | **硬指標**（每件、每方案）：`duel-perf bounds` 正規化後 `min.y ≥ 0`；三角形數不超過該件現版 ×1.3；對決 8v8 `gl-duel-probe` draw ≤ 320（現 296）；perf32 逐輪 ≥ .40；取景矩陣 `--all --match=<key>` 全過；`node --test` 全綠；trace seeds 1–20 相等；console 0 error。 | 為了美術把 draw 推回 400、或改時長契約 |
| 3 | **資產標竿盲讀**（每件挑定方案後，context-free 讀者，材料由 `a2-sheet.mjs` 產出的三情境圖）：概念類別 2/2、特徵命中 ≥3/5、「可愛／玩具」0/N；**最多 3 輪**，未過交最佳版標「未過」由使用者簽字。輪數從方案 X 第 1 輪起算，不沿用該件舊 R-A2 輪數，也不覆蓋舊紀錄。 | 第 4 輪、或把舊方案的輪數歸零重來卻不換方案 |
| 4 | **三情境可辨**（每件）：英雄視角看得出是哪一件（讀者命名或描述特徵 ≥2/2）；實際桌面大小（844×390 hover）三系辨識色與剪影可辨（讀者系別 2/2）；滿編 8v8 該尊在自己那側被遮擋率 ≤ 30%（沿用 legend-presence 量法）且讀者仍能指出它（2/2）。 | 只驗英雄視角大圖 |
| 5 | **來源／對象可讀**（招式）：拼板舟 swarmHalfSplash、福壽綿長 wardRegen1、紅帽 hauntLost 三支，各以 P4 原讀者規程重讀**本支**（不重跑 18 支）：效果／系別維持 ≥15/18，對象題 ≥ 12/18（P4 原「過」線沿用 §8 的判法）；「我方單一」增益必須只亮受益那一尊（st.bodySpot 或等價）、不得罩兩尊或只亮施招者；送王船金箔帆「降」被讀成減益箭頭的語意衝突要在 vocab 層處理並在本卷記錄處置（本卷不重讀送王船，列後續）。 | 增益仍罩兩尊；改題目讓對象題變簡單 |
| 6 | **大士爺護心鏡回修**：橘球移離護心鏡後補讀一次（6 讀者），護心鏡 ≥3/6；舌／龕依 2026-09-17 簽字為引擎限制不列本卷目標。 | 改模型比例換讀者猜中 |
| 7 | **詛咒一件**（待填：S0 三選一）：造型在實際桌面大小可辨（讀者說出「詛咒／不祥」2/2）、與六種既有造型不混淆（三情境圖並排讀者指認正確 ≥5/6）；768 卡面 9 項與 curse-migration 既有檢查全過。 | 新造型讓既有六種混淆 |
| 8 | **不動的東西**：三系色票與「不可愛」守則（ART_BIBLE）、演出時長、規則、A1 構圖契約；每件實作後 trace-eq 相等、A1 slot 塊矩陣過。 | 為了美術動色票或時長 |
| 9 | **收卷**：每件一個子卷目錄（acceptance 執行紀錄、方案 sheet、盲讀材料與讀者 JSON）；發布逐版核對送達；藍圖 §5 A2 狀態改寫並列未過項；P4 8 支中本卷處理到的逐支更新狀態、其餘照舊列未解。 | 只交截圖不留讀者原始 JSON |

## 執行紀錄

（S0 起逐項補）
