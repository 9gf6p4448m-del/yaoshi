# 驗收條件（凍結於動手前）— 妖市 v0.53「開標後燒香重觸發競標」

凍結時間：2026-09-11，worktree HEAD = c866b01（＝待修版本，動手前）
探針：`tests/tools/reveal-reentry-probe.mjs`（Playwright 真實 UI，單一指令、決定性）

- **A1 相位不得倒退**：在「🏮 本夜成交總覽」畫面（`#mainbtn` 文字＝「🕯️ 請神」或「開戰」）真按
  `.incbar` 的「＋」之後，`#mainbtn` 文字**不得**含「蓋牌」，且 `#stage` 內**不得**重新出現 `#market`。
- **A2 壽命只扣一次**：同一顆種子，「對照組（總覽畫面不按＋）」與「重現組（總覽畫面按一次＋）」
  在第 1 夜結束（按過「🕯️ 請神」、`resolveShrines` 跑完）時，四家壽命**逐位相等**。
- **A3 開標只跑一次**：重現組第 1 夜的 `resolveAuction` 呼叫次數 == 1。
- **A4 正常路徑不得壞（不變量掃描）**：
  1. 出價階段按「＋」仍會讓 `INC.amt` +1（一般夜與請神夜皆是）；
  2. 燒的香真的扣壽命：對照組第 1 夜燒 1 點，請神後真人壽命 == 開標前壽命 − 出價支出 − 1；
  3. `legend-drive --all --seeds=1,2,3` 仍 0 error（AI 席、真人選尊、落空保留三條路徑照走）。
- **A5**：9 套 `node --test tests/*.test.mjs` 全綠；`trace-eq` 對 c866b01 的結果要寫明 equal／differs 與理由。

**紅燈要求（鑑別力）**：探針對 c866b01 必須紅在 A1／A2／A3 的**行為斷言**上（不是 TypeError）；
對修好的版本全綠。反面：A4-2 在健康狀態下必須綠（證明探針量得到「香真的燒掉了」，不是反向探針）。

---

## 事後**加嚴**紀錄（只增不減，未動任何原訂門檻；`02 §2.1`「細化與加嚴自行記錄即可」）

動手後由 fresh-context 對抗覆審（`opus`，只給檔案與修法、不給對話史，任務是「反駁已修好」）抓出
**F1（HIGH）：A1／A6 的綠燈 100% 來自「移除 DOM」那一半，`BIDS_OPEN` 那道閘在整組證據裡一行都沒被執行到
——把閘刪掉探針照樣全綠＝零鑑別力**。據此加了三條，原有 A1–A5 一字未動：

- **A6 熱座回歸**：雙人熱座第 1 夜兩席各真按一次「＋」都要 0→1；整夜走得到「開戰」；
  總覽畫面按「＋」不得倒退；`resolveAuction` 次數 == 1。
- **A7 直接行使相位閘**：在總覽畫面**直接呼叫** `showMarket()`／`incBump(1)`／`submitHumanBids()` 三支，
  `#mainbtn` 文字、`INC.amt`、`S.round`、`resolveAuction` 次數、`#market` 是否出現，五項**全都不得變**。
- **A8 不得卡死**：三個回合都不得推不動（閘設太緊會讓畫面停住，這條把它抓出來）。
- **A7 的鑑別力用兩個「只刪一行」的突變體驗**（原檔全程唯讀，不做反向 sed）：
  M1＝刪掉 `showMarket` 的 `if(!BIDS_OPEN) return;` ⇒ **A7／A4-2／A8 必須紅**；
  M2＝刪掉交卷時清 `#south .incbar` 那行（閘留著）⇒ **必須仍全綠**，
  證明修好的路徑不是只靠「把按鈕藏起來」——使用者那一下真的按到了（`plus2.ok=1`、`incBtns=2`），是閘擋下的。
