# 普通版 H9 雙虎／血祭加強候選：試跑凍結規則（2026-09-24）

**性質**：探索試跑，只用來挑候選；不是正式驗收，不改 `index.html`、不改 H9／H1 判準、不覆寫任何既有 raw。

## 使用者裁定（2026-09-24）

- 普通版 H9 雙虎 +0.98pp、血祭 +1.34pp 未達 +3～+10pp；使用者選擇**開新候選臂加強數值**。此舉改變 [H9 診斷](../2026-09-23-destiny/h9-diagnostic.md)「不因 H9 fail 就直接加數值」的既有方針，由使用者明確裁定。
- 雙虎：加強部隊（不加買路錢，避免加重失敗者懲罰造成避標）。血祭：放寬低命攻擊加成。
- 試跑規模：原選每組 2,000 局；因持有局僅 15–20%，2,000 局的 H9 差 95% 區間約 ±7pp，無鑑別力，經使用者同意改為每組 10,000 局。

## 候選（只在試跑記憶體內替換 `index.html` 字面值，每個替換須恰好命中 1 處）

| 候選 | 替換 | 現值 → 候選值 |
|---|---|---|
| tiger-S | `index.html:2050` 雙虎部隊 | `atk:9,hp:8` → `atk:10,hp:10` |
| tiger-M | 同上 | `atk:9,hp:8` → `atk:11,hp:12` |
| blood-S | `index.html:4535` 血祭低命加成 | `life<=15) a+=1` → `life<=20) a+=1` |
| blood-M | 同上 | `life<=15) a+=1` → `life<=20) a+=2` |

## 臂與種子

- 種子 `30001..40000`（與正式 `10001..20000` 不重疊），每臂 10,000 局，全部完成才分析。
- 共用（候選替換不影響，因效果整條被移除）：`h9-zero-twinTiger`、`h9-zero-bloodOath`（未替換來源）。
- 每個候選：`h9-normal`、`h1-splitter`、`h1-<目標鏈>`，以候選來源跑。
- 策略與 AI 與 `tests/tools/l1-destiny-run.mjs` 的 ordinaryH1／ordinaryH9 分支相同。

## 指標（同 `tests/tools/l1-destiny-ordinary-analyze.mjs`）

- H9：`h9-normal` 與 `h9-zero-<鏈>` 的持有者條件勝率差（pp），需 `+3 ≤ 差 ≤ +10` 且 normal 絕對率 ≤ 85%。
- H1：`h1-<鏈>` 與 `h1-splitter` 座位 0 勝率差（pp），需 `−8 ≤ 差 ≤ +5`。

## 挑選規則（跑前凍結，跑後不改）

1. 每條鏈在 S／M 中找 H9 與 H1 都符合的候選；兩者都符合選 S（幅度小者）。
2. 該鏈 S／M 都不符合 → 停手回報使用者，不自行加大幅度或換旋鈕。
3. 選出的候選才進入正式驗收：改 `index.html`、新增候選臂、以正式種子 `10001..20000` 重跑，判準照原口徑。
4. 另記錄但不作為挑選條件：各候選的持有局數、H9 點估計的 95% 配對 bootstrap 區間。

## 第二輪（2026-09-24，第一輪結果見 report.md 後由使用者裁定，跑前凍結）

- 雙虎：不再加數值；普通版 H9 雙虎改列**已知診斷基線**（第一輪部隊 10／10、11／12 的 H9 為 −0.71／−0.03，加強部隊不動 H9）。
- 血祭：新增候選 `blood-L`＝`life<=15) a+=1` → `life<=25) a+=2`，只跑 `h9-normal`、`h1-splitter`、`h1-bloodOath` 三臂，種子、局數、指標同上；`h9-zero-bloodOath` 沿用第一輪（替換不影響 zero 臂，已驗證）。
- 挑選：`blood-L` 的 H9 與 H1 皆符合 → 進入正式驗收；不符合 → 停手回報，不再自行加級。
