# 法線貼花小卷（技術驗證）——驗收凍結檔（2026-09-06 深夜，動手前訂）

> 使用者裁定（09-06 深夜「照建議」）：Q2 甲＝只做技術驗證，挑 1 隻證明「貼花／描邊在現行引擎做得出、盲讀能讀出」，再決定要不要鋪開；先量 M-4 軟體 GL 成本。
> 基準 SHA：`d698875`（v0.38）。本檔訂下後任何讓通過機率上升的改動走 `02 §2.1`。

## 範圍
- 目標生物：`eye`（gaps.md ④「石體橫向裂縫」回修卷三輪六位 0/6——石 6/6 讀出、但沒有一位讀成「石頭被一道橫向縫裂開」，主縫被歸給眼窩／咬合口）。
- 機制：**程序式裂紋貼花**——在 `js/creature-figures.js` 的 `dressMaterial` 注入 GLSL：以 rest-pose 本地座標（既有 `vLocalPosR`）定義 N 條橫向裂紋線（y=y0±jag·noise），核心變暗（albedo）＋兩唇法線傾斜（上唇朝下、下唇朝上＝內陰影）。不動幾何、不動 GLB、不加 pass、不加貼圖。資料表 `DECALS[生物鍵]`（材質名正則＋參數），無表項的生物零效果；`?decal=0` 全關。
- 不做：tiger_c 白毛邊、其他簽字項；不動 `tools/anyCreature`；不動 index.html 引擎（只 bump VERSION）。

## 驗收條件（動手前凍結）
| 編號 | 條件 | 判法（指令） | 什麼實作會讓它變紅 |
|---|---|---|---|
| D1 等價 | 引擎逐位元組相等 | `node scratchpad/trace-eq.mjs <d698875 的 index.html>` seeds 1..20 IDENTICAL | 動到 index.html 任何引擎碼 |
| D2 活性 | eye 的裂紋真的畫出來 | `creature-shoot` 拍 `glb=eye` 兩張（`decal=1` vs `decal=0`，同機位同相位 idle、`auto=0`）；rock 區像素差（任一通道 >8）的像素數 **≥ 300**（@2x 844×390） | uniform 沒送到、注入點錯、材質正則沒配到 rock |
| D2′ 突變 | 證據有鑑別力 | 把 `DECALS.eye.dark=1.0、tilt=0` 再拍一次 → D2 像素差 **< 30**（=0 才對，留雜訊餘裕）；還原用備份副本不用反向 sed | 像素差來自別處（雜訊／動畫相位），不是貼花 |
| D3 盲讀 | 裂縫被讀成「石頭裂開」 | hero（`light=1&fx=1&rim=zuli`）＋stage（`light=0`）兩張，**兩位** context-free 讀者（fresh sonnet，只給圖）；問「這是什麼／列 5 個最明顯的特徵／石頭表面有什麼」。**過＝主印象「眼／巨眼」2/2 不退步 且 ≥1/2 在特徵或表面題逐字出現「裂縫／裂開／裂痕／龜裂／裂紋」且指的是石頭表面（不是眼窩）**；0/2＝紅（技術驗證失敗，記 gaps）；「可愛」出現＝紅 | 裂紋太細看不到、被讀成條紋／髮絲／陰影 |
| D4 效能（硬體 GL） | 不拖慢對決 | `duel-perf perf --uncap` 8v8，基準（worktree d698875 `--root`）與新版**各跑 3 次、單獨跑、取 rAF 中位數的中位數**；新版 ≥ **0.95×** 基準 | 每像素多做的事太重（loop／noise 未被 uCrackN=0 短路） |
| D5 效能（軟體 GL，M-4 待量） | 軟體路徑也能跑、順便量到數字 | 同 D4 但 chromium 用 `--use-angle=swiftshader`（`bloomOK=false` 路徑）；新版 ≥ **0.90×** 基準，且 shader 編譯 0 error；把兩版絕對 fps 記進 gaps.md M-4 | GLSL 在 SwiftShader 編不過（uniform int 迴圈）、或成本翻倍 |
| D6 範圍 | 無表項的生物零效果 | `creature-shoot` 拍 `glb=tiger_c` `decal=1` vs `decal=0` 像素差 **< 30**；`duel-drive` 3 場 0 error | 注入碼在 uCrackN=0 時仍改到 normal／albedo |
| D7 回報 | 完成四件套 | 檔案:行號、指令原文、實際輸出；`git diff --stat` 逐檔對應 | — |

## 決定閾值的依據（訂當下）
- D2 300 px：eye 在 @2x 約 300×360 px，一條寬 2–3 px、長 150 px 的裂紋 ≈ 400 px，三條 ≈ 1000；300 是「至少一條完整出現」。
- D4 0.95：後處理卷合體 raf 中位 106.4 vs 111.1（96%）為前例；本卷不加 pass，理應 >0.98。
- D3 ≥1/2：前三輪 0/6 的項目，技術驗證的問題是「這條路能不能讀出來」，不是出貨；出貨門檻（2/2）留給鋪開卷。

## 突變／還原程序
- 備份 `js/creature-figures.js` → `scratchpad/creature-figures.decal.bak`；突變改 `DECALS.eye`；驗完 `cp` 還原、`git diff` 確認。

## 程序修正紀錄（動手後；判準門檻 300／30／0.95／0.90 未動）
1. **D2/D2′/D6 的截圖相位 idle → `reset`＋預覽頁新參數 `?freeze=1`（idle timeScale 0）**。依據：同參數兩次拍攝的雜訊底——idle＋fx=1：eye 8493 含粒子；idle fx=0：tiger_c 27／eye 810；reset 不凍：eye 0／tiger_c 2019（呼吸動畫時序）；reset＋freeze：eye 0／tiger_c 0。原程序下 D2′「<30」與 D6「<30」在**任何實作**下都會被動畫雜訊打紅（`02 §2.1` 例外：無論對錯都不可能通過→自行修正、事後回報）。治具改動：`tests/tools/creature-preview.html` setPhase('reset') 讀 `?freeze=1`。
2. **D2′ 突變式擴充**：實作在凍結後多加了「假斜面」albedo 項（上唇 ×0.78、下唇 ×1.18），原突變（dark=1、tilt=0）殺不掉它；突變改為 dark=1、tilt=0、假斜面係數 0（三項全歸零＝「shader 路徑仍執行、效果為零」）。方向：這是把反向探針補完整，不影響 D2 的通過機率。
3. **D5 治具**：`tests/tools/duel-perf.mjs` 加 `--gl=swiftshader`（原本寫死 d3d11）。
4. 盲讀圖仍用 idle＋fx=1＋deviceScaleFactor 2（讀者看的是自然畫面，不需逐像素可重現）。

## 結果（逐條填）
- D2：`fz_eye_1.png` vs `fz_eye_0a.png` 像素差（>8）**11297**，bbox (687,130)-(1019,569) ✅（≥300）
- D2′：突變版 vs decal=0 **0** ✅（<30）；還原用 `scratchpad/creature-figures.decal.bak` 覆蓋，`grep -c MUTATION`=0
- D6：`fz_tiger_1.png` vs `fz_tiger_0a.png` **0** ✅（<30）；雜訊底 tiger_c/eye 皆 0
- 調參紀錄（盲讀前，屬實作）：單層雜訊版目視為「平滑波浪條紋」→ 改兩層雜訊（低頻 5.0＋高頻 ×4.3、0.45 權重）、width 0.022→0.028、dark 0.30→0.22、tilt 0.7→0.9、加假斜面；D2 由 6871→11297。
- **D3 第 1 輪（兩位 fresh sonnet，只給 hero＋stage）：實質未過。** 主印象「眼」2/2（A「碎裂岩石外殼中嵌著發光眼睛」、B「獨眼守護石像」）、無「可愛」。「裂縫」字面 2/2 但都指幾何：A「右側一條縱向凹陷帶像裂縫」、B「板塊裂縫卡在石塊接縫處」；貼花所在位置（眼下偏左的下崖）B 讀成「數道細密的平行刻痕（抓痕／風化紋）」、A 未提。歸因：核心暗線＋上唇壓暗＋下唇提亮＝三條平行帶→抓痕。**不以字面 2/2 判過**（判準本意是貼花被讀成裂縫）。
  第 2 輪改法（實作，不動門檻）：去上唇暗帶、只留一條暗線＋細下唇高光；每條線加角度參數，做一條斜向分岔（主縫＋分岔＝裂縫簽名）。第 2 輪除凍結的單圖題，另加 2 位「配對題」（decal=1 vs 0 同機位兩張，問石面差在哪）當補充證據；D3 判定仍以凍結的單圖題為準，最多 3 輪。
- **D3 第 2 輪（改法：單暗線＋細下唇高光、加斜向分岔；D2 重量 2287 px 仍 ≥300）：過 2/2。** 單圖題：C「下半身一道彎曲的深色**裂縫**或刻痕」、D「眼睛下方、胸口偏右一道深色弧形刮痕／**裂痕**」；主印象 C「獨眼岩石怪物」、D「岩石系怪獸…鑲著發光巨眼」＝2/2；無「可愛」。補充配對題（decal=1 vs 0，兩位順序對調）：E、F 都把貼花圖判為「較被裂開／受損」（2/2 方向正確）；F 定位正確（眼下軀幹、「裂痕／刻痕／傷疤」）、E 定位到右上肩的第三條線（「鋸齒紋／裂痕邊緣」）。
- **D4（硬體 GL，780M，--uncap，各 3 次單獨跑）**：新版 rAF 中位 fps 105.3／99.0／100.0 → 中位 **100.0**；基準 106.4／97.1／99.0 → 中位 **99.0**；比值 **1.01** ≥0.95 ✅；draw calls 1002（new_2 1060 為該場合成陣容差異）；0 error。檔：`scratchpad/perf/{new,base}_{1,2,3}.json`。
- **D5（軟體 GL，SwiftShader）：比值條不可判定（訊號不可信，`02 §6.2`）；「編譯執行 0 error」那半條 ✅。** 兩輪對決量測方向相反：第 1 輪 new 0.3 vs base 3.1 renders/s、第 2 輪 new 5.2 vs base 0.7；四筆的 p95 都是 3.5～3.9 s 或 0.96 s、rafFrames 2／12／18／3——歸因＝SwiftShader 首次渲染的 shader JIT 停頓（3～4 s）落不落在 2.5 s 量測窗內，屬**量測本身**，不是受測物（依據：慢的那輪 p95 恰為一次 3.5 s+ 停頓、幀數 2～3；快的那輪 p95 <1 s、幀數 12～18）。補做穩態探針（`scratchpad/sw-steady.mjs`：預覽頁 eye×8、暖機 12 s、fps() ×5）：decal=1 中位 208、decal=0 中位 256，但樣本 36～454 極散且軟體 GL 下 fps() 200+ 不合理（rAF 未被渲染阻塞，量的是 rAF 節奏不是渲染）→ 同樣不可信。**處置**：不宣告 D5 通過；需要真判定的話，選項：甲 接受「0 error＋硬體 GL 1.01×＋軟體路徑本就 0.3 fps 不可玩」為足夠（等同 09-06 M-4 簽字的延伸）／乙 duel-perf 各 ≥5 輪並把量測窗改到停頓之後（改治具，約 40 分鐘）／丙 真低階機。**要使用者裁。**
- D6 後半：`duel-drive` 真對局 3 場（seed 11）**errors 0、ys3d true、abOnAllUnits true、burn 4／trait 3** ✅（`decal-evidence/duel-drive.json`）。
- D7：見 commit 訊息與回報（`git diff --stat` 逐檔）。
- **D5 處置：使用者 2026-09-06 深夜裁定甲**（「依照建議開工」）——接受「SwiftShader 編譯執行 0 error＋硬體 GL 1.01×＋軟體路徑本就 0.3 fps 不可玩」為足夠；比值條不再追。本卷結案。
