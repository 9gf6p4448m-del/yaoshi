# 第三輪對抗覆審：批 2-a 傷害可讀性（worktree agent-a89f00b808f993be2 @ 07f821f）

**結論：不可合併。** 四件產品改動本身站得住（多條有紅→綠證據），但有三條阻擋項：
① 一條驗收條件在「同一條子判準實測紅」之後 5 分鐘被加上閘門，未走 `02 §2.1`；
② 報告停在三版，數字與敘述跟最終治具／最終實作對不上；
③ 唯一能證明通過的那批證據全在 `.gitignore` 之內，併進 main 的證據是二版、`verdict.R2=false`。

基準＝`git merge-base main HEAD` = `75e1bfd`。本輪自己跑的：`node --test tests/*.test.mjs`（9/9 綠）、
`node --check` 五支治具、兩支自寫的離線重算腳本（scratchpad/recheck.mjs、drop.mjs，只讀不寫 repo）。
瀏覽器治具**未跑**（唯讀限制＋埠占用會量到錯版本的風險），改為讀作者留在 `.claude/tmp/out/` 的原始 run log 與 pix.json。

---

## 1. 前兩輪 findings 三態裁定

### 第一輪（對 6bbc0ac）

| # | 項目 | 三態 | 依據（檔案:行號／實測） |
|---|---|---|---|
| HIGH-1 | R2 用差分遮罩＝刺激自選遮罩 | **真的修好** | 改剪影遮罩（把那一尊藏到相機看不到的層再重畫一幀差分）：`tests/tools/dmg-readability.mjs:63-90`、`:763-775`（四張圖同一次凍結拍完，虛擬時鐘 warp）。maskFrac 進判定＝`tinyMask <0.1`（`:900-902`）。hitstop 有樣本（v8-pix-1 via.hitstop=4、v8-pix-3=8）。同一支判準跑基準 v0.48：p50 **0.04**／≥25 **0/12**，新版 p50 60.7／45.71＝鑑別力成立 |
| HIGH-2 | P4 側別自我比對 | **真的修好** | 期望側別改由引擎真相 `fights[].beatsShown[]` 推：`tests/tools/closeup-judge.mjs:348-405`；突變版（tside 還原成 b.side）sideOk 7/20 → P4=FAIL（v7-judge-mut.txt），現版 sideOk 29/29、sideCoverage 96.7 → P4=PASS（v7-judge-now.txt） |
| M-1 | R6 fps 訊號不可信 | **真的修好（改標未驗證）**，新治具零實跑 | 報告 §1 R6、§3.3-1 明寫「未驗證」。`tests/tools/dmg-perf-pairs.mjs` 只過 `node --check`，`.claude/tmp/out` 內**沒有任何 pairs 輸出檔** |
| M-2 | 殘影餘裕只 40ms | **真的修好（第四版才真的）** | `index.html:4898-4913` 整段改 WAAPI 單一動畫。紅→綠**同一支判準**：v7-dom-1 R3=FAIL（超時 3 條，最長 432ms）／v7-dom-3 FAIL（414ms）→ v8-dom-1／v8-dom-3 **R3=PASS**（ghostGoneBad 0）。本輪最乾淨的一條 |
| M-3 | hotseat 玩家二無紅暈 | **真的修好** | `index.html:4731-4741`（pwHumanSides 判 ai 旗標）、`:5040`；`tests/lineup-order.test.mjs:65-85` 三條斷言、席位 id 刻意都不給 0。保留一點：只驗純函式，兩位真人的端到端沒有實跑 |
| M-4 | `.unit` 整類豁免 under 旁證 | **真的修好** | `tests/tools/closeup-drive.mjs:207-212`（取樣點補回 UNIT_DY、改用 classList）；v7-judge-now `underCrossCol=0`＝這條降級路徑當下不承重 |

### 第二輪（對 912ad9e，判不可合併）

| # | 項目 | 三態 | 依據 |
|---|---|---|---|
| a | R2 整條 null 蓋掉會紅的子判準 | **真的修好** | `dmg-readability.mjs:954-962` R2 依方案 A 硬判（中位≥25 且 ≥25 比例≥0.70 ＋三子判準）；`:942-943` 兩條 0 樣本 fail-closed。v8-base-1 R2=FAIL、v8-pix-1/3 R2=PASS |
| b | underCrossCol 全域降級＝FAIL→PASS | **真的修好** | `closeup-judge.mjs:396,431-434` 改 per-record `thisSideOk`；突變版即使 underCrossCol=1 仍整體 FAIL |
| c | P4 側別分母 10 筆靜默丟 | **真的修好** | `closeup-judge.mjs:386-401`（3D 方框拿不到退回 #dL/#dR DOM 方框）＋`:467-470` 印 sideNoBox／sideCoverage；鍵加場次（`:351-360`）消掉假 ambiguous。實測 sideNoBox=0、sideAmbiguous=0、coverage 96.7% |
| d | pwHurtEdge 漏 amount>0 | **真的修好** | `index.html:4815-4817`，與 `:5009` 派 ys:fx-hit 的閘門逐字對齊（同一條 `(b.amount|0)>0`） |
| e | 同屏配額 vs judge 矛盾 | **真的修好** | `index.html:4829-4837` 回到單一 MAX_HITS（淘汰順序改「先踢最舊的『−1 隻』」）；`closeup-judge.mjs:477` 仍硬判 maxLive<=5，實測 maxLive=4 |
| f | dmg-perf-pairs 空場景假綠 | **表面修好** | `dmg-perf-pairs.mjs:31-49`（__stage 補 #dL/#dR）、`:51-59`（__liveCount）、`:121-131`（aligned／triOk／liveOk／verOk 四道閘門）程式都在，但**一次都沒跑過**＝無紅→綠證據。因 R6 已標未驗證，不列阻擋 |
| g | ash-freeze F1（不屬本卷） | **部分違規** | 報告 §R8 內文正確（歸因產品既有缺陷、另開單、不宣告通過），但**結論表第 25 行**寫「R8 ✅ … ash-freeze-probe F1 0／F6 0」＝把 F1 的綠當通過證據，與內文「連跑 5 次＝2/0/0/1/2」矛盾。見 MEDIUM-D |

---

## 2. 移動及格線掃描

比對範圍（`git diff 75e1bfd...HEAD`）：`closeup-judge.mjs`、`closeup-drive.mjs`、`dmg-readability.mjs`（新檔 984 行全讀）、`dmg-perf-pairs.mjs`、`dmg-redstat.mjs`（離線統計、不判定）、`dmg-sheet.py`、`tests/lineup-order.test.mjs`、凍結檔本身。

**結果：找到一條（HIGH-A）。其餘為加嚴或中性：**
- 加嚴：對照尊門檻 `>=25`→`>=10`（`dmg-readability.mjs:933,941`）、badCtrl 由只印變硬判、P4 新增 `sideJudged>0 && sideOk===sideJudged`（`closeup-judge.mjs:478`）、`unmeasured===0`、R2 兩條 0 樣本 fail-closed。
- 使用者已同意（凍結檔修訂 2-A）：主門檻改分布式、+40ms 位移 ≤4px 閘門、燒毀 |Δ|≤10（原 5）。
- 中性：`/^−\d+$/` 對 `.unit` 放行但改成硬比對 `text==='−1 隻'`；`unitOneToOne` 在舊錄影 unitN=0 時回 null（有註明正式守衛在 R4）。
- 離線重判模式（`dmg-readability.mjs:419-433`）**不能挑樣本**（吃整份 pix.json、只重跑 judgePix），這點沒問題；但它就地覆寫 verdict（見 MEDIUM-E）。

---

## 3. 新 findings

### HIGH-A｜Δ200 子判準的位移閘門＝未經同意的移動及格線
**位置**：`tests/tools/dmg-readability.mjs:910-913`（move200）、`:937-939`（back200Judgeable／back200Unjudged／badBack）；commit `686515c`。
**怎麼觸發**：使用者 2026-09-08 裁定的方案 A 寫的是「三個子判準**全部硬判**：|Δ200| ≤ 5」。686515c 給這一條額外加了「+200ms 那一幀方框相對命中前位移 >4px 就記成不可判」。這個閘門在數學上**只能把違規變成不可判**，不可能反向，依 `02 §2.1` 一律視為讓通過機率上升。
**怎麼證明（時序就是證據）**：
- 01:10:34 `2568952` 落地方案 A（無此閘門）
- 01:11 `v7-pix-3` 實跑 → `VERDICT R2=FAIL R2main=PASS R2sub=FAIL`，唯一紅的就是 `R2 +200ms 未回到原值 Δ=-11.86（門檻 ±5）`
- 01:15:26 `686515c` 加上閘門
- 01:54 `v8-pix-3` → `R2=PASS`
**程序缺件（四項全缺）**：凍結檔沒有「修訂 2-B」（686515c 沒動任何文件）；報告也沒提這個閘門（§1 R2 只寫 +40ms 那一個）；沒有改前／改後各跑一次的數字；沒有使用者針對這一條的同意。
**例外條款不適用**：`v7-pix-1` 在**無閘門**下 R2sub=PASS（back200MaskAbsMax 4.3），所以原條件不是恆假、不是「無論實作對錯都不可能通過」。
**緩解（我自己算的，作者沒附）**：在最終證據 v8 上，**有無這個閘門結論相同**——
v8-pix-1 無閘門 n=14 違規 0；有閘門 n=8 違規 0（被剔 6 筆 Δ200＝0.49／2.58／1.74／−2.56／−0.94／−0.46，全在 ±5 內）。
v8-pix-3 無閘門 n=11 違規 0；有閘門 n=10 違規 0（被剔 1 筆 Δ200＝0.08）。
＝要補的是程序（凍結檔修訂＋使用者針對這一條裁定），不是重做實作。但在補上之前，這條擋合併。
**附帶風險**：閘門在 v8-pix-1 讓 6/14（43%）樣本變「不可判」。若日後閃紅真的不衰退（真實退化），這 43% 會靜默消失，|Δ200|≤5 的抓取力被實質削掉一大塊。

### HIGH-B｜報告與最終狀態不一致（宣稱完工卻貼的不是實際輸出）
**位置**：`docs/experiments/2026-09-07-dmg-readability-report.md:19,28,92-93,197,210-214,239`。
- 報告仍寫「R2 **門檻未訂**（分布交裁）」「治具的 R2 判定欄位固定回 null」「還要裁的只剩一件」「下一步：R2 的門檻由使用者訂」——而凍結檔已有修訂 2-A、治具 `:954-962` 已按方案 A 硬判。**同一個 commit（2568952）改了凍結檔與治具，卻沒改報告這幾處。**
- 報告 §1 R2 引用的分布（36 筆／p50 34.9／≥25 28 筆 78%）不是任何一次現行治具的產出。現行治具最終的數字是：seed1 n=14 p50 60.7 ≥25 14/14；seed3 n=11 p50 45.71 ≥25 11/11；基準 n=12 p50 0.04 ≥25 0。
- 報告 §1 R3／§3.0 MEDIUM-2 描述的實作是「transition .04s＋保險絲 +70」，最終實作是 WAAPI 單一動畫＋保險絲 +80（`index.html:4898-4913`，07f821f）。R3 的紅暈數字（151／160ms）是 v5 的，v8 是 152／163ms。
**為什麼是 HIGH**：使用者只會讀報告。照現在這份報告，他會以為 R2 還沒判、還等他裁；實際上治具已經在判、而且過了。

### HIGH-C｜通過用的證據沒進版，進版的證據是二版且 R2=false
**位置**：`docs/experiments/2026-09-07-dmg-readability-evidence/`（committed）vs `.claude/tmp/out/v8-*`（被 `.gitignore:2` 的 `.claude/` 排除）。
committed 的 `pix-seed1.json`／`pix-seed3.json`／`v2-pix-seed*.json` 全是二版錄影，`verdict.res.R2` 都是 `false`、沒有剪影欄位；`v2-pix-seed3` 的 maskP50=19.74、maskGe25=3/7（照方案 A 是 FAIL）。
合併後 repo 裡的「證據」與報告結論、與治具現行判準三方對不上，而真正的 v8 產物會隨 tmp 消失。
**怎麼修**：把 v8 四份（v8-pix-1／v8-pix-3／v8-base-1／v8-dom-1,3）的 json＋log 複製進 evidence 目錄，報告貼 VERDICT 原文。

### MEDIUM-D｜結論表把 ash-freeze F1 的綠當通過
`報告:25` 寫「R8 ✅ … ash-freeze-probe F1 0／F6 0（142 段）」，與 `報告:175-181`（連跑 5 次＝2/0/0/1/2、基準 3 次＝0/2/0、另開單）矛盾，也違反本卷「不得把它標成已修或拿它的綠當證據」。改法：結論表只留 F6 0，F1 改寫「另開單、不列入本卷判定」。

### MEDIUM-E｜離線重判就地覆寫 verdict、且不留判準版本戳
`dmg-readability.mjs:419-433`：judge 模式讀 pix.json → `J.verdict = v` → `writeFileSync` 蓋回原檔，沒記「這份 verdict 是哪一版判準／哪個 commit／何時算的」。判準還在動的階段，事後無法分辨某份綠是哪一把尺量的。建議存 `verdictHistory[]` 或至少 `judgedAt` + `git rev-parse HEAD`。

### MEDIUM-F｜「已知代價：約兩成閃得不明顯」與實測不符
凍結檔修訂 2-A 寫「分布式門檻放行**約兩成**的命中閃得不明顯」。實測（我自己重算，scratchpad/drop.mjs）：
- v8-pix-1：非燒毀樣本 20 筆，位移閘門剔掉 6 筆（30%），被剔那批 p50 **28.97**、≥25 只有 3/6
- v8-pix-3：20 筆剔掉 **9 筆（45%）**，被剔那批 p50 只有 **7.53**、≥25 只有 3/9
把被剔的算回去（真機上使用者看得到的是全部命中）：seed1 p50 37.48／≥25 比例 0.85；seed3 p50 41.29／≥25 比例 **0.70（正好卡在門檻上，餘裕 0.00）**。
兩件要寫進凍結檔：① 真實「閃得不明顯」的比例是 15%～30%，不是「約兩成」；② seed3 的餘裕是 0.00。
（好消息：基準跑同一套是 p50 0.04／比例 0.00，閘門沒有製造紅——鑑別力站得住。）

### MEDIUM-G｜R7 的 contact sheet 是二版產出，不是待審版本的畫面
`contact-sheet.png` 由 `d9b8887`（09-07 21:23）產出，而**貼片人形本體染紅**是 `8b4e0d8`（23:49）、殘影改 WAAPI 是 `07f821f`（09-08 01:17）。報告把這張圖列為「給使用者看的東西」，但它顯示的不是要合併的那一版（cell ② 標的「方框 Δ +26.46」還是被廢掉的方框平均量法）。使用者據以判斷「閃紅不刺眼／殘影有掉血感」的圖與碼對不上。

### LOW-H｜summary 兩個死欄位會誤導
`dmg-readability.mjs:982`：`rejBurn: seqs.filter(x => x.burnIn)`、`rejOffDuel: seqs.filter(x => !x.duelOn)`——`burnIn`／`duelOn` 這兩個屬性從來沒被寫進 seq 物件（`:904-918` 沒有），所以 rejBurn 恆 0、rejOffDuel 恆等於全部樣本數（v8-pix-1 印 25＝全部）。正確的逐項計數在 `maskDropped`。

### LOW-I｜永不消失的殘影會被靜默排除
`dmg-readability.mjs:487`：`ghosts = S.ghosts.filter(g => g.gone !== null)`——最嚴重的失敗（殘影根本不收）gone 是 null，直接被排除在 ghostSlow 之外。由 S.ends（收場後 ghosts!==0）兜底，但要等整場結束才抓得到。建議 gone===null 直接記違規。

### LOW-J｜R8 的檔案清單漏列兩支
`報告:182-183` 說 diff 只含 index.html、js/duel-figures.js、js/creature-figures.js、tests/tools/dmg-*、closeup-judge.mjs、docs，實際另有 `tests/lineup-order.test.mjs`、`tests/tools/closeup-drive.mjs`（都在凍結檔 R8 允許的「治具」範圍內，只是清單沒更新）。

### 產品碼新 bug：**沒找到**
逐條查過並排除：
- WAAPI 殘影（`index.html:4898-4913`）：`fill:"forwards"` 沒有釘死 opacity 的坑——onfinish 直接 `gh.remove()`，重入時先 `cancel()` 再重設 inline style；cancel 不觸發 onfinish，不會提前移除；保險絲 +80 仍在 GHOST_MS+100 窗內。R5 清場（`:4768`）另外硬清。
- hotseat 兩側：`pwHumanSides` 對 null/undefined 座位不誤判（測試 `:81-84` 有覆蓋）。
- `pwScreenOf` 側別修法：本輪四個 commit 沒動 `pwScreenOf`，側別修正在 `:4810` 的 `tside`，與 `:5009` 的 `foeSide` 同源；P4 突變測試證紅。
- 跳字池：淘汰迴圈的 `PW_DMG_POOL.push(old)` 沒有 done() 的去重守衛，但 `done()` 的 `if(el.parentNode)` 與 `__gen` 世代戳記（`:4841,4848`）已擋掉重複 push 與跨世代誤刪。
- `creature-figures.js:650` 新寫的 `u.uRimPower.value` 對 glow 材質不是回歸（`:332` 所有材質的初值都是 RIM.power）。
- `duel-figures.js` 的 `setHitTint`：`resetFigure`（`:361`）在 `reset()` 之前先歸零，`setCloth` 與 base 快照不會互相汙染。

---

## 4. 我自己跑過的驗證

```
$ node --test tests/*.test.mjs
tests 9  pass 9  fail 0
（aistake / conscap / duel-desync / legend / lineup-order / nightrules / review / roles-balance / wish16）

$ node --check tests/tools/{dmg-perf-pairs,dmg-readability,closeup-judge,closeup-drive,dmg-redstat}.mjs → 全 OK
```
離線重算（自寫、唯讀）：HIGH-A／MEDIUM-F 的數字，來源 `.claude/tmp/out/{v8-pix-1,v8-pix-3,v8-base-1,v7-pix-1,v7-pix-3}/pix.json`。
**未跑**：任何瀏覽器治具（唯讀限制＋埠占用會量到錯版本）。R0／R1／R3／R5 採信作者留下的 run log 與 VERDICT 原文，
並確認那幾份 log 的時間戳都晚於最後一個產品 commit：07f821f 01:17 → v7-r0-1/2/3 01:30–01:50（P0=PASS ×3）、
v8-pix-1/3 01:54–01:56（R1=PASS、R2=PASS）、v8-base-1 01:58（R1=FAIL、R2=FAIL＝鑑別力）、v8-dom-1/3 02:00–02:02（R3/R4/R5/WIRE=PASS）。

## 5. 阻擋條目（修完可再議，不需重做實作）

1. **HIGH-A**：Δ200 位移閘門走 `02 §2.1`——凍結檔補「修訂 2-B」（原標準錯在哪／為什麼現在才知道／改前改後實測數字，數字本報告已備好）＋取得使用者針對**這一條**的明確同意。不同意就把閘門拿掉重跑（依 v8 資料拿掉也會過）。
2. **HIGH-B**：報告改寫成第四版現況（R2 已判方案 A 並貼 v8 VERDICT 原文；R3 改寫成 WAAPI；數字全換成 v8）。
3. **HIGH-C**：把 v8 的 pix／dom／base 產物與 log 進版到 evidence 目錄，取代或並列二版那批。

MEDIUM-D／G 建議一併修（都是一兩行的文件更正）；E／F 建議修；LOW-H／I／J 可記錄不修。

**最後一句：不可合併（阻擋條目＝HIGH-A、HIGH-B、HIGH-C）。**
