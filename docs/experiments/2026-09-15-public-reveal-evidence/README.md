# 公開 v0.57.8 三人逐件揭盅：實際流程證據

2026-09-15，直接瀏覽 https://9gf6p4448m-del.github.io/yaoshi/ 。瀏覽器讀得 RELEASE_VERSION=0.57.8（舊 VERSION 仍為 0.56e），CFG.T 前後均 650。未修改產品、未 route 替換、未改 S/history 或演出時長、未按跳過。這是桌機 Chromium 的 844×390 視口，非手機真機。

## 成功重現

`node tests/tools/public-reveal-capture.mjs 3`

seed 3，solo 青面攤主，正常走新手頁及主按鈕，`pickMark(2)`；以產品出價入口 `openSheet`／五次 `bump(1)`／`closeSheet` 對槽 0、2 各出 5 命，其他出價為零，然後依序按開標／下一件。沒有直接寫入標書或結算結果。

第一夜槽 1 虎爺印自然有三位 AI 同場競標：收驚婆 2、閭山法師 5、孝女白琴 2，閭山法師得標。同槽三張均已實際讀圖：

- `seed-3/slot-1-push.png`：鏡頭推近虎爺，中央資訊卡已撤下，桌前可見錢柱。
- `seed-3/slot-1-gold.png`：同一隻虎爺向勝方飛移、外緣亮金橙色，桌前錢柱仍在；不是結果卡假裝的得標畫面。飛行中的模型上半部會碰到頂部 HUD／畫面上缘，保留此限制。
- `seed-3/slot-1-card.png`：居中卡顯示「3 人出價／3 人比標」、三筆 2／5／2，勝方膠囊亮金色，按鈕等待玩家按下一件。

`seed-3/capture.json` 記錄 URL、版本、seed、輸入策略、原始公告／結果文字、同槽事件序列；errors=[]，執行退出碼 0。事件間隔保持公開演出：槽 1 推鏡至得標約 820ms，得標至結果卡約 920ms。

## 未命中的搜尋與限制

- 根目錄的截圖／capture.json 是 seed 1 僅槽 2 出 5 命的完整探索局：各槽 1 人，不能算三人驗收。
- `seed-2/` 是槽 0、2 各出 5 命：公告為 2／2／4／0 人，foundThree=false，不能算三人驗收。
- 三張檔名由事件後時間取樣取得；以上結論另以讀圖確認，不靠檔名或事件本身宣稱美術通過。
