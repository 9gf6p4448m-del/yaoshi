# 計畫：招式演出卷（2026-09-12，取代招式可辨性卷批 1–3）

> 讀者：製作人與接手的 agent。前身：`2026-09-11-plan-fx-legibility.md`（可辨性卷）。本卷是方向重定，不是新功能。

## 0. 一句話
**0.55 批 0 的「徽記剪影」手段做錯了：目標（招式一眼能辨）不變，手段改成「本體動作＋小型紙紮道具＋受招方反應」，先做一支原型讓製作人挑，挑定再鋪 27 支。**

## 1. 為什麼重定（證據）
1. 兩輪盲讀：批 0 六讀者只認出千里眼銅鈴；刀印帽回修後三對六讀者 12 格 0/3（`2026-09-12-emblem-fix-report.md` §9）。六位讀者不約而同說 16 張只剩「金鈴／藍刀劍／紅雙尖角／紅階梯塊」四個族群。
2. 製作人親看材料（`…-emblem-fix-evidence/sheets-after/a06.png`、`…-b0-evidence/sheets-after/a11.png`）：純色無光照 billboard 貼在有描邊光影的紙紮模型上，像剪貼畫，尺寸比角色大，出現消失沒有紙紮質感。
3. 結構性原因：可辨性計畫把「讀者能說出法寶名稱」當主要資訊，於是要一個 logo；但畫面裡已有那件法寶的 3D 本體（虎爺印＝那隻虎），logo 是第二份、風格不合的身分載體。

## 2. 目標重定
招式可辨 ＝ 觀眾一眼看出 **(a) 誰對誰做了什麼**（傷害／護盾／偷取／詛咒／群體）**(b) 哪一系**（三系語彙沿用 `2026-09-11-plan-fx-legibility.md` §7 的顏色與材質）**(c) 法寶身分為次要**，由 3D 本體＋拍賣桌熟悉度承擔。

## 3. 手段規範
- 每支招＝**本體動作**（既有節點做前傾／彈跳／張口／自割，沒骨骼就整體位移＋squash-stretch）＋**小型紙紮道具**（印文、紙血條、紅帽、紙錢……有厚度＋墨線描邊＋輕微翹曲）＋**受招方反應**（壓扁／抖動／後退／迷途打轉）。
- 禁：純色無光照 billboard 當主視覺；道具尺寸超過施招本體高的 1/2；與本體動作無連動的懸浮圖示。
  - **道具尺寸上限暫為記錄項（§7.2 Q5 裁定）**：「施招本體高」目前沒有治具量得出來，批 1 前補一支
    「量 figure bounding box 高度 × 對照該招道具峰值尺寸」的治具，**輸出成表、只記錄不擋批**；
    等有 9 支的實測分布再決定門檻要不要進 P 閘。**記錄項不等於規則失效**——明顯超過仍是違規要自己抓；
    由建構上成立的那道防線（尺寸唯一來源＝`vocab.js` 的 `ICON` 表、`o.size` 拒收）照舊有效。
- 徽記剪影（`emblems.js`）降級為「受招方身上的小印記」或不再使用；`vocab.js` 三系語彙表、`PW_FX.TRAIT_MS_BY_TIER`、phase gate、N11/N12 尺寸防線、L10 治具全部沿用。
- tier 1 短版 260ms 保住辨識元素（本體動作＋受招反應至少各一拍）。

## 4. 流程（品味迴圈）
1. **原型**：虎爺印一支、三版（A 撲咬為主／B 印為主／C 香火為主），每版 6 幀 sheet＋GIF／WebM，走 `threejs-visual-loop` 自評兩輪 → 製作人挑。
2. **語彙定稿**：依挑定版把「本體動作／道具／反應」三件寫成三系語彙（每系各一段），寫進 ART_BIBLE §10 取代徽記段。
3. **鋪 27 支**：三批 **香火 9 → 祖靈 9 → 陰氣 9**（§7.2 Q2 裁定改序，原寫「祖靈 9／香火 9／陰氣 9」），
   每批 P0–P8 閘門；每批合併前製作人看 sheet。各批範本招＝香火 `biteGamble` 虎爺印（E 轉正式）／
   祖靈 `eliteSelfCut` 獻祭刀／陰氣 `hauntLost` 魔神仔紅帽（Q1），範本招單獨走完 P0–P8、
   製作人簽字後才鋪其餘 8 支。**香火排第一的理由**：E 定稿在香火、範本已完工，且香火的腳下語彙
   （`st.ring` 貼桌陣）是唯一已存在的積木——祖靈要等 `st.pillar`、陰氣要等 `st.stain`。
4. 三尊大招（tier 3）不在本卷。

## 5. 閘門 P0–P8（凍結；數字沿用可辨性卷同名閘門，改的只有 P4 的題目）
- P0 等價：`trace-eq` 對基準逐位元組相等（＝L5）。
- P1 一招一組「動作＋道具＋反應」，登記表可機械檢查（＝L1 改物件）。
- P2 phase gate 機械判（＝L2）。
- P3 對比：道具在暗紅桌面／紫夜空上的 ΔE 與面積，門檻沿用 L3（0.8%／ΔE）；量測位置 844×390@2x、bloom 0.7、seed 7。
- **P4 盲讀（主閘門，改題）**：三對 fresh 讀者、同材料管線（`blindread-sheet.mjs`）。每格三題：Q1「這招在做什麼」（五選一：打／護／偷／咒／群體）、Q2「打到哪一方」、Q3「哪一系」；通過＝三對中 ≥2 對兩位皆 Q1–Q3 全對。法寶名稱改為第四題只記錄不判。P4-pre 材料鑑別力沿用修訂四後的對照組。
- P5 短版原生合身（＝L6）、P6 短版品質下限（＝L7）、P7 效能（＝L8：fps 比值 ≥0.95、draw call 絕對 ≤1000）、P8 零錯＋規則測試全綠（＝L9）。
- 範圍與文件同 L11／L12。

## 6. 退路
`?fxvocab=1` 看 0.55 徽記版；預設 0.54 演出（本日 0.55.1 上線）。`?fxtier=0` 仍回 v0.53。

## 7. 待製作人裁（原型交付後）
① 三版挑一（或混合）② 印文要不要帶字（「虎」／篆紋／無字）③ 短版 260ms 要不要提到 300ms 給本體動作留空間（改數字＝改判準，另簽）。

### 7.1 裁定（2026-09-12 晚，原型 A/B/C 交付後）
① 合成 **D 版＝C 的華麗（金箔顆粒流、紙錢炸、印文燒出）＋B 的落地重音（大印砸落、震動、貼桌陣）**；② 印文一律「虎」；③ 短版 **改 300ms**（使用者同晚追加裁定「直接做 300 確保動作都讀得到」；記於 `2026-09-10-acceptance-fx-tiers.md` §2.1 修訂；代價每場對決約 +5–10% 時長）。原型 worktree `agent-a0917744177b854a1`（A/B/C 在 `3e4348a`），證據 `docs/experiments/2026-09-12-tiger-proto-evidence/`。
- **D → E（同晚）**：D 把 B 的橫移＋砸落插進 C 的動作裡，t1 300ms 下落印拍只剩 2 幀亮相；製作人問「能不能動作維持 C 的華麗、只在最後是 B 的蓋印」→ 裁定 **E 版＝C 動作原封不動、大印於撲的同時在獵物正上方垂直落下、落地時刻＝咬中時刻（震動／貼桌陣／紙錢炸／印文燒紅疊同一衝擊拍）**；印面配置改「暗印面＋鎏金印身＋鎏金陽刻虎字」（bloom 吃字的解法）；t1 維持 300。300 下 23 支既有短版 fill 紅＝時間軸寫死 260 絕對毫秒，另派純重構改比例（260 下 sigdump 逐位元組相同為判準）。
- **E 定稿（同晚）**：使用者裁 E 過、大印上的「虎」字取丁＝接受現況（玩家要讀的是蓋上去的印文；大印只需讀得出是一枚印）。E＝語彙範本（worktree `agent-a0917744177b854a1` `e0b3383`，`?proto=tigerE`，`js/trait-fx/proto/tiger.js`；t1 衝擊拍 3 幀正面亮相、draw call 168）。下一步：流程 §4-2 語彙定稿 → §4-3 香火系第一批（虎爺印 E 轉正式）。

### 7.2 語彙定稿裁定（2026-09-12 晚，Q1–Q10 **全部照建議答案**）

語彙文件＝`docs/design/2026-09-12-fx-vocab-draft.md`（已改標「定稿」），權威落點＝`ART_BIBLE.md` §10.2／10.3／10.7。

| # | 結論 |
|---|---|
| **Q1** | 各系範本招：香火＝`biteGamble` 虎爺印（E 轉正式）／祖靈＝`eliteSelfCut` 獻祭刀／陰氣＝`hauntLost` 魔神仔紅帽；兩支盲讀標竿（射日神弓、虎姑婆指甲）留作「不要動壞」的對照組，不當範本。 |
| **Q2** | 批序改 **香火 9 → 祖靈 9 → 陰氣 9**（§4-3 已同步改寫）。 |
| **Q3** | **本卷不做受招方非等比壓扁**（`w.mo.s`／`scaleBone` 的 `scl` 都是純量，要做得動引擎層＝另開一卷）；替代＝等比壓縮＋骨骼高頻抖＋邊光暴亮三件一起上。 |
| **Q4** | 祖靈保留「丁 日與雷」第四家族，但寫死限縮：只有射日 `sun`、雷女 `bolt` 兩支可用。 |
| **Q5** | 道具尺寸上限「≤ 本體高 1/2」補治具，但**只當記錄項不擋批**（§3 已加註）；記錄項不等於規則失效。 |
| **Q6** | 陰氣「禁幾何規整的圓與方」**新增明文例外：`swarmPierce` 陰陽眼銅錢**（GLB 有 `CoinA`／`CoinB`，外圓內方＝法寶身分）。 |
| **Q7** | 陰氣形狀語彙由「缺一角的形」改為「**邊緣不規則、末端下垂**」；「**規則形被撕掉一角**」歸香火，只給 `swarmLastStand` 破軍旗的殘旗。 |
| **Q8** | 三尊三招（`eliteBlind`／`wardGuardAll`／`hauntAnswer`）**語彙與色票納入、盲讀閘門不納入**；另立「`traitfx-drive --tier=3` clean 且視覺回歸不退」的機械項。 |
| **Q9** | ART_BIBLE §10 **只換兩小節、新增一小節**：§10.2 徽記規則→道具規則、§10.3 因果三段→三拍結構與衝擊拍、新增 §10.7 道具家族與反應家族；§10.1／10.4／10.5／10.6 保留。`vocab.js` 另加 `MOVE_SPEC[trId]={prop,act,react}` 三欄登記表供 P1 機械檢查（schema 見語彙檔 §E Q9）。**實測**：`tests/fxvocab.test.mjs` 從未讀 `ART_BIBLE.md`，改寫後 15 綠不變。 |
| **Q10** | `emblems.js` 的 27 個外框頂點表**保留、廢掉平面用法**：`st.icon`／`st.icons` 退給治具，道具一律走 `st.paperStamp`／`st.paperProps`。`EMBLEM_OF` 雙射與「≤24 外框頂點」不必重訂（改寫後的 §10.2 第 3 條仍是這一條）。 |

**批 1 動手前要先做完的三件**（不做完不准開）：① `st.paperStamp`／`st.solid`／`st.stick` 從原型
worktree `agent-a0917744177b854a1` 轉正進 `js/trait-fx.js`＋常駐預熱（`programsGrew` 維持 0）
② `vocab.js` 建 `MOVE_SPEC` 登記表並補 P1 的機械檢查 ③ Q5 的尺寸記錄治具。
`st.pillar`（祖靈）／`st.stain`（陰氣）／`st.paperProps`（群體道具）在各自那一批動手前補齊。

### 7.3 批 1 階段 A 的狀態（2026-09-13，範本招簽字前）

**三件前置全部完成**，虎爺印 E 已轉正，P0–P8 除 P4（盲讀，照 §4 流程要等 8 支鋪完）之外全綠。
報告＝`docs/experiments/2026-09-13-xianghuo-b1-report.md`，交付物＝`…-b1-evidence/`，
worktree `agent-af4b6dd83859a028a`（**未合併、未 push**，`index.html` 一行未動、版號未上）。

| 前置 | 落點 |
|---|---|
| ① `st.paperStamp`／`st.solid`／`st.stick` | 基準 `f8f7c98` 上已在 `js/trait-fx.js:715`／`:485`／`:794`，本階段驗證 `programsGrew=0` 並抽出共用零件 |
| ② `MOVE_SPEC` ＋ P1 機械檢查 | `js/trait-fx/vocab.js:176`（＋`:139` 白名單）、`tests/fxvocab.test.mjs:130-196` |
| ③ Q5 尺寸記錄治具 | `tests/tools/prop-size.mjs`（＋掛勾 `traitfx-preview.html:252`） |
| ＋ `st.paperProps`（香火批要用） | `js/trait-fx.js:837` |

**下一步＝製作人看 sheet／GIF 簽字**（§4 流程：範本招簽字後才准鋪其餘 8 支）。
同時要裁的：大印尺寸超過「本體高 1/2」36%（規則要量 scale 還是量包圍盒）、
語彙檔 §C 八處動詞正規化裡的兩處、`st.paperProps` 不做墨線邊的規格例外。
